# NFC-e (E0): base da escolha do modo de emissão — Plano de Implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Deixar pronta a **base** que permite ao administrador escolher, em Configurações > Fiscal, como a NFC-e é emitida (**Nenhum**, **SEFAZ direto** com certificado digital A1 ou, só em desenvolvimento, o provedor de teste) e onde um provedor pago poderá entrar depois: (1) o **modo** guardado no banco e lido a cada emissão; (2) um **cofre** que guarda cifrados no banco o certificado A1 e o CSC, cadastrados pela tela e **nunca devolvidos** ao navegador; (3) as conferências do certificado (senha, validade, CNPJ da empresa); (4) os **campos da reforma tributária** (IBS/CBS) no cadastro fiscal; (5) a **trava de produção** atualizada para o modo direto. Ainda **não** existe o adaptador do modo direto: neste plano nenhuma nota nova é emitida.

**Architecture:** Um módulo de cofre (`fiscal/vault.py`, AES-256-GCM, chave mestra `FISCAL_SECRET_KEY`), um leitor de certificado (`fiscal/certificate.py`), um serviço de segredos (`fiscal_secrets.py`) e um **registro de modos** (`fiscal/registry.py`) que decide qual provedor atende cada emissão a partir do modo salvo. A migration 014 acrescenta `fiscal_settings.mode`, a tabela `fiscal_secrets` e as colunas de IBS/CBS. O frontend ganha o seletor de modo e o cartão do cofre (certificado e CSC) em Configurações > Fiscal e quatro campos fiscais novos.

**Tech Stack:** FastAPI/SQLAlchemy async/Alembic/pytest (backend); React 18, TypeScript estrito, React Query 5, Vitest (frontend). Uma dependência nova no backend: `cryptography` (cifra e leitura do `.pfx`).

**Spec:** `docs/superpowers/specs/2026-09-26-nfce-modo-direto-design.md` (§3 arquitetura, §5 dados, §6 telas, §8 etapa E0). **Bases já executadas:** C1a, C1b, C2, C3. Ficam para as próximas etapas: o **teste de fogo** (E1: endpoints da SEFAZ-PE, QR-Code v3, mapeamento do IBS/CBS), o **envio online** (E2), o evento de cancelamento e a inutilização (E3), a contingência offline (E4), a ida para produção (E5) e o adaptador de provedor (plano próprio, depois da escolha do provedor).

## Global Constraints

- **Segredo nunca volta para o navegador nem entra em log, mensagem de erro, auditoria ou teste.** As respostas da API só têm dados públicos: titular, CNPJ, vencimento, dias que faltam, impressão digital e o **ID** do CSC (o código dele nunca). O arquivo `.pfx` e as senhas só existem cifrados no banco e em memória durante uma operação.
- **Cofre:** AES-256-GCM, nonce aleatório de 12 bytes na frente do texto cifrado, e o **tipo do segredo vai como dado autenticado** (`certificate`, `certificate_password`, `csc_hml`, `csc_prod`): um texto cifrado copiado para outro tipo **não abre**. A chave mestra é `FISCAL_SECRET_KEY` (32 bytes em base64 **estrito**, com o preenchimento `=`; uma frase escolhida à mão não vira chave), lida **só** do ambiente (Coolify). Sem ela ou com ela inválida o cofre fica **indisponível**: a API recusa guardar (não guarda em claro) e a tela avisa. Trocar ou perder a chave invalida o que foi guardado; documentado.
- **Certificado só é aceito se:** o `.pfx` abre com a senha, tem até 1 MB, está dentro da validade e, quando a empresa já tem CNPJ salvo, **o CNPJ do certificado é o mesmo** (o CNPJ vem do OID ICP-Brasil 2.16.76.1.3.3 ou do CN "NOME:CNPJ"). Senha errada e arquivo inválido dão mensagens diferentes e **em português**. Certificado e senha são gravados **juntos, na mesma transação**: nunca um sem o outro.
- **O modo mora no banco** (`fiscal_settings.mode`, padrão `none`) e é lido a **cada** emissão, consulta, cancelamento e ciclo de reenvio: mudar a tela vale sem reiniciar. O atalho de desenvolvimento `FISCAL_PROVIDER=fake` só vale com `ENVIRONMENT=development` (em qualquer outro valor é ignorado) e, forçado, dispensa a consulta ao banco. O modo `fake` só pode ser **salvo** em desenvolvimento.
- **Modo escolhido sem adaptador não emite nada:** `sefaz_direto` devolve "sem provedor" até a etapa E2 (`mode_available` falso), então a emissão se comporta como desligada e o PDV segue normal. Nenhum caminho pode cair no provedor falso fora do desenvolvimento.
- **Trava de produção:** só passa com modo real (`REAL_MODES`) **e** disponível, emissão ligada com os dados da empresa, cofre aberto (chave mestra válida) com certificado que **decifra**, abre com a senha guardada, está dentro da validade, **traz o CNPJ** e é o da empresa, CSC de produção que decifra, ao menos uma venda de teste em homologação (`hml-`, autorizada **ou** cancelada) e a confirmação do contador. Como o adaptador direto só chega na E2, **produção continua inalcançável**: é o comportamento desejado, não um defeito.
- **Campos de IBS/CBS (reforma tributária) são só guardados:** CST (3 dígitos), cClassTrib (6 dígitos), alíquotas de IBS e CBS. **Os valores vêm do contador** e o mapeamento no XML será conferido no teste de fogo (E1): não inventar valores, não exigi-los para emitir ainda (a regra de "pronto para emitir" não muda) e não confiar neles em nenhum cálculo neste plano. Aliquota com vírgula na tela vira ponto no envio.
- **Migration 014 aditiva e reversível** (colunas anuláveis ou com valor padrão, tabela nova), sem mudança de dados. Não rodar contra produção; **sem deploy** sem o "pode fazer o deploy" explícito do usuário. Ao fazer o deploy, criar antes a variável `FISCAL_SECRET_KEY` no Coolify (o `docker-compose.yaml` só repassa as variáveis listadas nele, por isso ela foi acrescentada lá).
- **Testes nunca usam certificado real.** Os certificados de teste são gerados na hora com `cryptography` (`tests/fiscal_certs.py`), com CNPJ fictício (`11222333000181`). **Nada de dados reais** (CNPJ, endereço, nome) e nenhum segredo no repositório.
- **Mensagens ao usuário em português.** Backend: `cd backend && python -m pytest -q` (venv ativo). Frontend: em `frontend/`, `npx tsc -b` sem erros ao fim de toda tarefa e a suíte com a contagem prometida.
- **Não testado contra PostgreSQL real** (o Docker estava fora do ar ao escrever este plano): a migration 014 (tabela `fiscal_secrets` com `BYTEA`/`JSON`, colunas novas) foi conferida só pelo SQL gerado e por bancos falsos. Antes de qualquer deploy, rode `alembic upgrade head` e um envio de certificado num Postgres local ou em homologação.
- **Arquivos de outra pessoa na árvore:** `.impeccable/config.json` e `frontend/src/pages/orders/NewOrder.tsx` podem ter alterações não commitadas do usuário; este plano **não** toca o `NewOrder.tsx`. Nunca use `git add .` nem `git add -A`.
- Commits terminam com a linha `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`. Comandos para o Git Bash, na raiz do repositório.
- **Cuidado nas verificações no navegador:** desligue a impressão automática do caixa (`pdv-printer` com `enabled:false`) antes; um diálogo real de impressão trava o painel. Painel oculto pausa o polling do React Query (force `document.visibilityState` como `'visible'`).

## Review Focus

1. **Vazamento de segredo:** certificado, senha ou código do CSC em resposta, exceção, log, evento de auditoria ou fixture. → `TestStatusENuncaVaza` e `TestRotas` (Task 3), `FiscalSecretsOut` (Task 3).
2. **Cofre fraco:** texto cifrado que abre em outro tipo, nonce repetido, chave curta ou inválida aceita, gravar em claro sem chave. → `TestChaveMestra`, `TestCifra` (Task 1) e os testes de cofre sem chave em `TestCertificado`/`TestCsc` (Task 3).
3. **Certificado aceito indevidamente:** vencido, CNPJ de outra empresa, senha errada, arquivo grande demais, senha e arquivo gravados pela metade. → `TestInspect`, `TestProblemas`, `TestVencimento` (Task 1) e `TestCertificado` (Task 3).
4. **Modo lido do lugar errado:** emissão usando um modo velho, provedor falso fora do desenvolvimento, `sefaz_direto` emitindo sem adaptador, consulta ao banco desnecessária no atalho de desenvolvimento. → `TestModoEmVigor`, `TestAdaptadores`, `TestCarregar` (Task 4).
5. **Trava de produção burlável:** modo `none`/`fake` aceito, certificado vencido ou de outro CNPJ, CSC de produção ausente, adaptador inexistente contado como pronto. → `TestProblems`, `TestSaveSettings` (Task 4).
6. **Migration 014:** reversível, sem perder dados, colunas de IBS/CBS nos três lugares, tabela do cofre com `kind` único. → `test_fiscal_e0_schema.py` (Task 2).
7. **Regressão do fluxo já pronto:** emissão, cancelamento e reenvio devem continuar iguais com o provedor vindo do registro (o mesmo que o C3 fazia). → `TestEntryPoints`, `test_fiscal_cancel.py`, `test_fiscal_retry.py` (Task 4).
8. **Trava só olhando o cadastro:** conferir os dados públicos (`secrets_status`) sem abrir o cofre deixa passar chave mestra perdida, certificado sem CNPJ ou ainda não válido. → `TestProblems` (Task 8).
9. **Frontend:** o secreto ficando em estado depois do envio, ids repetidos nos dois blocos de CSC, remoção sem confirmação, botão habilitado sem arquivo/senha, aviso do cofre ausente, `mode` sumindo do corpo do PUT, vírgula das alíquotas. → roteiros das Tasks 5 e 6.

---

## Estrutura de arquivos

| Arquivo | Responsabilidade |
|---|---|
| `backend/app/services/fiscal/vault.py` | Cifra e decifra segredos (AES-256-GCM, tipo como dado autenticado) |
| `backend/app/services/fiscal/certificate.py` | Lê o `.pfx`, extrai titular/CNPJ/validade, confere a empresa e classifica o vencimento |
| `backend/app/services/fiscal_secrets.py` | Guarda, remove, lista (sem segredo) e carrega certificado e CSC |
| `backend/app/services/fiscal/registry.py` | Modo efetivo e provedor de cada modo (o único lugar que decide) |
| `backend/app/services/fiscal_golive.py` (substituir) | Trava de produção pelo modo direto |
| `backend/alembic/versions/014_add_fiscal_mode_secrets_ibs_cbs.py` | Modo, tabela do cofre e colunas de IBS/CBS |
| `models/product.py`, `models/order.py`, `models/fiscal.py`, `models/__init__.py`, `schemas/fiscal.py`, `fiscal/rules.py`, `core/config.py`, `requirements.txt`, `fiscal_service.py`, `fiscal_cancel.py`, `fiscal_retry.py`, `api/v1/fiscal.py` (modificar) | Ligar tudo |
| `frontend/src/types/fiscal.ts`, `api/fiscal.ts`, `utils/fiscal.ts` (modificar) | Tipos, chamadas e regras puras |
| `frontend/src/components/fiscal/FiscalFieldsForm.tsx`, `pages/settings/FiscalTab.tsx` (modificar) | Campos de IBS/CBS; modo de emissão e cofre |
| `docker-compose.yaml`, `backend/.env.example`, `docs/fiscal/README.md` (modificar) | `FISCAL_SECRET_KEY` e o que o E0 faz |
| `frontend/nginx.conf` (modificar) | Limite explícito do corpo da API (certificado de até 1 MB) |

---

### Task 1: Cofre e leitor de certificado

**Files:**
- Create: `backend/app/services/fiscal/vault.py`, `backend/app/services/fiscal/certificate.py`, `backend/tests/fiscal_certs.py`
- Modify: `backend/requirements.txt`, `backend/app/core/config.py`
- Test: `backend/tests/test_fiscal_vault.py`, `backend/tests/test_fiscal_certificate.py`

**Interfaces:**
- Produces (`vault.py`): `VaultUnavailable`, `vault_available()`, `encrypt(kind, plaintext: bytes) -> bytes` (nonce de 12 bytes + texto cifrado, com o `kind` como dado autenticado) e `decrypt(kind, blob) -> bytes`. Produces (`certificate.py`): `CertInfo`, `CertificateError`, `inspect_pfx(data, password) -> CertInfo`, `days_left(info, now)`, `problems_for_company(info, cnpj, now) -> list[str]`, `expiry_level(info, now) -> 'ok' | 'warn' | 'critical'` (aviso a 60 dias, crítico a 15). Produces (`tests/fiscal_certs.py`): `make_pfx(...)`, gerador de `.pfx` descartável para os testes. `Settings.FISCAL_SECRET_KEY` (vazio por padrão).

- [ ] **Step 1: Escrever os testes que falham**

Create `backend/tests/fiscal_certs.py`:

```python
"""Certificados A1 de TESTE gerados na hora (nenhum certificado real entra no repositório nem nos testes)."""
import datetime as dt

from cryptography import x509
from cryptography.hazmat.primitives import hashes, serialization
from cryptography.hazmat.primitives.asymmetric import rsa
from cryptography.hazmat.primitives.serialization import pkcs12
from cryptography.x509.oid import NameOID

PASSWORD = "senha-de-teste"
CNPJ = "11222333000181"


def make_pfx(*, cnpj_in_cn: str | None = CNPJ, cnpj_in_san: str | None = None, password: str = PASSWORD,
             valid_from_days: int = -30, valid_for_days: int = 335, with_key: bool = True) -> bytes:
    """Um .pfx com titular 'EMPRESA TESTE LTDA:<cnpj>' (padrão ICP-Brasil), opcionalmente com o CNPJ no SubjectAltName."""
    key = rsa.generate_private_key(public_exponent=65537, key_size=2048)
    common_name = f"EMPRESA TESTE LTDA:{cnpj_in_cn}" if cnpj_in_cn else "EMPRESA TESTE LTDA"
    name = x509.Name([x509.NameAttribute(NameOID.COMMON_NAME, common_name)])
    now = dt.datetime.now(dt.timezone.utc)
    builder = (
        x509.CertificateBuilder()
        .subject_name(name)
        .issuer_name(name)
        .public_key(key.public_key())
        .serial_number(x509.random_serial_number())
        .not_valid_before(now + dt.timedelta(days=valid_from_days))
        .not_valid_after(now + dt.timedelta(days=valid_from_days + valid_for_days))
    )
    if cnpj_in_san:
        der = bytes([0x13, len(cnpj_in_san)]) + cnpj_in_san.encode()  # PrintableString
        other = x509.OtherName(x509.ObjectIdentifier("2.16.76.1.3.3"), der)
        builder = builder.add_extension(x509.SubjectAlternativeName([other]), critical=False)
    cert = builder.sign(key, hashes.SHA256())
    return pkcs12.serialize_key_and_certificates(
        b"teste", key if with_key else None, cert, None, serialization.BestAvailableEncryption(password.encode())
    )
```

Create `backend/tests/test_fiscal_vault.py`:

```python
"""Cofre dos segredos fiscais: cifra com a chave mestra do servidor, amarra cada segredo ao seu tipo e nunca vaza a causa do erro."""
import base64
import os

import pytest

# `app.core.config` exige estas variáveis já no import.
os.environ.setdefault("DATABASE_URL", "postgresql+asyncpg://teste:teste@localhost/teste")
os.environ.setdefault("DATABASE_URL_SYNC", "postgresql://teste:teste@localhost/teste")
os.environ.setdefault("SECRET_KEY", "teste")

from app.services.fiscal import vault  # noqa: E402

KEY = base64.b64encode(bytes(range(32))).decode()
OTHER_KEY = base64.b64encode(bytes(range(1, 33))).decode()


@pytest.fixture
def key(monkeypatch):
    monkeypatch.setattr(vault.app_settings, "FISCAL_SECRET_KEY", KEY)


class TestChaveMestra:
    def test_sem_chave_o_cofre_esta_indisponivel_e_a_mensagem_ensina_a_criar(self, monkeypatch):
        monkeypatch.setattr(vault.app_settings, "FISCAL_SECRET_KEY", "")
        assert vault.vault_available() is False
        with pytest.raises(vault.VaultUnavailable, match="FISCAL_SECRET_KEY.*secrets.token_bytes"):
            vault.encrypt("certificate", b"x")

    def test_chave_invalida_ou_do_tamanho_errado_e_recusada(self, monkeypatch):
        for bad in ("nao-e-base64!!", base64.b64encode(b"curta").decode(), base64.b64encode(bytes(64)).decode()):
            monkeypatch.setattr(vault.app_settings, "FISCAL_SECRET_KEY", bad)
            assert vault.vault_available() is False, bad

    def test_aceita_base64_padrao_e_urlsafe(self, monkeypatch):
        raw = bytes(range(200, 232))
        for text in (base64.b64encode(raw).decode(), base64.urlsafe_b64encode(raw).decode().rstrip("=")):
            monkeypatch.setattr(vault.app_settings, "FISCAL_SECRET_KEY", text)
            assert vault.decrypt("csc", vault.encrypt("csc", b"ok")) == b"ok"


class TestCifra:
    def test_ida_e_volta(self, key):
        secret = b"conteudo do pfx \x00\xff com bytes quaisquer"
        assert vault.decrypt("certificate", vault.encrypt("certificate", secret)) == secret

    def test_o_texto_cifrado_nao_contem_o_segredo_e_muda_a_cada_vez(self, key):
        first, second = vault.encrypt("csc_prod", b"TOKEN-SECRETO"), vault.encrypt("csc_prod", b"TOKEN-SECRETO")
        assert b"TOKEN-SECRETO" not in first and first != second  # nonce novo a cada chamada
        assert len(first) == vault.NONCE_BYTES + len(b"TOKEN-SECRETO") + 16

    def test_segredo_de_um_tipo_nao_abre_como_outro_tipo(self, key):
        blob = vault.encrypt("certificate_password", b"senha")
        with pytest.raises(vault.VaultUnavailable, match="certificate"):
            vault.decrypt("certificate", blob)

    def test_chave_mestra_trocada_ou_dado_corrompido_pede_para_cadastrar_de_novo(self, key, monkeypatch):
        blob = vault.encrypt("csc_hml", b"abc")
        monkeypatch.setattr(vault.app_settings, "FISCAL_SECRET_KEY", OTHER_KEY)
        with pytest.raises(vault.VaultUnavailable, match="Cadastre-o de novo"):
            vault.decrypt("csc_hml", blob)
        monkeypatch.setattr(vault.app_settings, "FISCAL_SECRET_KEY", KEY)
        with pytest.raises(vault.VaultUnavailable):
            vault.decrypt("csc_hml", blob[:-1] + bytes([blob[-1] ^ 1]))
        with pytest.raises(vault.VaultUnavailable):
            vault.decrypt("csc_hml", b"curto")
```

Create `backend/tests/test_fiscal_certificate.py`:

```python
"""Certificado digital A1: senha, validade, CNPJ do titular e o nível de alerta de vencimento. Só certificados de teste gerados na hora."""
from datetime import datetime, timedelta, timezone

import pytest

from app.services.fiscal import certificate as cert
from tests.fiscal_certs import CNPJ, PASSWORD, make_pfx

NOW = datetime.now(timezone.utc)


class TestInspect:
    def test_le_titular_cnpj_e_validade(self):
        info = cert.inspect_pfx(make_pfx(), PASSWORD)
        assert info.subject == f"EMPRESA TESTE LTDA:{CNPJ}" and info.cnpj == CNPJ
        assert info.not_before < NOW < info.not_after and len(info.fingerprint) == 64

    def test_cnpj_tambem_vem_do_campo_icp_brasil_do_subject_alt_name(self):
        info = cert.inspect_pfx(make_pfx(cnpj_in_cn=None, cnpj_in_san=CNPJ), PASSWORD)
        assert info.cnpj == CNPJ

    def test_certificado_sem_cnpj_devolve_none(self):
        assert cert.inspect_pfx(make_pfx(cnpj_in_cn=None), PASSWORD).cnpj is None

    def test_senha_errada_ou_arquivo_que_nao_e_pfx_da_mensagem_em_portugues(self):
        for data, password in ((make_pfx(), "errada"), (b"isto nao e um pfx", PASSWORD), (b"", PASSWORD)):
            with pytest.raises(cert.CertificateError, match="confira a senha"):
                cert.inspect_pfx(data, password)

    def test_pfx_sem_chave_privada_e_recusado(self):
        with pytest.raises(cert.CertificateError, match="chave privada"):
            cert.inspect_pfx(make_pfx(with_key=False), PASSWORD)

    def test_a_impressao_digital_e_estavel_para_o_mesmo_certificado(self):
        pfx = make_pfx()
        assert cert.inspect_pfx(pfx, PASSWORD).fingerprint == cert.inspect_pfx(pfx, PASSWORD).fingerprint
        assert cert.inspect_pfx(make_pfx(), PASSWORD).fingerprint != cert.inspect_pfx(pfx, PASSWORD).fingerprint


class TestProblemas:
    def test_certificado_valido_da_empresa_certa_nao_tem_problema(self):
        info = cert.inspect_pfx(make_pfx(), PASSWORD)
        assert cert.problems_for_company(info, CNPJ) == []
        assert cert.problems_for_company(info, "11.222.333/0001-81") == []  # o CNPJ da empresa pode vir com pontuação

    def test_vencido(self):
        info = cert.inspect_pfx(make_pfx(valid_from_days=-400, valid_for_days=365), PASSWORD)
        assert any("venceu em" in p for p in cert.problems_for_company(info, CNPJ))

    def test_ainda_nao_vale(self):
        info = cert.inspect_pfx(make_pfx(valid_from_days=10, valid_for_days=365), PASSWORD)
        assert any("só vale a partir de" in p for p in cert.problems_for_company(info, CNPJ))

    def test_cnpj_diferente_da_empresa(self):
        info = cert.inspect_pfx(make_pfx(), PASSWORD)
        assert any("diferente" in p for p in cert.problems_for_company(info, "99888777000166"))

    def test_sem_cnpj_no_certificado_avisa_em_vez_de_aceitar_calado(self):
        info = cert.inspect_pfx(make_pfx(cnpj_in_cn=None), PASSWORD)
        assert any("não achei o CNPJ" in p for p in cert.problems_for_company(info, CNPJ))

    def test_sem_cnpj_da_empresa_cadastrado_so_confere_a_validade(self):
        assert cert.problems_for_company(cert.inspect_pfx(make_pfx(), PASSWORD), None) == []


class TestVencimento:
    def level(self, days_left: int) -> str:
        info = cert.inspect_pfx(make_pfx(valid_from_days=-10, valid_for_days=10 + days_left), PASSWORD)
        return cert.expiry_level(info)

    def test_niveis(self):
        assert self.level(200) == "ok"
        assert self.level(59) == "warn"
        assert self.level(14) == "critical"
        assert self.level(-5) == "critical"  # já venceu

    def test_dias_que_faltam(self):
        info = cert.inspect_pfx(make_pfx(valid_for_days=100, valid_from_days=0), PASSWORD)
        assert cert.days_left(info) in (98, 99)
        assert cert.days_left(info, now=info.not_after + timedelta(days=3)) == -3
```

- [ ] **Step 2: Rodar e ver falhar**

```bash
cd backend && python -m pytest -q tests/test_fiscal_vault.py tests/test_fiscal_certificate.py; cd ..
```
Expected: FAIL (`No module named 'app.services.fiscal.vault'` na coleta dos dois arquivos).

- [ ] **Step 3: Implementar**

Create `backend/app/services/fiscal/vault.py`:

```python
"""Cofre dos segredos fiscais (certificado digital, senha, CSC, token do provedor).

Os segredos ficam cifrados no banco (AES-256-GCM). A chave mestra vem da variável de ambiente `FISCAL_SECRET_KEY` (só existe no
servidor, nunca no banco nem no repositório). Sem ela, nada é cifrado nem decifrado. Cada segredo é amarrado ao seu `kind` (dado
autenticado adicional): um segredo copiado para a linha de outro tipo não decifra.
"""
import base64
import binascii
import os

from cryptography.exceptions import InvalidTag
from cryptography.hazmat.primitives.ciphers.aead import AESGCM

from app.core.config import settings as app_settings

NONCE_BYTES = 12
KEY_BYTES = 32

HOW_TO_CREATE_KEY = 'gere uma com: python -c "import secrets,base64;print(base64.b64encode(secrets.token_bytes(32)).decode())"'


class VaultUnavailable(Exception):
    """Sem chave mestra válida (ou segredo que não decifra): a mensagem já é para mostrar ao usuário."""


def _master_key() -> bytes:
    raw = (app_settings.FISCAL_SECRET_KEY or "").strip()
    if not raw:
        raise VaultUnavailable(f"A chave mestra dos segredos fiscais (FISCAL_SECRET_KEY) não está configurada no servidor: {HOW_TO_CREATE_KEY}.")
    try:
        key = base64.b64decode(raw, validate=True)
    except (binascii.Error, ValueError):
        try:
            key = base64.urlsafe_b64decode(raw + "=" * (-len(raw) % 4))
        except (binascii.Error, ValueError):
            raise VaultUnavailable("FISCAL_SECRET_KEY não é um base64 válido.") from None
    if len(key) != KEY_BYTES:
        raise VaultUnavailable(f"FISCAL_SECRET_KEY deve ter {KEY_BYTES} bytes em base64: {HOW_TO_CREATE_KEY}.")
    return key


def vault_available() -> bool:
    try:
        _master_key()
    except VaultUnavailable:
        return False
    return True


def encrypt(kind: str, plaintext: bytes) -> bytes:
    """Devolve `nonce (12 bytes) + texto cifrado`. O nonce é novo a cada chamada."""
    nonce = os.urandom(NONCE_BYTES)
    return nonce + AESGCM(_master_key()).encrypt(nonce, plaintext, kind.encode())


def decrypt(kind: str, blob: bytes) -> bytes:
    key = _master_key()
    try:
        return AESGCM(key).decrypt(blob[:NONCE_BYTES], blob[NONCE_BYTES:], kind.encode())
    except (InvalidTag, ValueError):
        raise VaultUnavailable(
            f"Não foi possível abrir o segredo '{kind}': a chave mestra mudou ou o dado está corrompido. Cadastre-o de novo."
        ) from None
```

Create `backend/app/services/fiscal/certificate.py`:

```python
"""Certificado digital A1 (.pfx): confere senha, validade e a que CNPJ ele pertence, sem guardar nada."""
import hashlib
import re
from dataclasses import dataclass
from datetime import datetime, timezone

from cryptography import x509
from cryptography.hazmat.primitives import hashes, serialization
from cryptography.hazmat.primitives.serialization import pkcs12
from cryptography.x509.oid import NameOID

# ICP-Brasil: o CNPJ do titular vai no campo "otherName" do SubjectAltName, com este identificador.
ICP_BRASIL_CNPJ_OID = x509.ObjectIdentifier("2.16.76.1.3.3")

WARN_DAYS = 60
CRITICAL_DAYS = 15


class CertificateError(ValueError):
    """Problema com o arquivo ou a senha do certificado; a mensagem já é para mostrar ao usuário."""


@dataclass(frozen=True)
class CertInfo:
    subject: str
    cnpj: str | None
    not_before: datetime
    not_after: datetime
    fingerprint: str  # SHA-256 do certificado, para conferir sem expor nada


def _digits(text: str) -> str:
    return re.sub(r"\D", "", text)


def _cnpj_from(cert: x509.Certificate) -> str | None:
    try:
        san = cert.extensions.get_extension_for_class(x509.SubjectAlternativeName).value
        for name in san.get_values_for_type(x509.OtherName):
            if name.type_id == ICP_BRASIL_CNPJ_OID:
                digits = _digits(name.value[2:].decode("latin-1"))  # tira o cabeçalho DER (tag e tamanho)
                if len(digits) == 14:
                    return digits
    except (x509.ExtensionNotFound, ValueError):
        pass
    common_name = cert.subject.get_attributes_for_oid(NameOID.COMMON_NAME)
    if common_name and ":" in common_name[0].value:  # padrão ICP-Brasil: "NOME DA EMPRESA:11222333000181"
        digits = _digits(common_name[0].value.rsplit(":", 1)[1])
        if len(digits) == 14:
            return digits
    return None


def inspect_pfx(data: bytes, password: str) -> CertInfo:
    """Abre o .pfx e devolve os dados públicos do certificado. Levanta CertificateError com mensagem em português."""
    try:
        key, cert, _ = pkcs12.load_key_and_certificates(data, password.encode("utf-8"))
    except (ValueError, TypeError):
        raise CertificateError("Não foi possível abrir o certificado: confira a senha e se o arquivo é um .pfx (A1).") from None
    if cert is None or key is None:
        raise CertificateError("O arquivo não contém o certificado com a chave privada (é preciso um A1 completo).")
    name = cert.subject.get_attributes_for_oid(NameOID.COMMON_NAME)
    return CertInfo(
        subject=name[0].value if name else cert.subject.rfc4514_string(),
        cnpj=_cnpj_from(cert),
        not_before=cert.not_valid_before_utc,
        not_after=cert.not_valid_after_utc,
        fingerprint=hashlib.sha256(cert.public_bytes(serialization.Encoding.DER)).hexdigest(),
    )


def days_left(info: CertInfo, now: datetime | None = None) -> int:
    """Dias até vencer (negativo se já venceu)."""
    return (info.not_after - (now or datetime.now(timezone.utc))).days


def problems_for_company(info: CertInfo, company_cnpj: str | None, now: datetime | None = None) -> list[str]:
    """O que impede usar este certificado para esta empresa (lista vazia = serve)."""
    now = now or datetime.now(timezone.utc)
    problems: list[str] = []
    if info.not_after < now:
        problems.append(f"o certificado venceu em {info.not_after:%d/%m/%Y}")
    elif info.not_before > now:
        problems.append(f"o certificado só vale a partir de {info.not_before:%d/%m/%Y}")
    if company_cnpj:
        if info.cnpj is None:
            problems.append("não achei o CNPJ dentro do certificado para conferir com o da empresa")
        elif info.cnpj != _digits(company_cnpj):
            problems.append("o CNPJ do certificado é diferente do CNPJ da empresa cadastrado")
    return problems


def expiry_level(info: CertInfo, now: datetime | None = None) -> str:
    """ok | warn (até 60 dias) | critical (até 15 dias ou vencido), para a tela."""
    left = days_left(info, now)
    if left <= CRITICAL_DAYS:
        return "critical"
    return "warn" if left <= WARN_DAYS else "ok"
```

Salve o script abaixo **fora do repositório** (por exemplo `%TEMP%/apply_edits.py`) e rode-o **da raiz do repositório**. Ele troca cada trecho antigo pelo novo, aceita arquivos LF ou CRLF e **para** se algum trecho não aparecer exatamente uma vez. Aqui ele acrescenta a dependência e a chave mestra na configuração:

```python
import sys
from pathlib import Path

EDITS = {
  "backend/requirements.txt": [
    [
      "Biblioteca de criptografia explícita (certificado A1 e cofre dos segredos)",
      "httpx==0.27.2\n",
      "httpx==0.27.2\ncryptography>=42\n"
    ]
  ],
  "backend/app/core/config.py": [
    [
      "Chave mestra do cofre dos segredos fiscais",
      "    FISCAL_PROVIDER: str = \"none\"\n",
      "    FISCAL_PROVIDER: str = \"none\"\n    # Chave mestra (32 bytes em base64) que cifra o certificado, o CSC e os tokens guardados no banco. Só existe no servidor.\n    FISCAL_SECRET_KEY: str = \"\"\n"
    ]
  ]
}

for rel, edits in EDITS.items():
    path = Path(rel.split("::")[0])
    text = path.read_text(encoding="utf-8", newline="")
    crlf = "\r\n" in text
    text = text.replace("\r\n", "\n")
    for label, old, new in edits:
        count = text.count(old)
        if count != 1:
            sys.exit(f"{rel}: '{label}' casou {count} vezes (esperado 1)")
        text = text.replace(old, new)
    path.write_text(text.replace("\n", "\r\n") if crlf else text, encoding="utf-8", newline="")
    print(f"ok {rel}")
```

```bash
python apply_edits.py
cd backend && pip install -r requirements.txt; cd ..
```
Expected: `ok backend/requirements.txt` e `ok backend/app/core/config.py`; o `pip` instala (ou já tem) o `cryptography`.

- [ ] **Step 4: Rodar e ver passar**

```bash
cd backend && python -m pytest -q; cd ..
```
Expected: `203 passed` (182 já existentes + 21 novos).

- [ ] **Step 5: Commit**

```bash
git add backend/app/services/fiscal/vault.py backend/app/services/fiscal/certificate.py backend/app/core/config.py backend/requirements.txt backend/tests/fiscal_certs.py backend/tests/test_fiscal_vault.py backend/tests/test_fiscal_certificate.py
git commit -m "feat(fiscal): cofre cifrado de segredos e leitura do certificado digital A1"
```

---

### Task 2: Modo, tabela do cofre e campos de IBS/CBS (migration 014)

**Files:**
- Create: `backend/alembic/versions/014_add_fiscal_mode_secrets_ibs_cbs.py`
- Modify: `backend/app/models/product.py`, `backend/app/models/order.py`, `backend/app/models/fiscal.py`, `backend/app/models/__init__.py`, `backend/app/schemas/fiscal.py`, `backend/app/services/fiscal/rules.py`, `backend/tests/test_fiscal_schema.py`, `backend/tests/test_fiscal_c3_schema.py`
- Test: `backend/tests/test_fiscal_e0_schema.py`

**Interfaces:**
- Consumes: nada da Task 1 além do que já está no banco de testes falso.
- Produces: `fiscal_settings.mode` (`none` | `sefaz_direto` | `fake`, padrão `none`); tabela `fiscal_secrets` (`kind` único, `ciphertext` BYTEA, `meta` JSON só com o que não é secreto, `updated_at`, `updated_by_name`) e o modelo `FiscalSecret`; colunas `cst_ibs_cbs`, `c_class_trib`, `aliquota_ibs`, `aliquota_cbs` em `products`, `order_items` e `fiscal_defaults`; `FISCAL_FIELDS` com os quatro campos novos (rótulos e validação: 3 e 6 dígitos, alíquotas de 0 a 100); `FiscalSettingsIn.mode` (`fake` só em desenvolvimento) e `mode` em `FiscalSettingsOut`. A regra "pronto para emitir" **não** muda.

- [ ] **Step 1: Escrever os testes que falham**

Create `backend/tests/test_fiscal_e0_schema.py`:

```python
"""E0: modo de emissão, cofre de segredos e campos de IBS/CBS (regras, schemas, modelos e a migration 014)."""
import os
from decimal import Decimal

import pytest

# `app.core.config` exige estas variáveis já no import.
os.environ.setdefault("DATABASE_URL", "postgresql+asyncpg://teste:teste@localhost/teste")
os.environ.setdefault("DATABASE_URL_SYNC", "postgresql://teste:teste@localhost/teste")
os.environ.setdefault("SECRET_KEY", "teste")

from pydantic import ValidationError  # noqa: E402

from app.core.database import Base  # noqa: E402
import app.models  # noqa: E402,F401
from app.schemas.fiscal import FiscalDefaultIn, FiscalSettingsIn, FiscalSettingsOut  # noqa: E402
from app.schemas.product import ProductCreate, ProductOut  # noqa: E402
from app.services.fiscal import rules  # noqa: E402
from tests.test_fiscal_schema import sql_for  # noqa: E402

IBS = ("cst_ibs_cbs", "c_class_trib", "aliquota_ibs", "aliquota_cbs")


class TestRegrasIbsCbs:
    def test_cst_tem_3_digitos_e_a_classificacao_6(self):
        assert rules.normalize_field("cst_ibs_cbs", "000") == "000"
        assert rules.normalize_field("c_class_trib", "000001") == "000001"
        with pytest.raises(ValueError, match="CST do IBS/CBS deve ter 3 dígitos"):
            rules.normalize_field("cst_ibs_cbs", "00")
        with pytest.raises(ValueError, match="Classificação tributária do IBS/CBS deve ter 6 dígitos"):
            rules.normalize_field("c_class_trib", "1234")

    def test_aliquotas_de_0_a_100_com_4_casas_e_virgula_decimal(self):
        assert rules.normalize_field("aliquota_ibs", "0,1") == Decimal("0.1000")
        assert rules.normalize_field("aliquota_cbs", "0.9") == Decimal("0.9000")
        for bad in ("101", "-1", "NaN", "abc"):
            with pytest.raises(ValueError):
                rules.normalize_field("aliquota_cbs", bad)

    def test_vazio_vira_none_e_os_campos_entram_na_lista_fiscal(self):
        assert rules.normalize_field("c_class_trib", "") is None
        assert set(IBS) <= set(rules.FISCAL_FIELDS)

    def test_por_enquanto_ibs_cbs_nao_e_exigido_para_o_produto_ficar_pronto(self):
        # O que a nota exige de fato é definido na prova de fogo (E1), depois de ler a nota técnica; até lá nada muda no "pronto".
        merged = {field: None for field in rules.FISCAL_FIELDS} | {"ncm": "07020000", "origem": 0, "cfop": "5102", "cst_icms": "41"}
        assert rules.is_ready(merged)


class TestSchemas:
    def test_produto_e_padrao_da_categoria_aceitam_ibs_cbs(self):
        product = ProductCreate(name="BANANA", price="6.99", category="Frutas", cst_ibs_cbs="000", c_class_trib="000001", aliquota_cbs="0,9")
        assert (product.cst_ibs_cbs, product.c_class_trib, product.aliquota_cbs) == ("000", "000001", Decimal("0.9000"))
        assert FiscalDefaultIn(category="Frutas", aliquota_ibs="0,1").aliquota_ibs == Decimal("0.1000")
        assert set(IBS) <= set(ProductOut.model_fields)

    def test_campo_ibs_invalido_e_erro_de_validacao_em_portugues(self):
        with pytest.raises(ValidationError, match="CST do IBS/CBS deve ter 3 dígitos"):
            ProductCreate(name="X", price="1", category="C", cst_ibs_cbs="1")

    def test_modo_de_emissao_padrao_nenhum_e_saida_com_o_modo(self):
        assert FiscalSettingsIn().mode == "none" and "mode" in FiscalSettingsOut.model_fields

    def test_modos_validos_e_o_de_teste_so_em_desenvolvimento(self, monkeypatch):
        assert FiscalSettingsIn(mode="sefaz_direto").mode == "sefaz_direto"
        monkeypatch.setattr("app.schemas.fiscal.app_settings.ENVIRONMENT", "development")
        assert FiscalSettingsIn(mode="fake").mode == "fake"
        monkeypatch.setattr("app.schemas.fiscal.app_settings.ENVIRONMENT", "production")
        with pytest.raises(ValidationError, match="só existe em desenvolvimento"):
            FiscalSettingsIn(mode="fake")
        for bad in ("provider", "sefaz", ""):  # "provider" ainda não tem adaptador
            with pytest.raises(ValidationError):
                FiscalSettingsIn(mode=bad)


class TestMigration014:
    def test_cria_o_modo_o_cofre_e_as_colunas_de_ibs_cbs_e_desfaz(self):
        up = sql_for("013:014")
        assert "ALTER TABLE fiscal_settings ADD COLUMN mode VARCHAR(20) DEFAULT 'none' NOT NULL" in up
        assert "CREATE TABLE fiscal_secrets (" in up and "ciphertext BYTEA NOT NULL" in up and "UNIQUE (kind)" in up
        for table in ("products", "order_items", "fiscal_defaults"):
            for column in IBS:
                assert f"ALTER TABLE {table} ADD COLUMN {column} " in up, (table, column)
                assert column in Base.metadata.tables[table].columns
        secrets = Base.metadata.tables["fiscal_secrets"].columns.keys()
        body = up.split("CREATE TABLE fiscal_secrets (")[1].split(");")[0]
        assert all(f"\n    {column} " in body for column in secrets)
        down = sql_for("014:013", downgrade=True)
        assert "DROP TABLE fiscal_secrets" in down and "ALTER TABLE fiscal_settings DROP COLUMN mode" in down
        assert "ALTER TABLE products DROP COLUMN aliquota_cbs" in down
```

Mesmo procedimento de edição (salve como `apply_edits.py` fora do repositório e rode da raiz). Aqui ele ajusta os testes de schema de C1a/C3 para o campo novo da configuração:

```python
import sys
from pathlib import Path

EDITS = {
  "backend/tests/test_fiscal_schema.py": [
    [
      "Colunas de IBS/CBS e o modo vêm da migration 014",
      "LATER_SETTINGS_COLUMNS = {\"cancel_window_minutes\", \"production_confirmed_by\", \"production_confirmed_at\"}\n",
      "LATER_SETTINGS_COLUMNS = {\"cancel_window_minutes\", \"production_confirmed_by\", \"production_confirmed_at\", \"mode\"}\n# Colunas de IBS/CBS acrescentadas pela migration 014 (testadas em test_fiscal_e0_schema.py)\nIBS_FIELDS = {\"cst_ibs_cbs\", \"c_class_trib\", \"aliquota_ibs\", \"aliquota_cbs\"}\n"
    ],
    [
      "A migration 011 não cria as colunas de IBS/CBS",
      "            for field in FISCAL_FIELDS:\n                assert f\"ALTER TABLE {table} ADD COLUMN {field} \" in sql, (table, field)\n",
      "            for field in FISCAL_FIELDS:\n                if field in IBS_FIELDS:\n                    continue\n                assert f\"ALTER TABLE {table} ADD COLUMN {field} \" in sql, (table, field)\n"
    ],
    [
      "A migration 012 não cria as colunas de IBS/CBS",
      "                if column in LATER_SETTINGS_COLUMNS:\n                    continue\n",
      "                if column in LATER_SETTINGS_COLUMNS or column in IBS_FIELDS:\n                    continue\n"
    ]
  ],
  "backend/tests/test_fiscal_c3_schema.py": [
    [
      "A linha de configuração dos testes agora tem o modo de emissão",
      "regime=\"normal\", series=1, cancel_window_minutes=30,",
      "regime=\"normal\", mode=\"none\", series=1, cancel_window_minutes=30,"
    ]
  ]
}

for rel, edits in EDITS.items():
    path = Path(rel.split("::")[0])
    text = path.read_text(encoding="utf-8", newline="")
    crlf = "\r\n" in text
    text = text.replace("\r\n", "\n")
    for label, old, new in edits:
        count = text.count(old)
        if count != 1:
            sys.exit(f"{rel}: '{label}' casou {count} vezes (esperado 1)")
        text = text.replace(old, new)
    path.write_text(text.replace("\n", "\r\n") if crlf else text, encoding="utf-8", newline="")
    print(f"ok {rel}")
```

```bash
python apply_edits.py
```
Expected: `ok` para os dois arquivos.

- [ ] **Step 2: Rodar e ver falhar**

```bash
cd backend && python -m pytest -q tests/test_fiscal_e0_schema.py; cd ..
```
Expected: FAIL (`cannot import name 'FiscalSecret'` e a migration 014 ainda não existe).

- [ ] **Step 3: Implementar**

Create `backend/alembic/versions/014_add_fiscal_mode_secrets_ibs_cbs.py`:

```python
"""modo de emissão fiscal, cofre de segredos e campos de IBS/CBS

Revision ID: 014
Revises: 013
Create Date: 2026-09-26
"""
import sqlalchemy as sa
from sqlalchemy.dialects.postgresql import UUID

from alembic import op

revision = "014"
down_revision = "013"
branch_labels = None
depends_on = None

# Reforma tributária: grupo IBS/CBS por item (NT 2025.002). Os valores vêm do contador.
IBS_COLUMNS = [
    ("cst_ibs_cbs", sa.String(3)),
    ("c_class_trib", sa.String(6)),
    ("aliquota_ibs", sa.Numeric(7, 4)),
    ("aliquota_cbs", sa.Numeric(7, 4)),
]
IBS_TABLES = ("products", "order_items", "fiscal_defaults")


def upgrade() -> None:
    op.add_column("fiscal_settings", sa.Column("mode", sa.String(20), nullable=False, server_default="none"))
    op.create_table(
        "fiscal_secrets",
        sa.Column("id", UUID(as_uuid=True), primary_key=True, server_default=sa.text("gen_random_uuid()")),
        sa.Column("kind", sa.String(30), nullable=False, unique=True),
        sa.Column("ciphertext", sa.LargeBinary(), nullable=False),
        sa.Column("meta", sa.JSON(), nullable=True),
        sa.Column("updated_at", sa.DateTime(timezone=True), nullable=False, server_default=sa.text("now()")),
        sa.Column("updated_by_name", sa.String(255), nullable=True),
    )
    for table in IBS_TABLES:
        for name, type_ in IBS_COLUMNS:
            op.add_column(table, sa.Column(name, type_, nullable=True))


def downgrade() -> None:
    for table in reversed(IBS_TABLES):
        for name, _ in reversed(IBS_COLUMNS):
            op.drop_column(table, name)
    op.drop_table("fiscal_secrets")
    op.drop_column("fiscal_settings", "mode")
```

Mesmo procedimento de edição:

```python
import sys
from pathlib import Path

EDITS = {
  "backend/app/models/product.py": [
    [
      "Campos de IBS/CBS no produto",
      "    cst_cofins: Mapped[str | None] = mapped_column(String(2), nullable=True)\n",
      "    cst_cofins: Mapped[str | None] = mapped_column(String(2), nullable=True)\n    # Reforma tributária (grupo IBS/CBS, NT 2025.002): os valores vêm do contador\n    cst_ibs_cbs: Mapped[str | None] = mapped_column(String(3), nullable=True)\n    c_class_trib: Mapped[str | None] = mapped_column(String(6), nullable=True)\n    aliquota_ibs: Mapped[Decimal | None] = mapped_column(Numeric(7, 4), nullable=True)\n    aliquota_cbs: Mapped[Decimal | None] = mapped_column(Numeric(7, 4), nullable=True)\n"
    ]
  ],
  "backend/app/models/order.py": [
    [
      "Campos de IBS/CBS copiados para o item",
      "    cst_cofins: Mapped[str | None] = mapped_column(String(2), nullable=True)\n",
      "    cst_cofins: Mapped[str | None] = mapped_column(String(2), nullable=True)\n    # Reforma tributária (grupo IBS/CBS, NT 2025.002): os valores vêm do contador\n    cst_ibs_cbs: Mapped[str | None] = mapped_column(String(3), nullable=True)\n    c_class_trib: Mapped[str | None] = mapped_column(String(6), nullable=True)\n    aliquota_ibs: Mapped[Decimal | None] = mapped_column(Numeric(7, 4), nullable=True)\n    aliquota_cbs: Mapped[Decimal | None] = mapped_column(Numeric(7, 4), nullable=True)\n"
    ]
  ],
  "backend/app/models/fiscal.py": [
    [
      "Importar LargeBinary",
      "from sqlalchemy import Boolean, DateTime, ForeignKey, Integer, JSON, Numeric, String, Text, func\n",
      "from sqlalchemy import Boolean, DateTime, ForeignKey, Integer, JSON, LargeBinary, Numeric, String, Text, func\n"
    ],
    [
      "Modo de emissão",
      "    regime: Mapped[str] = mapped_column(String(10), nullable=False, default=\"normal\", server_default=\"normal\")\n",
      "    regime: Mapped[str] = mapped_column(String(10), nullable=False, default=\"normal\", server_default=\"normal\")\n    # Como a nota é emitida: none | sefaz_direto | fake (só em desenvolvimento). \"provider\" entra com o adaptador do provedor.\n    mode: Mapped[str] = mapped_column(String(20), nullable=False, default=\"none\", server_default=\"none\")\n"
    ],
    [
      "Campos de IBS/CBS no padrão da categoria",
      "    cst_cofins: Mapped[str | None] = mapped_column(String(2), nullable=True)\n",
      "    cst_cofins: Mapped[str | None] = mapped_column(String(2), nullable=True)\n    # Reforma tributária (grupo IBS/CBS, NT 2025.002): os valores vêm do contador\n    cst_ibs_cbs: Mapped[str | None] = mapped_column(String(3), nullable=True)\n    c_class_trib: Mapped[str | None] = mapped_column(String(6), nullable=True)\n    aliquota_ibs: Mapped[Decimal | None] = mapped_column(Numeric(7, 4), nullable=True)\n    aliquota_cbs: Mapped[Decimal | None] = mapped_column(Numeric(7, 4), nullable=True)\n"
    ],
    [
      "Cofre dos segredos",
      "    created_by_name: Mapped[str] = mapped_column(String(255), nullable=False)\n",
      "    created_by_name: Mapped[str] = mapped_column(String(255), nullable=False)\n\n\nclass FiscalSecret(Base):\n    \"\"\"Um segredo fiscal cifrado (ver app/services/fiscal/vault.py). `meta` guarda só o que NÃO é secreto (titular, vencimento...).\"\"\"\n\n    __tablename__ = \"fiscal_secrets\"\n\n    id: Mapped[uuid.UUID] = mapped_column(primary_key=True, default=uuid.uuid4)\n    kind: Mapped[str] = mapped_column(String(30), nullable=False, unique=True)  # certificate | certificate_password | csc_hml | csc_prod\n    ciphertext: Mapped[bytes] = mapped_column(LargeBinary, nullable=False)\n    meta: Mapped[dict | None] = mapped_column(JSON, nullable=True)\n    updated_at: Mapped[datetime] = mapped_column(\n        DateTime(timezone=True), server_default=func.now(), onupdate=func.now(), nullable=False\n    )\n    updated_by_name: Mapped[str | None] = mapped_column(String(255), nullable=True)\n"
    ]
  ],
  "backend/app/models/__init__.py": [
    [
      "Importar o cofre",
      "from app.models.fiscal import FiscalDefault, FiscalEvent, FiscalSettings\n",
      "from app.models.fiscal import FiscalDefault, FiscalEvent, FiscalSecret, FiscalSettings\n"
    ],
    [
      "Exportar o cofre",
      "    \"FiscalDefault\", \"FiscalEvent\", \"FiscalSettings\",\n",
      "    \"FiscalDefault\", \"FiscalEvent\", \"FiscalSecret\", \"FiscalSettings\",\n"
    ]
  ],
  "backend/app/services/fiscal/rules.py": [
    [
      "Campos de IBS/CBS",
      "FISCAL_FIELDS = (\"ncm\", \"cest\", \"origem\", \"cfop\", \"cst_icms\", \"aliquota_icms\", \"cst_pis\", \"cst_cofins\")\n",
      "FISCAL_FIELDS = (\n    \"ncm\", \"cest\", \"origem\", \"cfop\", \"cst_icms\", \"aliquota_icms\", \"cst_pis\", \"cst_cofins\",\n    \"cst_ibs_cbs\", \"c_class_trib\", \"aliquota_ibs\", \"aliquota_cbs\",\n)\n"
    ],
    [
      "Rótulos do IBS/CBS",
      "    \"cst_cofins\": \"CST do COFINS\",\n}",
      "    \"cst_cofins\": \"CST do COFINS\",\n    \"cst_ibs_cbs\": \"CST do IBS/CBS\",\n    \"c_class_trib\": \"Classificação tributária do IBS/CBS\",\n    \"aliquota_ibs\": \"Alíquota do IBS\",\n    \"aliquota_cbs\": \"Alíquota da CBS\",\n}"
    ],
    [
      "Validar o IBS/CBS",
      "    if name in (\"cst_icms\", \"cst_pis\", \"cst_cofins\"):\n        return _digits(value, label, 2)\n",
      "    if name in (\"cst_icms\", \"cst_pis\", \"cst_cofins\"):\n        return _digits(value, label, 2)\n    if name == \"cst_ibs_cbs\":\n        return _digits(value, label, 3)\n    if name == \"c_class_trib\":\n        return _digits(value, label, 6)\n    if name in (\"aliquota_ibs\", \"aliquota_cbs\"):\n        try:\n            rate = Decimal(str(value).replace(\",\", \".\"))\n        except InvalidOperation:\n            raise ValueError(f\"{label} inválida\") from None\n        if not rate.is_finite() or not Decimal(\"0\") <= rate <= Decimal(\"100\"):\n            raise ValueError(f\"{label} deve ficar entre 0 e 100\")\n        return rate.quantize(Decimal(\"0.0001\"))\n"
    ]
  ],
  "backend/app/schemas/fiscal.py": [
    [
      "Campos de IBS/CBS na entrada",
      "    cst_pis: str | None = None\n    cst_cofins: str | None = None\n\n    @field_validator(*FISCAL_FIELDS, mode=\"before\")",
      "    cst_pis: str | None = None\n    cst_cofins: str | None = None\n    cst_ibs_cbs: str | None = None\n    c_class_trib: str | None = None\n    aliquota_ibs: Decimal | None = None\n    aliquota_cbs: Decimal | None = None\n\n    @field_validator(*FISCAL_FIELDS, mode=\"before\")"
    ],
    [
      "Campos de IBS/CBS na saída",
      "class FiscalFieldsOut(BaseModel):\n    ncm: str | None = None\n    cest: str | None = None\n    origem: int | None = None\n    cfop: str | None = None\n    cst_icms: str | None = None\n    aliquota_icms: Decimal | None = None\n    cst_pis: str | None = None\n    cst_cofins: str | None = None\n",
      "class FiscalFieldsOut(BaseModel):\n    ncm: str | None = None\n    cest: str | None = None\n    origem: int | None = None\n    cfop: str | None = None\n    cst_icms: str | None = None\n    aliquota_icms: Decimal | None = None\n    cst_pis: str | None = None\n    cst_cofins: str | None = None\n    cst_ibs_cbs: str | None = None\n    c_class_trib: str | None = None\n    aliquota_ibs: Decimal | None = None\n    aliquota_cbs: Decimal | None = None\n"
    ],
    [
      "Modo de emissão na entrada",
      "    regime: Literal[\"normal\", \"simples\"] = \"normal\"\n    series: int = 1\n",
      "    regime: Literal[\"normal\", \"simples\"] = \"normal\"\n    # \"provider\" entra quando existir o adaptador do provedor; \"fake\" só em desenvolvimento.\n    mode: Literal[\"none\", \"sefaz_direto\", \"fake\"] = \"none\"\n    series: int = 1\n"
    ],
    [
      "O modo de teste só existe em desenvolvimento",
      "    @field_validator(\"cancel_window_minutes\")\n",
      "    @field_validator(\"mode\")\n    @classmethod\n    def fake_only_in_development(cls, v: str) -> str:\n        if v == \"fake\" and app_settings.ENVIRONMENT != \"development\":\n            raise ValueError(\"O modo de teste (provedor falso) só existe em desenvolvimento\")\n        return v\n\n    @field_validator(\"cancel_window_minutes\")\n"
    ],
    [
      "Importar a configuração",
      "from app.services.fiscal.rules import FISCAL_FIELDS, normalize_field\n",
      "from app.core.config import settings as app_settings\nfrom app.services.fiscal.rules import FISCAL_FIELDS, normalize_field\n"
    ],
    [
      "Modo de emissão na saída",
      "    regime: str\n    series: int\n    cancel_window_minutes: int\n",
      "    regime: str\n    mode: str = \"none\"\n    series: int\n    cancel_window_minutes: int\n"
    ]
  ]
}

for rel, edits in EDITS.items():
    path = Path(rel.split("::")[0])
    text = path.read_text(encoding="utf-8", newline="")
    crlf = "\r\n" in text
    text = text.replace("\r\n", "\n")
    for label, old, new in edits:
        count = text.count(old)
        if count != 1:
            sys.exit(f"{rel}: '{label}' casou {count} vezes (esperado 1)")
        text = text.replace(old, new)
    path.write_text(text.replace("\n", "\r\n") if crlf else text, encoding="utf-8", newline="")
    print(f"ok {rel}")
```

```bash
python apply_edits.py
```
Expected: `ok` para os seis arquivos.

- [ ] **Step 4: Rodar e ver passar**

```bash
cd backend && python -m pytest -q; cd ..
```
Expected: `212 passed` (203 + 9).

- [ ] **Step 5: Commit**

```bash
git add backend/alembic/versions/014_add_fiscal_mode_secrets_ibs_cbs.py backend/app/models/product.py backend/app/models/order.py backend/app/models/fiscal.py backend/app/models/__init__.py backend/app/services/fiscal/rules.py backend/app/schemas/fiscal.py backend/tests/test_fiscal_e0_schema.py backend/tests/test_fiscal_schema.py backend/tests/test_fiscal_c3_schema.py
git commit -m "feat(fiscal): modo de emissão, tabela do cofre e campos de IBS/CBS (migration 014)"
```

---

### Task 3: Serviço de segredos e rotas do cofre

**Files:**
- Create: `backend/app/services/fiscal_secrets.py`
- Modify: `backend/app/schemas/fiscal.py`, `backend/app/api/v1/fiscal.py`
- Test: `backend/tests/test_fiscal_secrets.py`

**Interfaces:**
- Consumes: `vault` e `certificate` (Task 1); `FiscalSecret` e `fiscal_settings.cnpj` (Task 2).
- Produces (`fiscal_secrets.py`): `save_certificate(db, pfx, password, company_cnpj, user_name) -> CertInfo`, `save_csc(db, environment, csc_id, token, user_name)`, `delete_certificate(db)`, `delete_csc(db, environment)`, `secrets_status(db) -> dict` (sem segredo), `load_certificate(db) -> (bytes, str)` e `load_csc(db, environment) -> (id, token)` (para o adaptador da E2). Limites: `.pfx` até 1 MB, código do CSC com 10 caracteres ou mais, ID só com dígitos (até 6). Rotas, **só administrador**: `GET /fiscal/secrets`, `PUT|DELETE /fiscal/secrets/certificate` (multipart: `file` + `password`), `PUT|DELETE /fiscal/secrets/csc/{environment}` (`{id, token}`); todas respondem `FiscalSecretsOut`. Erros de negócio viram 422 com mensagem em português; cofre sem chave mestra vira 409.

- [ ] **Step 1: Escrever o teste que falha**

Create `backend/tests/test_fiscal_secrets.py`:

```python
"""Segredos fiscais pela tela: certificado A1 e CSC cifrados no cofre, e a garantia de que nada secreto volta ao navegador."""
import asyncio
import base64
import json
import os
import uuid
from datetime import datetime, timedelta, timezone
from types import SimpleNamespace

import pytest

# `app.core.config` exige estas variáveis já no import.
os.environ.setdefault("DATABASE_URL", "postgresql+asyncpg://teste:teste@localhost/teste")
os.environ.setdefault("DATABASE_URL_SYNC", "postgresql://teste:teste@localhost/teste")
os.environ.setdefault("SECRET_KEY", "teste")

from fastapi import HTTPException  # noqa: E402
from fastapi.testclient import TestClient  # noqa: E402

from app.api.deps import get_current_user  # noqa: E402
from app.core.database import get_db  # noqa: E402
from app.main import app  # noqa: E402
from app.models.fiscal import FiscalSecret, FiscalSettings  # noqa: E402
from app.models.user import UserRole  # noqa: E402
from app.services import fiscal_secrets as secrets  # noqa: E402
from app.services.fiscal import vault  # noqa: E402
from tests.fiscal_certs import CNPJ, PASSWORD, make_pfx  # noqa: E402

KEY = base64.b64encode(bytes(range(32))).decode()
CSC_TOKEN = "TOKEN-DO-CSC-1234567890"


def run(coro):
    return asyncio.run(coro)


class MemoryDb:
    """Banco falso em memória para os segredos: guarda o que é adicionado e responde às consultas por tipo."""

    def __init__(self, settings=None):
        self.store: list[FiscalSecret] = []
        self.settings = settings
        self.commits = 0
        self.statements = []

    async def execute(self, statement):
        text = str(statement)
        self.statements.append(text)
        self.current = None
        if "fiscal_settings" in text:
            self.rows = [self.settings] if self.settings else []
        elif "fiscal_secrets.kind =" in text:
            kind = list(statement.compile().params.values())[0]
            self.rows = [row for row in self.store if row.kind == kind]
        else:
            self.rows = list(self.store)
        return self

    def scalar_one_or_none(self):
        return self.rows[0] if self.rows else None

    def scalars(self):
        return self

    def all(self):
        return list(self.rows)

    def add(self, obj):
        self.store.append(obj)

    async def delete(self, obj):
        self.store.remove(obj)

    async def commit(self):
        self.commits += 1


@pytest.fixture(autouse=True)
def vault_key(monkeypatch):
    monkeypatch.setattr(vault.app_settings, "FISCAL_SECRET_KEY", KEY)


def company(cnpj=CNPJ):
    return FiscalSettings(cnpj=cnpj, enabled=False, environment="homologacao", regime="normal", mode="none", series=1, cancel_window_minutes=30)


class TestCertificado:
    def test_guarda_o_pfx_e_a_senha_cifrados_e_so_dados_publicos_na_meta(self):
        db, pfx = MemoryDb(), make_pfx()
        info = run(secrets.save_certificate(db, pfx, PASSWORD, CNPJ, "Maria"))
        kinds = {row.kind: row for row in db.store}
        assert set(kinds) == {"certificate", "certificate_password"} and db.commits == 1
        assert pfx not in kinds["certificate"].ciphertext and PASSWORD.encode() not in kinds["certificate_password"].ciphertext
        assert vault.decrypt("certificate", kinds["certificate"].ciphertext) == pfx
        assert vault.decrypt("certificate_password", kinds["certificate_password"].ciphertext) == PASSWORD.encode()
        assert kinds["certificate"].meta["cnpj"] == CNPJ and kinds["certificate"].updated_by_name == "Maria"
        assert PASSWORD not in json.dumps(kinds["certificate"].meta) and info.cnpj == CNPJ

    def test_cadastrar_de_novo_troca_o_certificado_sem_duplicar_linhas(self):
        db = MemoryDb()
        run(secrets.save_certificate(db, make_pfx(), PASSWORD, CNPJ, "Maria"))
        first = {row.kind: row.ciphertext for row in db.store}
        run(secrets.save_certificate(db, make_pfx(), PASSWORD, CNPJ, "Joao"))
        assert len(db.store) == 2 and {row.kind: row.ciphertext for row in db.store} != first
        assert all(row.updated_by_name == "Joao" for row in db.store)

    @pytest.mark.parametrize("pfx,password,cnpj,message", [
        pytest.param(None, PASSWORD, CNPJ, "Envie o arquivo", id="sem-arquivo"),
        pytest.param(b"", PASSWORD, CNPJ, "Envie o arquivo", id="arquivo-vazio"),
        pytest.param(b"x" * 1_000_001, PASSWORD, CNPJ, "até 1 MB", id="arquivo-grande"),
        pytest.param("valido", "errada", CNPJ, "confira a senha", id="senha-errada"),
        pytest.param("valido", PASSWORD, "99888777000166", "diferente", id="outro-cnpj"),
    ])
    def test_recusas_em_portugues_sem_gravar_nada(self, pfx, password, cnpj, message):
        db = MemoryDb()
        data = make_pfx() if pfx == "valido" else pfx
        with pytest.raises(HTTPException) as info:
            run(secrets.save_certificate(db, data, password, cnpj, "Maria"))
        assert info.value.status_code == 422 and message in info.value.detail
        assert db.store == [] and db.commits == 0

    def test_certificado_vencido_e_recusado(self):
        with pytest.raises(HTTPException) as info:
            run(secrets.save_certificate(MemoryDb(), make_pfx(valid_from_days=-400, valid_for_days=365), PASSWORD, CNPJ, "Maria"))
        assert "Certificado recusado" in info.value.detail and "venceu" in info.value.detail

    def test_sem_chave_mestra_no_servidor_nao_grava_e_ensina_a_criar(self, monkeypatch):
        monkeypatch.setattr(vault.app_settings, "FISCAL_SECRET_KEY", "")
        db = MemoryDb()
        with pytest.raises(HTTPException) as info:
            run(secrets.save_certificate(db, make_pfx(), PASSWORD, CNPJ, "Maria"))
        assert info.value.status_code == 409 and "FISCAL_SECRET_KEY" in info.value.detail and db.store == []

    def test_remover_apaga_o_certificado_e_a_senha(self):
        db = MemoryDb()
        run(secrets.save_certificate(db, make_pfx(), PASSWORD, CNPJ, "Maria"))
        run(secrets.delete_certificate(db))
        assert db.store == []


class TestCsc:
    def test_guarda_cifrado_e_a_meta_so_tem_o_id(self):
        db = MemoryDb()
        run(secrets.save_csc(db, "homologacao", " 000001 ", f" {CSC_TOKEN} ", "Maria"))
        (row,) = db.store
        assert row.kind == "csc_hml" and row.meta == {"id": "000001"} and CSC_TOKEN.encode() not in row.ciphertext
        assert json.loads(vault.decrypt("csc_hml", row.ciphertext)) == {"id": "000001", "token": CSC_TOKEN}

    def test_producao_e_homologacao_sao_separados(self):
        db = MemoryDb()
        run(secrets.save_csc(db, "homologacao", "1", CSC_TOKEN, "Maria"))
        run(secrets.save_csc(db, "producao", "2", CSC_TOKEN + "X", "Maria"))
        assert sorted(row.kind for row in db.store) == ["csc_hml", "csc_prod"]
        run(secrets.delete_csc(db, "producao"))
        assert [row.kind for row in db.store] == ["csc_hml"]

    @pytest.mark.parametrize("environment,csc_id,token,message", [
        ("teste", "1", CSC_TOKEN, "Ambiente inválido"),
        ("homologacao", "abc", CSC_TOKEN, "só números"),
        ("homologacao", "1234567", CSC_TOKEN, "só números"),
        ("homologacao", "", CSC_TOKEN, "só números"),
        ("homologacao", "1", "curto", "curto demais"),
    ])
    def test_recusas(self, environment, csc_id, token, message):
        db = MemoryDb()
        with pytest.raises(HTTPException) as info:
            run(secrets.save_csc(db, environment, csc_id, token, "Maria"))
        assert info.value.status_code == 422 and message in info.value.detail and db.store == []

    def test_sem_chave_mestra_nao_grava(self, monkeypatch):
        monkeypatch.setattr(vault.app_settings, "FISCAL_SECRET_KEY", "")
        with pytest.raises(HTTPException) as info:
            run(secrets.save_csc(MemoryDb(), "homologacao", "1", CSC_TOKEN, "Maria"))
        assert info.value.status_code == 409


class TestStatusENuncaVaza:
    def test_status_tem_so_dados_publicos(self):
        db = MemoryDb()
        run(secrets.save_certificate(db, make_pfx(), PASSWORD, CNPJ, "Maria"))
        run(secrets.save_csc(db, "homologacao", "7", CSC_TOKEN, "Maria"))
        status = run(secrets.secrets_status(db))
        assert status["vault_available"] is True
        assert status["certificate"]["configured"] and status["certificate"]["cnpj"] == CNPJ
        assert status["certificate"]["level"] == "ok" and 300 <= status["certificate"]["days_left"] <= 335
        assert status["csc_hml"] == {"configured": True, "id": "7"} and status["csc_prod"] == {"configured": False, "id": None}
        text = repr(status)
        assert PASSWORD not in text and CSC_TOKEN not in text and "ciphertext" not in text

    def test_vencimento_proximo_vira_alerta(self):
        db = MemoryDb()
        run(secrets.save_certificate(db, make_pfx(valid_from_days=-340, valid_for_days=365), PASSWORD, CNPJ, "Maria"))
        certificate = run(secrets.secrets_status(db))["certificate"]
        assert certificate["level"] == "warn" and certificate["days_left"] < 60

    def test_nada_cadastrado_e_sem_chave_mestra(self, monkeypatch):
        monkeypatch.setattr(vault.app_settings, "FISCAL_SECRET_KEY", "")
        status = run(secrets.secrets_status(MemoryDb()))
        assert status == {"vault_available": False, "certificate": {"configured": False},
                          "csc_hml": {"configured": False, "id": None}, "csc_prod": {"configured": False, "id": None}}


class TestLeituraParaOAdaptador:
    def test_carrega_o_certificado_e_o_csc_decifrados_so_em_memoria(self):
        db, pfx = MemoryDb(), make_pfx()
        assert run(secrets.load_certificate(db)) is None and run(secrets.load_csc(db, "producao")) is None
        run(secrets.save_certificate(db, pfx, PASSWORD, CNPJ, "Maria"))
        run(secrets.save_csc(db, "producao", "9", CSC_TOKEN, "Maria"))
        assert run(secrets.load_certificate(db)) == (pfx, PASSWORD)
        assert run(secrets.load_csc(db, "producao")) == ("9", CSC_TOKEN)
        assert run(secrets.load_csc(db, "homologacao")) is None


@pytest.fixture
def client():
    def make(role=UserRole.admin, db=None):
        user = SimpleNamespace(id=uuid.uuid4(), name="Maria", role=role, is_active=True)
        app.dependency_overrides[get_current_user] = lambda: user
        app.dependency_overrides[get_db] = lambda: db or MemoryDb(company())
        return TestClient(app)

    yield make
    app.dependency_overrides.clear()


ROUTES = [
    ("get", "/api/v1/fiscal/secrets", {}),
    ("put", "/api/v1/fiscal/secrets/certificate", {"files": {"file": ("c.pfx", b"x")}, "data": {"password": "x"}}),
    ("delete", "/api/v1/fiscal/secrets/certificate", {}),
    ("put", "/api/v1/fiscal/secrets/csc/homologacao", {"json": {"id": "1", "token": "x" * 12}}),
    ("delete", "/api/v1/fiscal/secrets/csc/homologacao", {}),
]


class TestRotas:
    @pytest.mark.parametrize("method,path,kwargs", ROUTES)
    def test_operador_nao_mexe_nos_segredos(self, client, method, path, kwargs):
        assert getattr(client(UserRole.operator), method)(path, **kwargs).status_code == 403

    def test_envia_o_certificado_e_a_resposta_nao_tem_nenhum_segredo(self, client):
        c, pfx = client(), make_pfx()
        response = c.put("/api/v1/fiscal/secrets/certificate", files={"file": ("empresa.pfx", pfx, "application/x-pkcs12")}, data={"password": PASSWORD})
        assert response.status_code == 200
        body = response.json()
        assert body["certificate"]["configured"] is True and body["certificate"]["cnpj"] == CNPJ
        assert PASSWORD not in response.text and base64.b64encode(pfx).decode()[:40] not in response.text

    def test_senha_errada_e_422_em_portugues(self, client):
        response = client().put("/api/v1/fiscal/secrets/certificate", files={"file": ("e.pfx", make_pfx())}, data={"password": "errada"})
        assert response.status_code == 422 and "confira a senha" in response.json()["detail"]

    def test_certificado_de_outro_cnpj_e_recusado_pelo_cnpj_da_empresa_cadastrada(self, client):
        response = client(db=MemoryDb(company("99888777000166"))).put(
            "/api/v1/fiscal/secrets/certificate", files={"file": ("e.pfx", make_pfx())}, data={"password": PASSWORD})
        assert response.status_code == 422 and "diferente" in response.json()["detail"]

    def test_csc_e_remocao(self, client):
        db = MemoryDb(company())
        c = client(db=db)
        response = c.put("/api/v1/fiscal/secrets/csc/producao", json={"id": "3", "token": CSC_TOKEN})
        assert response.status_code == 200 and response.json()["csc_prod"] == {"configured": True, "id": "3"}
        assert CSC_TOKEN not in response.text
        response = c.delete("/api/v1/fiscal/secrets/csc/producao")
        assert response.status_code == 200 and response.json()["csc_prod"]["configured"] is False

    def test_sem_chave_mestra_a_tela_recebe_o_aviso(self, client, monkeypatch):
        monkeypatch.setattr(vault.app_settings, "FISCAL_SECRET_KEY", "")
        body = client().get("/api/v1/fiscal/secrets").json()
        assert body["vault_available"] is False
        response = client().put("/api/v1/fiscal/secrets/csc/homologacao", json={"id": "1", "token": CSC_TOKEN})
        assert response.status_code == 409 and "FISCAL_SECRET_KEY" in response.json()["detail"]
```

- [ ] **Step 2: Rodar e ver falhar**

```bash
cd backend && python -m pytest -q tests/test_fiscal_secrets.py; cd ..
```
Expected: FAIL (`cannot import name 'fiscal_secrets'`).

- [ ] **Step 3: Implementar**

Create `backend/app/services/fiscal_secrets.py`:

```python
"""Segredos fiscais cadastrados pela tela: certificado A1 (com a senha) e CSC de homologação e de produção.

Tudo é cifrado pelo cofre (`fiscal/vault.py`) antes de ir para o banco. O que sai daqui para a tela é só `secrets_status`, que
devolve dados públicos (titular do certificado, vencimento, ID do CSC) e **nunca** o conteúdo de um segredo.
"""
import json
from datetime import datetime, timezone
from typing import Any

from fastapi import HTTPException
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from app.models.fiscal import FiscalSecret
from app.services.fiscal import certificate as cert
from app.services.fiscal import vault

KIND_CERTIFICATE = "certificate"
KIND_CERTIFICATE_PASSWORD = "certificate_password"
CSC_KINDS = {"homologacao": "csc_hml", "producao": "csc_prod"}
MAX_PFX_BYTES = 1_000_000
CSC_TOKEN_MIN = 10


def _vault_or_409() -> None:
    try:
        vault.encrypt("teste", b"")  # só para saber se a chave mestra existe e é válida
    except vault.VaultUnavailable as exc:
        raise HTTPException(status_code=409, detail=str(exc)) from None


async def _row(db: AsyncSession, kind: str) -> FiscalSecret | None:
    return (await db.execute(select(FiscalSecret).where(FiscalSecret.kind == kind))).scalar_one_or_none()


async def _upsert(db: AsyncSession, kind: str, plaintext: bytes, meta: dict[str, Any] | None, user_name: str) -> None:
    row = await _row(db, kind)
    if row is None:
        row = FiscalSecret(kind=kind)
        db.add(row)
    row.ciphertext = vault.encrypt(kind, plaintext)
    row.meta = meta
    row.updated_by_name = user_name


async def save_certificate(db: AsyncSession, pfx: bytes, password: str, company_cnpj: str | None, user_name: str) -> cert.CertInfo:
    if not pfx or len(pfx) > MAX_PFX_BYTES:
        raise HTTPException(status_code=422, detail="Envie o arquivo do certificado (.pfx, até 1 MB).")
    _vault_or_409()
    try:
        info = cert.inspect_pfx(pfx, password)
    except cert.CertificateError as exc:
        raise HTTPException(status_code=422, detail=str(exc)) from None
    problems = cert.problems_for_company(info, company_cnpj)
    if problems:
        raise HTTPException(status_code=422, detail="Certificado recusado: " + "; ".join(problems) + ".")
    meta = {
        "subject": info.subject, "cnpj": info.cnpj, "not_before": info.not_before.isoformat(),
        "not_after": info.not_after.isoformat(), "fingerprint": info.fingerprint,
    }
    await _upsert(db, KIND_CERTIFICATE, pfx, meta, user_name)
    await _upsert(db, KIND_CERTIFICATE_PASSWORD, password.encode("utf-8"), None, user_name)
    await db.commit()
    return info


async def save_csc(db: AsyncSession, environment: str, csc_id: str, token: str, user_name: str) -> None:
    kind = CSC_KINDS.get(environment)
    if kind is None:
        raise HTTPException(status_code=422, detail="Ambiente inválido: use homologacao ou producao.")
    csc_id, token = (csc_id or "").strip(), (token or "").strip()
    if not csc_id.isdigit() or len(csc_id) > 6:
        raise HTTPException(status_code=422, detail="O ID do CSC deve ter só números (até 6 dígitos).")
    if len(token) < CSC_TOKEN_MIN:
        raise HTTPException(status_code=422, detail="O CSC (código) parece curto demais: confira o valor gerado na SEFAZ.")
    _vault_or_409()
    await _upsert(db, kind, json.dumps({"id": csc_id, "token": token}).encode("utf-8"), {"id": csc_id}, user_name)
    await db.commit()


async def delete_certificate(db: AsyncSession) -> None:
    for kind in (KIND_CERTIFICATE, KIND_CERTIFICATE_PASSWORD):
        row = await _row(db, kind)
        if row is not None:
            await db.delete(row)
    await db.commit()


async def delete_csc(db: AsyncSession, environment: str) -> None:
    kind = CSC_KINDS.get(environment)
    if kind is None:
        raise HTTPException(status_code=422, detail="Ambiente inválido: use homologacao ou producao.")
    row = await _row(db, kind)
    if row is not None:
        await db.delete(row)
    await db.commit()


def _certificate_status(row: FiscalSecret | None, now: datetime) -> dict[str, Any]:
    if row is None or not row.meta:
        return {"configured": False}
    not_after = datetime.fromisoformat(row.meta["not_after"])
    info = cert.CertInfo(
        subject=row.meta["subject"], cnpj=row.meta.get("cnpj"), not_before=datetime.fromisoformat(row.meta["not_before"]),
        not_after=not_after, fingerprint=row.meta["fingerprint"],
    )
    return {
        "configured": True, "subject": info.subject, "cnpj": info.cnpj, "not_after": info.not_after,
        "days_left": cert.days_left(info, now), "level": cert.expiry_level(info, now),
    }


async def secrets_status(db: AsyncSession, now: datetime | None = None) -> dict[str, Any]:
    """O que a tela pode saber dos segredos: o que está cadastrado e os dados públicos. Nunca o conteúdo."""
    now = now or datetime.now(timezone.utc)
    rows = {row.kind: row for row in (await db.execute(select(FiscalSecret))).scalars().all()}
    return {
        "vault_available": vault.vault_available(),
        "certificate": _certificate_status(rows.get(KIND_CERTIFICATE), now),
        "csc_hml": {"configured": "csc_hml" in rows, "id": (rows["csc_hml"].meta or {}).get("id") if "csc_hml" in rows else None},
        "csc_prod": {"configured": "csc_prod" in rows, "id": (rows["csc_prod"].meta or {}).get("id") if "csc_prod" in rows else None},
    }


async def load_certificate(db: AsyncSession) -> tuple[bytes, str] | None:
    """Para o adaptador do modo direto (E1/E2): o .pfx e a senha decifrados, só em memória. None se não estiver cadastrado."""
    pfx_row, password_row = await _row(db, KIND_CERTIFICATE), await _row(db, KIND_CERTIFICATE_PASSWORD)
    if pfx_row is None or password_row is None:
        return None
    return vault.decrypt(KIND_CERTIFICATE, pfx_row.ciphertext), vault.decrypt(KIND_CERTIFICATE_PASSWORD, password_row.ciphertext).decode("utf-8")


async def load_csc(db: AsyncSession, environment: str) -> tuple[str, str] | None:
    """O CSC (ID e código) do ambiente, decifrado só em memória. None se não estiver cadastrado."""
    kind = CSC_KINDS.get(environment)
    row = await _row(db, kind) if kind else None
    if row is None:
        return None
    data = json.loads(vault.decrypt(kind, row.ciphertext))
    return data["id"], data["token"]
```

Mesmo procedimento de edição: os esquemas de resposta do cofre e as rotas (só administrador):

```python
import sys
from pathlib import Path

EDITS = {
  "backend/app/schemas/fiscal.py::secrets": [
    [
      "Schemas dos segredos",
      "class PendingProductOut(BaseModel):\n    id: uuid.UUID\n    code: int\n    name: str\n    category: str | None\n    missing: list[str]\n",
      "class PendingProductOut(BaseModel):\n    id: uuid.UUID\n    code: int\n    name: str\n    category: str | None\n    missing: list[str]\n\n\nclass CertificateStatusOut(BaseModel):\n    \"\"\"O que a tela sabe do certificado: só dados públicos (nunca o arquivo nem a senha).\"\"\"\n\n    configured: bool\n    subject: str | None = None\n    cnpj: str | None = None\n    not_after: datetime | None = None\n    days_left: int | None = None\n    level: Literal[\"ok\", \"warn\", \"critical\"] | None = None\n\n\nclass CscStatusOut(BaseModel):\n    configured: bool\n    id: str | None = None\n\n\nclass FiscalSecretsOut(BaseModel):\n    vault_available: bool\n    certificate: CertificateStatusOut\n    csc_hml: CscStatusOut\n    csc_prod: CscStatusOut\n\n\nclass CscIn(BaseModel):\n    id: str\n    token: str\n"
    ]
  ],
  "backend/app/api/v1/fiscal.py::secrets": [
    [
      "Importar upload e o serviço dos segredos",
      "from fastapi import APIRouter, Depends, HTTPException\n",
      "from fastapi import APIRouter, Depends, File, Form, HTTPException, UploadFile\n"
    ],
    [
      "Importar os schemas dos segredos",
      "from app.schemas.fiscal import (\n    FiscalDefaultIn, FiscalDefaultOut, FiscalIssuerOut, FiscalSettingsIn, FiscalSettingsOut, FiscalStatusOut,\n    PendingProductOut,\n)\n",
      "from app.schemas.fiscal import (\n    CscIn, FiscalDefaultIn, FiscalDefaultOut, FiscalIssuerOut, FiscalSecretsOut, FiscalSettingsIn, FiscalSettingsOut,\n    FiscalStatusOut, PendingProductOut,\n)\n"
    ],
    [
      "Importar o serviço dos segredos",
      "from app.services import fiscal_cancel, fiscal_retry, fiscal_service\n",
      "from app.services import fiscal_cancel, fiscal_retry, fiscal_secrets, fiscal_service\n"
    ],
    [
      "Rotas dos segredos",
      "    except fiscal_service.EmissionDisabled:\n        raise HTTPException(status_code=409, detail=\"Emissão fiscal desligada. Ligue em Configurações > Fiscal\") from None\n",
      "    except fiscal_service.EmissionDisabled:\n        raise HTTPException(status_code=409, detail=\"Emissão fiscal desligada. Ligue em Configurações > Fiscal\") from None\n\n\n@router.get(\"/secrets\", response_model=FiscalSecretsOut)\nasync def get_secrets(db: AsyncSession = Depends(get_db), _: User = Depends(require_admin)):\n    \"\"\"O que está cadastrado no cofre (certificado, CSC), sem nenhum conteúdo secreto.\"\"\"\n    return await fiscal_secrets.secrets_status(db)\n\n\n@router.put(\"/secrets/certificate\", response_model=FiscalSecretsOut)\nasync def put_certificate(\n    file: UploadFile = File(...), password: str = Form(...), db: AsyncSession = Depends(get_db), admin: User = Depends(require_admin)\n):\n    company = (await db.execute(fiscal_service.settings_query())).scalar_one_or_none()\n    pfx = await file.read(fiscal_secrets.MAX_PFX_BYTES + 1)\n    await fiscal_secrets.save_certificate(db, pfx, password, company.cnpj if company else None, admin.name)\n    return await fiscal_secrets.secrets_status(db)\n\n\n@router.delete(\"/secrets/certificate\", response_model=FiscalSecretsOut)\nasync def delete_certificate(db: AsyncSession = Depends(get_db), _: User = Depends(require_admin)):\n    await fiscal_secrets.delete_certificate(db)\n    return await fiscal_secrets.secrets_status(db)\n\n\n@router.put(\"/secrets/csc/{environment}\", response_model=FiscalSecretsOut)\nasync def put_csc(environment: str, body: CscIn, db: AsyncSession = Depends(get_db), admin: User = Depends(require_admin)):\n    await fiscal_secrets.save_csc(db, environment, body.id, body.token, admin.name)\n    return await fiscal_secrets.secrets_status(db)\n\n\n@router.delete(\"/secrets/csc/{environment}\", response_model=FiscalSecretsOut)\nasync def delete_csc(environment: str, db: AsyncSession = Depends(get_db), _: User = Depends(require_admin)):\n    await fiscal_secrets.delete_csc(db, environment)\n    return await fiscal_secrets.secrets_status(db)\n"
    ]
  ]
}

for rel, edits in EDITS.items():
    path = Path(rel.split("::")[0])
    text = path.read_text(encoding="utf-8", newline="")
    crlf = "\r\n" in text
    text = text.replace("\r\n", "\n")
    for label, old, new in edits:
        count = text.count(old)
        if count != 1:
            sys.exit(f"{rel}: '{label}' casou {count} vezes (esperado 1)")
        text = text.replace(old, new)
    path.write_text(text.replace("\n", "\r\n") if crlf else text, encoding="utf-8", newline="")
    print(f"ok {rel}")
```

```bash
python apply_edits.py
```
Expected: `ok` para os dois arquivos.

- [ ] **Step 4: Rodar e ver passar**

```bash
cd backend && python -m pytest -q; cd ..
```
Expected: `244 passed` (212 + 32).

- [ ] **Step 5: Commit**

```bash
git add backend/app/services/fiscal_secrets.py backend/app/schemas/fiscal.py backend/app/api/v1/fiscal.py backend/tests/test_fiscal_secrets.py
git commit -m "feat(fiscal): guardar e conferir certificado A1 e CSC no cofre, sem devolver segredo"
```

---

### Task 4: Registro de modos e trava de produção pelo modo direto

**Files:**
- Create: `backend/app/services/fiscal/registry.py`
- Replace: `backend/app/services/fiscal_golive.py`, `backend/tests/test_fiscal_golive.py`
- Modify: `backend/app/services/fiscal_service.py`, `backend/app/services/fiscal_cancel.py`, `backend/app/services/fiscal_retry.py`, `backend/app/api/v1/fiscal.py`, `backend/app/schemas/fiscal.py`, `backend/tests/test_fiscal_service.py`, `backend/tests/test_fiscal_cancel.py`, `backend/tests/test_fiscal_retry.py`, `backend/tests/test_fiscal_api.py`, `backend/tests/test_fiscal_c3_api.py`
- Test: `backend/tests/test_fiscal_registry.py`

**Interfaces:**
- Consumes: `FiscalSecret`/`mode` (Task 2), o certificado e o CSC do cofre (Tasks 1 e 3).
- Produces (`registry.py`): `REAL_MODES = ("sefaz_direto",)`, `settings_query()` (movida de `fiscal_service`), `effective_mode(saved)`, `provider_for_mode(mode)`, `provider_for_settings(row)`, `mode_available(mode)`, `await load_provider(db)`. `fiscal_service`, `fiscal_cancel`, `fiscal_retry` e as rotas passam a pedir o provedor ao registro (o antigo `configured_provider()` some). `go_live_problems(db, data)` passa a exigir modo real disponível, certificado válido do mesmo CNPJ e CSC de produção. `FiscalStatusOut` ganha `mode` e `mode_available`.

- [ ] **Step 1: Escrever os testes que falham**

Create `backend/tests/test_fiscal_registry.py`:

```python
"""Registro de modos de emissão: o modo salvo no banco escolhe o adaptador; em desenvolvimento FISCAL_PROVIDER=fake o força."""
import asyncio
import os
from types import SimpleNamespace

import pytest

# `app.core.config` exige estas variáveis já no import.
os.environ.setdefault("DATABASE_URL", "postgresql+asyncpg://teste:teste@localhost/teste")
os.environ.setdefault("DATABASE_URL_SYNC", "postgresql://teste:teste@localhost/teste")
os.environ.setdefault("SECRET_KEY", "teste")

from app.services.fiscal import registry  # noqa: E402
from app.services.fiscal.fake import FakeFiscalProvider  # noqa: E402


def run(coro):
    return asyncio.run(coro)


class FakeDb:
    def __init__(self, row=None):
        self.row = row
        self.queries = 0

    async def execute(self, _statement):
        self.queries += 1
        return self

    def scalar_one_or_none(self):
        return self.row


@pytest.fixture
def dev(monkeypatch):
    monkeypatch.setattr(registry.app_settings, "FISCAL_PROVIDER", "none")
    monkeypatch.setattr(registry.app_settings, "ENVIRONMENT", "development")


class TestModoEmVigor:
    def test_usa_o_modo_salvo_e_none_quando_nao_ha(self, dev):
        assert registry.effective_mode("sefaz_direto") == "sefaz_direto"
        assert registry.effective_mode(None) == "none" and registry.effective_mode("") == "none"

    def test_o_atalho_de_desenvolvimento_forca_o_falso_so_em_desenvolvimento(self, monkeypatch):
        monkeypatch.setattr(registry.app_settings, "FISCAL_PROVIDER", "fake")
        monkeypatch.setattr(registry.app_settings, "ENVIRONMENT", "development")
        assert registry.effective_mode("sefaz_direto") == "fake"
        monkeypatch.setattr(registry.app_settings, "ENVIRONMENT", "production")
        assert registry.effective_mode("sefaz_direto") == "sefaz_direto"  # em produção o atalho é ignorado
        assert registry.effective_mode(None) == "none"

    def test_qualquer_outro_valor_da_variavel_nao_muda_nada(self, monkeypatch):
        monkeypatch.setattr(registry.app_settings, "ENVIRONMENT", "development")
        for value in ("nuvemfiscal", "sefaz_direto", ""):
            monkeypatch.setattr(registry.app_settings, "FISCAL_PROVIDER", value)
            assert registry.effective_mode("none") == "none"


class TestAdaptadores:
    def test_none_e_o_direto_ainda_nao_tem_adaptador_e_o_falso_so_em_desenvolvimento(self, dev, monkeypatch):
        assert registry.provider_for_mode("none") is None and registry.provider_for_mode("sefaz_direto") is None
        assert isinstance(registry.provider_for_mode("fake"), FakeFiscalProvider)
        monkeypatch.setattr(registry.app_settings, "ENVIRONMENT", "production")
        assert registry.provider_for_mode("fake") is None

    def test_modo_disponivel_e_ter_adaptador(self, dev):
        assert registry.mode_available("fake") is True
        assert registry.mode_available("none") is False and registry.mode_available("sefaz_direto") is False

    def test_o_provedor_falso_de_desenvolvimento_e_sempre_o_mesmo(self, dev):
        assert registry.provider_for_mode("fake") is registry.provider_for_mode("fake")

    def test_a_partir_da_linha_de_configuracao(self, dev):
        assert registry.provider_for_settings(SimpleNamespace(mode="fake")) is not None
        assert registry.provider_for_settings(SimpleNamespace(mode="none")) is None
        assert registry.provider_for_settings(None) is None
        assert registry.provider_for_settings(SimpleNamespace()) is None  # linha antiga, sem o campo


class TestCarregar:
    def test_le_o_modo_do_banco(self, dev):
        db = FakeDb(SimpleNamespace(mode="fake"))
        assert run(registry.load_provider(db)) is not None and db.queries == 1
        assert run(registry.load_provider(FakeDb(SimpleNamespace(mode="none")))) is None
        assert run(registry.load_provider(FakeDb(None))) is None

    def test_com_o_atalho_de_desenvolvimento_nem_consulta_o_banco(self, monkeypatch):
        monkeypatch.setattr(registry.app_settings, "FISCAL_PROVIDER", "fake")
        monkeypatch.setattr(registry.app_settings, "ENVIRONMENT", "development")
        db = FakeDb(SimpleNamespace(mode="none"))
        assert run(registry.load_provider(db)) is not None and db.queries == 0

    def test_consulta_de_configuracao_tem_ordem_fixa(self):
        sql = str(registry.settings_query())
        assert "ORDER BY fiscal_settings.id" in sql and "LIMIT" in sql
```

Substitua o conteúdo de `backend/tests/test_fiscal_golive.py` por:

```python
"""Ida para produção: o que precisa estar pronto antes de a emissão passar a valer como nota fiscal de verdade."""
import asyncio
import base64
import os
from datetime import datetime, timedelta, timezone

import pytest

# `app.core.config` exige estas variáveis já no import.
os.environ.setdefault("DATABASE_URL", "postgresql+asyncpg://teste:teste@localhost/teste")
os.environ.setdefault("DATABASE_URL_SYNC", "postgresql://teste:teste@localhost/teste")
os.environ.setdefault("SECRET_KEY", "teste")

from fastapi import HTTPException  # noqa: E402

from app.models.fiscal import FiscalSecret, FiscalSettings  # noqa: E402
from app.schemas.fiscal import FiscalSettingsIn  # noqa: E402
from app.services import fiscal_golive as golive  # noqa: E402
from app.services import fiscal_service as svc  # noqa: E402
from app.services.fiscal import vault  # noqa: E402

COMPLETE = dict(cnpj="11222333000181", ie="123456789", legal_name="EMPRESA TESTE LTDA", street="Rua Exemplo", number="100",
                district="Centro", city="Recife", city_ibge="2611606", state="PE", zip_code="50000000")
NOW = datetime.now(timezone.utc)


def run(coro):
    return asyncio.run(coro)


def certificate_row(days_left=300, cnpj="11222333000181"):
    not_after = NOW + timedelta(days=days_left)
    return FiscalSecret(kind="certificate", ciphertext=b"x", meta={
        "subject": "EMPRESA TESTE LTDA:" + (cnpj or ""), "cnpj": cnpj, "not_before": (NOW - timedelta(days=60)).isoformat(),
        "not_after": not_after.isoformat(), "fingerprint": "ab" * 32})


def csc_prod_row():
    return FiscalSecret(kind="csc_prod", ciphertext=b"x", meta={"id": "1"})


class FakeDb:
    """Responde por tipo de consulta: contagem de vendas de teste, segredos do cofre ou a linha de configuração."""

    def __init__(self, hml_authorized=1, secret_rows=(), settings=None):
        self.hml_authorized = hml_authorized
        self.secret_rows = list(secret_rows)
        self.settings = settings
        self.commits = 0
        self.count_params = []

    async def execute(self, statement):
        text = str(statement)
        if "count(" in text.lower():
            self.current, self.rows = self.hml_authorized, []
            self.count_params = list(statement.compile().params.values())
        elif "fiscal_secrets" in text:
            self.current, self.rows = None, list(self.secret_rows)
        else:
            self.current, self.rows = self.settings, [self.settings] if self.settings else []
        return self

    def scalar_one(self):
        return self.current

    def scalar_one_or_none(self):
        return self.current

    def scalars(self):
        return self

    def all(self):
        return list(self.rows)

    def add(self, _obj):
        pass

    async def commit(self):
        self.commits += 1

    async def refresh(self, _obj):
        pass


def data(**over):
    base = dict(enabled=True, environment="producao", mode="sefaz_direto", production_confirmation=True, **COMPLETE)
    base.update(over)
    return FiscalSettingsIn(**base)


@pytest.fixture
def adapter_ready(monkeypatch):
    """Simula o adaptador do modo direto já pronto (ele só chega na etapa E2)."""
    monkeypatch.setattr(golive, "mode_available", lambda mode: True)


@pytest.fixture(autouse=True)
def vault_key(monkeypatch):
    monkeypatch.setattr(vault.app_settings, "FISCAL_SECRET_KEY", base64.b64encode(bytes(range(32))).decode())


READY_ROWS = (certificate_row(), csc_prod_row())


class TestProblems:
    def test_tudo_pronto_nao_tem_pendencias(self, adapter_ready):
        assert run(golive.go_live_problems(FakeDb(2, READY_ROWS), data())) == []

    def test_so_o_modo_direto_e_real_o_desligado_e_o_de_teste_nao_servem(self, adapter_ready):
        for mode in ("none", "fake"):
            problems = run(golive.go_live_problems(FakeDb(2, READY_ROWS), data(mode=mode)))
            assert any("modo de emissão real" in p for p in problems), mode

    def test_modo_direto_ainda_sem_adaptador_no_sistema(self):
        problems = run(golive.go_live_problems(FakeDb(2, READY_ROWS), data()))  # de verdade: o adaptador chega na E2
        assert problems == ["o modo escolhido ainda não está disponível neste sistema (em desenvolvimento)"]

    def test_exige_certificado_valido_da_empresa_e_csc_de_producao(self, adapter_ready):
        assert run(golive.go_live_problems(FakeDb(2, [csc_prod_row()]), data())) == ["cadastre o certificado digital A1"]
        assert run(golive.go_live_problems(FakeDb(2, [certificate_row(days_left=-3), csc_prod_row()]), data())) == ["o certificado digital venceu"]
        assert run(golive.go_live_problems(FakeDb(2, [certificate_row(cnpj="99888777000166"), csc_prod_row()]), data())) == [
            "o CNPJ do certificado é diferente do CNPJ da empresa"]
        assert run(golive.go_live_problems(FakeDb(2, [certificate_row()]), data())) == ["cadastre o CSC de produção"]

    def test_exige_uma_venda_de_teste_autorizada_em_homologacao(self, adapter_ready):
        assert run(golive.go_live_problems(FakeDb(0, READY_ROWS), data())) == ["faça ao menos uma venda de teste autorizada em homologação"]

    def test_venda_de_teste_autorizada_e_depois_cancelada_tambem_conta(self, adapter_ready):
        db = FakeDb(1, READY_ROWS)
        assert run(golive.go_live_problems(db, data())) == []
        assert ["authorized", "cancelled"] in db.count_params  # o filtro aceita as duas situações

    def test_exige_a_confirmacao_do_contador(self, adapter_ready):
        assert run(golive.go_live_problems(FakeDb(1, READY_ROWS), data(production_confirmation=False))) == [
            "confirme que o contador validou o cupom emitido em homologação"]

    def test_exige_a_emissao_ligada_com_os_dados_da_empresa(self, adapter_ready):
        problems = run(golive.go_live_problems(FakeDb(1, READY_ROWS), FiscalSettingsIn(
            enabled=False, environment="producao", mode="sefaz_direto", production_confirmation=True, cnpj="11222333000181")))
        assert problems == ["ligue a emissão e preencha todos os dados da empresa"]

    def test_lista_tudo_de_uma_vez(self):
        problems = run(golive.go_live_problems(FakeDb(0), FiscalSettingsIn(environment="producao")))
        assert len(problems) == 4  # modo, emissão ligada, venda de teste e confirmação do contador


def row(environment="homologacao"):
    return FiscalSettings(enabled=True, environment=environment, regime="normal", mode="sefaz_direto", series=1, cancel_window_minutes=30)


class TestSaveSettings:
    def test_bloqueia_a_ida_para_producao_e_diz_tudo_que_falta(self):
        r = row()
        with pytest.raises(HTTPException) as info:
            run(svc.save_settings(FakeDb(0, settings=r), data(production_confirmation=False), "Maria"))
        assert info.value.status_code == 422 and info.value.detail.startswith("Para ir para produção: ")
        assert "ainda não está disponível" in info.value.detail
        assert r.environment == "homologacao" and r.production_confirmed_by is None

    def test_libera_registra_quem_confirmou_e_nao_grava_o_campo_de_confirmacao(self, adapter_ready):
        r = row()
        db = FakeDb(1, READY_ROWS, settings=r)
        run(svc.save_settings(db, data(), "Maria"))
        assert r.environment == "producao" and r.mode == "sefaz_direto" and r.production_confirmed_by == "Maria"
        assert r.production_confirmed_at is not None and not hasattr(r, "production_confirmation") and db.commits == 1

    def test_ja_em_producao_salvar_outros_dados_nao_repete_a_conferencia(self):
        r = row("producao")
        run(svc.save_settings(FakeDb(settings=r), data(production_confirmation=False, cancel_window_minutes=45), "Maria"))
        assert r.environment == "producao" and r.cancel_window_minutes == 45

    def test_voltar_para_homologacao_limpa_a_confirmacao(self):
        r = row("producao")
        r.production_confirmed_by = "Maria"
        run(svc.save_settings(FakeDb(settings=r), data(environment="homologacao"), "Joao"))
        assert r.environment == "homologacao" and r.production_confirmed_by is None and r.production_confirmed_at is None

    def test_em_homologacao_nao_consulta_a_conferencia_e_grava_o_modo(self):
        r = row()
        run(svc.save_settings(FakeDb(settings=r), FiscalSettingsIn(environment="homologacao", mode="sefaz_direto"), "Maria"))
        assert r.environment == "homologacao" and r.mode == "sefaz_direto"
```

Mesmo procedimento de edição (salve como `apply_edits.py` fora do repositório e rode da raiz). Aqui ele ajusta os testes existentes para o provedor vir do registro (`load_provider`/`provider_for_settings`) e para o status informar o modo:

```python
import sys
from pathlib import Path

EDITS = {
  "backend/tests/test_fiscal_service.py::registry": [
    [
      "Alteração 1",
      "from app.services import fiscal_service as svc  # noqa: E402\n",
      "from app.services import fiscal_service as svc  # noqa: E402\nfrom app.services.fiscal import registry  # noqa: E402\n"
    ],
    [
      "Alteração 2",
      "\nclass TestEntryPoints:\n",
      "\ndef mode_row(mode, enabled=True):\n    return SimpleNamespace(mode=mode, enabled=enabled)\n\n\nclass TestEntryPoints:\n"
    ],
    [
      "Alteração 3",
      "    def test_tentar_de_novo_sem_provedor_e_409(self, monkeypatch):\n        monkeypatch.setattr(svc.app_settings, \"FISCAL_PROVIDER\", \"none\")\n",
      "    \"\"\"O provedor vem do MODO salvo nas configurações (banco). Só em desenvolvimento FISCAL_PROVIDER=fake o força.\"\"\"\n\n    @pytest.fixture(autouse=True)\n    def dev_without_shortcut(self, monkeypatch):\n        monkeypatch.setattr(registry.app_settings, \"FISCAL_PROVIDER\", \"none\")\n        monkeypatch.setattr(registry.app_settings, \"ENVIRONMENT\", \"development\")\n\n    @pytest.mark.parametrize(\"row\", [None, \"none\", \"sefaz_direto\"])\n    def test_tentar_de_novo_sem_modo_de_emissao_disponivel_e_409(self, row):\n        rows = [] if row is None else [mode_row(row)]\n"
    ],
    [
      "Alteração 4",
      "            run(svc.retry_emission(FakeDb(), uuid.uuid4(), \"Maria\"))\n        assert info.value.status_code == 409 and \"não configurada\" in info.value.detail\n",
      "            run(svc.retry_emission(FakeDb(rows), uuid.uuid4(), \"Maria\"))  # \"sefaz_direto\" ainda não tem adaptador (E2)\n        assert info.value.status_code == 409 and \"sem modo de emissão\" in info.value.detail\n"
    ],
    [
      "Alteração 5",
      "    def test_tentar_de_novo_com_provedor_invalido_ou_falso_em_producao_e_409_e_nao_500(self, monkeypatch):\n        for name, environment in ((\"fake\", \"production\"), (\"nuvemfiscal\", \"development\")):\n            monkeypatch.setattr(svc.app_settings, \"FISCAL_PROVIDER\", name)\n            monkeypatch.setattr(svc.app_settings, \"ENVIRONMENT\", environment)\n            with pytest.raises(HTTPException) as info:\n                run(svc.retry_emission(FakeDb(), uuid.uuid4(), \"Maria\"))\n            assert info.value.status_code == 409 and \"não configurada\" in info.value.detail\n",
      "    def test_o_atalho_de_desenvolvimento_so_vale_em_desenvolvimento(self, monkeypatch):\n        monkeypatch.setattr(registry.app_settings, \"FISCAL_PROVIDER\", \"fake\")\n        monkeypatch.setattr(registry.app_settings, \"ENVIRONMENT\", \"production\")\n        with pytest.raises(HTTPException) as info:\n            run(svc.retry_emission(FakeDb([mode_row(\"none\")]), uuid.uuid4(), \"Maria\"))\n        assert info.value.status_code == 409\n\n    def test_o_modo_salvo_no_banco_escolhe_o_provedor(self):\n        order = make_order([order_item(BANANA)])\n        db = FakeDb([mode_row(\"fake\")], [order], [fiscal_settings()], [BANANA], [default()])  # 1ª consulta: o modo\n        assert run(svc.retry_emission(db, order.id, \"Maria\")) is order and order.fiscal_status == \"authorized\"\n"
    ],
    [
      "Alteração 6",
      "    def test_tentar_de_novo_com_emissao_desligada_e_409(self, monkeypatch):\n        monkeypatch.setattr(svc.app_settings, \"FISCAL_PROVIDER\", \"fake\")\n",
      "    def test_tentar_de_novo_com_emissao_desligada_e_409(self, monkeypatch):\n        monkeypatch.setattr(registry.app_settings, \"FISCAL_PROVIDER\", \"fake\")  # atalho: provedor sem consultar o modo\n"
    ],
    [
      "Alteração 7",
      "    def test_tentar_de_novo_em_pedido_inexistente_e_404(self, monkeypatch):\n        monkeypatch.setattr(svc.app_settings, \"FISCAL_PROVIDER\", \"fake\")\n",
      "    def test_tentar_de_novo_em_pedido_inexistente_e_404(self, monkeypatch):\n        monkeypatch.setattr(registry.app_settings, \"FISCAL_PROVIDER\", \"fake\")\n"
    ],
    [
      "Alteração 8",
      "        monkeypatch.setattr(svc.app_settings, \"FISCAL_PROVIDER\", \"fake\")\n\n",
      ""
    ],
    [
      "Alteração 9",
      "    def test_segundo_plano_com_provedor_desligado_nem_abre_o_banco(self, monkeypatch):\n        monkeypatch.setattr(svc.app_settings, \"FISCAL_PROVIDER\", \"none\")\n        monkeypatch.setattr(svc, \"AsyncSessionLocal\", lambda: pytest.fail(\"não deveria abrir o banco\"))\n",
      "    def test_segundo_plano_sem_modo_de_emissao_nao_emite(self, monkeypatch):\n        class Session:\n            async def __aenter__(self):\n                return FakeDb([mode_row(\"none\")])\n\n            async def __aexit__(self, *exc):\n                return False\n\n        async def must_not_emit(*args, **kwargs):\n            raise AssertionError(\"não deveria emitir\")\n\n        monkeypatch.setattr(svc, \"AsyncSessionLocal\", Session)\n        monkeypatch.setattr(svc, \"emit_order\", must_not_emit)\n"
    ]
  ],
  "backend/tests/test_fiscal_cancel.py::registry": [
    [
      "Alteração 1",
      "        monkeypatch.setattr(ops, \"configured_provider\", lambda: None)\n",
      "        monkeypatch.setattr(ops, \"provider_for_settings\", lambda row: None)\n"
    ],
    [
      "Alteração 2",
      "        assert info.value.status_code == 409 and \"não configurada\" in info.value.detail\n",
      "        assert info.value.status_code == 409 and \"sem modo de emissão\" in info.value.detail\n"
    ],
    [
      "Alteração 3",
      "        fake = provider_with_note()  # criado antes: o laço de eventos do teste ainda não está rodando\n        monkeypatch.setattr(ops, \"configured_provider\", lambda: fake)\n",
      "        fake = provider_with_note()  # criado antes: o laço de eventos do teste ainda não está rodando\n        monkeypatch.setattr(ops, \"provider_for_settings\", lambda row: fake)\n"
    ],
    [
      "Alteração 4",
      "\n        monkeypatch.setattr(ops, \"configured_provider\", lambda: fake)\n        o.status, o.payment_type, o.payment_splits, o.customer_id, o.notes = OrderStatus.delivered, PaymentType.cash, None, None, None\n",
      "\n        monkeypatch.setattr(ops, \"provider_for_settings\", lambda row: fake)\n        o.status, o.payment_type, o.payment_splits, o.customer_id, o.notes = OrderStatus.delivered, PaymentType.cash, None, None, None\n"
    ]
  ],
  "backend/tests/test_fiscal_retry.py::registry": [
    [
      "Alteração 1",
      "    def test_sem_provedor_ou_com_intervalo_zero_nao_inicia(self, monkeypatch):\n        monkeypatch.setattr(ops.app_settings, \"FISCAL_PROVIDER\", \"none\")\n        monkeypatch.setattr(ops.app_settings, \"FISCAL_RETRY_INTERVAL_SECONDS\", 120)\n        assert ops.start_retry_loop() is None\n        monkeypatch.setattr(ops.app_settings, \"FISCAL_PROVIDER\", \"fake\")\n",
      "    def test_com_intervalo_zero_nao_inicia(self, monkeypatch):\n"
    ],
    [
      "Alteração 2",
      "    def test_com_provedor_e_intervalo_inicia_uma_tarefa_que_pode_ser_cancelada(self, monkeypatch):\n        monkeypatch.setattr(ops.app_settings, \"FISCAL_PROVIDER\", \"fake\")\n",
      "    def test_com_intervalo_inicia_uma_tarefa_que_pode_ser_cancelada(self, monkeypatch):\n"
    ],
    [
      "Alteração 3",
      "        run(scenario())\n",
      "        run(scenario())\n\n    def test_sem_modo_de_emissao_disponivel_o_ciclo_nao_faz_nada_e_o_laco_segue(self, monkeypatch):\n        from app.services.fiscal import registry\n\n        monkeypatch.setattr(registry.app_settings, \"FISCAL_PROVIDER\", \"none\")\n        sleeps, calls = [], []\n\n        async def fake_sleep(seconds):\n            sleeps.append(seconds)\n            if len(sleeps) == 3:\n                raise asyncio.CancelledError\n\n        class Session:\n            async def __aenter__(self):\n                return FakeDb(settings=SimpleNamespace(enabled=True, environment=\"homologacao\", mode=\"none\"))\n\n            async def __aexit__(self, *exc):\n                return False\n\n        async def must_not_run(db, provider, now=None):\n            calls.append(1)\n\n        monkeypatch.setattr(ops.asyncio, \"sleep\", fake_sleep)\n        monkeypatch.setattr(ops, \"AsyncSessionLocal\", Session)\n        monkeypatch.setattr(ops, \"retry_pending\", must_not_run)\n        with pytest.raises(asyncio.CancelledError):\n            run(ops.run_retry_loop(120))\n        assert calls == [] and sleeps == [120, 120, 120]\n"
    ]
  ],
  "backend/tests/test_fiscal_api.py::registry": [
    [
      "Alteração 1",
      "    def row(self, enabled=True, environment=\"homologacao\", **issuer):\n        return SimpleNamespace(enabled=enabled, environment=environment, **{**self.ISSUER, **issuer})\n",
      "    def row(self, enabled=True, environment=\"homologacao\", mode=\"none\", **issuer):\n        return SimpleNamespace(enabled=enabled, environment=environment, mode=mode, **{**self.ISSUER, **issuer})\n"
    ],
    [
      "Alteração 2",
      "    def test_desligada_por_padrao(self, client, monkeypatch):\n        monkeypatch.setattr(\"app.api.v1.fiscal.app_settings.FISCAL_PROVIDER\", \"none\")\n",
      "    def test_desligada_por_padrao(self, client, monkeypatch):\n        monkeypatch.setattr(\"app.services.fiscal.registry.app_settings.FISCAL_PROVIDER\", \"none\")\n"
    ],
    [
      "Alteração 3",
      "        assert body == {\"enabled\": False, \"provider_configured\": False, \"environment\": \"homologacao\", \"issuer\": None}\n",
      "        assert body == {\"enabled\": False, \"provider_configured\": False, \"mode\": \"none\", \"mode_available\": False,\n                        \"environment\": \"homologacao\", \"issuer\": None}\n"
    ],
    [
      "Alteração 4",
      "    def test_ligada_so_com_provedor_configurado(self, client, monkeypatch):\n        monkeypatch.setattr(\"app.api.v1.fiscal.app_settings.FISCAL_PROVIDER\", \"none\")\n",
      "    def test_ligada_so_com_provedor_configurado(self, client, monkeypatch):\n        monkeypatch.setattr(\"app.services.fiscal.registry.app_settings.FISCAL_PROVIDER\", \"none\")\n"
    ],
    [
      "Alteração 5",
      "        assert client(db=FakeDb([self.row()])).get(\"/api/v1/fiscal/status\").json()[\"enabled\"] is False\n        monkeypatch.setattr(\"app.api.v1.fiscal.app_settings.FISCAL_PROVIDER\", \"fake\")\n",
      "        assert client(db=FakeDb([self.row()])).get(\"/api/v1/fiscal/status\").json()[\"enabled\"] is False\n        monkeypatch.setattr(\"app.services.fiscal.registry.app_settings.FISCAL_PROVIDER\", \"fake\")\n"
    ],
    [
      "Alteração 6",
      "        assert body[\"enabled\"] is True and body[\"provider_configured\"] is True and body[\"environment\"] == \"producao\"\n",
      "        assert body[\"enabled\"] is True and body[\"provider_configured\"] is True and body[\"environment\"] == \"producao\"\n        assert body[\"mode\"] == \"fake\"  # em desenvolvimento o atalho FISCAL_PROVIDER=fake força o provedor falso\n"
    ],
    [
      "Alteração 7",
      "\n    def test_qualquer_usuario_ve_a_empresa_para_imprimir_o_cupom_fiscal(self, client, monkeypatch):\n",
      "\n    def test_o_modo_vem_do_banco_e_o_direto_ainda_nao_tem_adaptador(self, client, monkeypatch):\n        monkeypatch.setattr(\"app.services.fiscal.registry.app_settings.FISCAL_PROVIDER\", \"none\")\n        monkeypatch.setattr(\"app.services.fiscal.registry.app_settings.ENVIRONMENT\", \"development\")\n        direct = client(db=FakeDb([self.row(mode=\"sefaz_direto\")])).get(\"/api/v1/fiscal/status\").json()\n        assert direct[\"mode\"] == \"sefaz_direto\" and direct[\"mode_available\"] is False and direct[\"enabled\"] is False\n        fake = client(db=FakeDb([self.row(mode=\"fake\")])).get(\"/api/v1/fiscal/status\").json()\n        assert fake[\"mode\"] == \"fake\" and fake[\"mode_available\"] is True and fake[\"enabled\"] is True\n\n    def test_qualquer_usuario_ve_a_empresa_para_imprimir_o_cupom_fiscal(self, client, monkeypatch):\n"
    ],
    [
      "Alteração 8",
      "    def test_qualquer_usuario_ve_a_empresa_para_imprimir_o_cupom_fiscal(self, client, monkeypatch):\n        monkeypatch.setattr(\"app.api.v1.fiscal.app_settings.FISCAL_PROVIDER\", \"none\")\n",
      "    def test_qualquer_usuario_ve_a_empresa_para_imprimir_o_cupom_fiscal(self, client, monkeypatch):\n        monkeypatch.setattr(\"app.services.fiscal.registry.app_settings.FISCAL_PROVIDER\", \"none\")\n"
    ],
    [
      "Alteração 9",
      "    def test_sem_cnpj_cadastrado_nao_ha_empresa_emitente(self, client, monkeypatch):\n        monkeypatch.setattr(\"app.api.v1.fiscal.app_settings.FISCAL_PROVIDER\", \"none\")\n",
      "    def test_sem_cnpj_cadastrado_nao_ha_empresa_emitente(self, client, monkeypatch):\n        monkeypatch.setattr(\"app.services.fiscal.registry.app_settings.FISCAL_PROVIDER\", \"none\")\n"
    ],
    [
      "Alteração 10",
      "        monkeypatch.setattr(\"app.api.v1.fiscal.app_settings.FISCAL_PROVIDER\", \"fake\")\n        monkeypatch.setattr(\"app.api.v1.fiscal.app_settings.ENVIRONMENT\", \"production\")\n",
      "        monkeypatch.setattr(\"app.services.fiscal.registry.app_settings.FISCAL_PROVIDER\", \"fake\")\n        monkeypatch.setattr(\"app.services.fiscal.registry.app_settings.ENVIRONMENT\", \"production\")\n"
    ],
    [
      "Alteração 11",
      "        monkeypatch.setattr(fiscal_service.app_settings, \"FISCAL_PROVIDER\", \"none\")\n",
      "        monkeypatch.setattr(\"app.services.fiscal.registry.app_settings.FISCAL_PROVIDER\", \"none\")\n"
    ]
  ],
  "backend/tests/test_fiscal_c3_api.py::registry": [
    [
      "Alteração 1",
      "from app.services import fiscal_cancel, fiscal_retry  # noqa: E402\n",
      "from app.services import fiscal_retry  # noqa: E402\nfrom app.services.fiscal import registry  # noqa: E402\n"
    ],
    [
      "Alteração 2",
      "    def test_consultar_situacao_sem_provedor_e_409(self, client, monkeypatch):\n        monkeypatch.setattr(fiscal_cancel.app_settings, \"FISCAL_PROVIDER\", \"none\")\n",
      "    def test_consultar_situacao_sem_provedor_e_409(self, client, monkeypatch):\n        monkeypatch.setattr(registry.app_settings, \"FISCAL_PROVIDER\", \"none\")\n"
    ],
    [
      "Alteração 3",
      "        assert response.status_code == 409 and \"não configurada\" in response.json()[\"detail\"]\n",
      "        assert response.status_code == 409 and \"sem modo de emissão\" in response.json()[\"detail\"]\n"
    ],
    [
      "Alteração 4",
      "    def test_consultar_situacao_de_pedido_inexistente_e_404(self, client, monkeypatch):\n        monkeypatch.setattr(fiscal_cancel.app_settings, \"FISCAL_PROVIDER\", \"fake\")\n",
      "    def test_consultar_situacao_de_pedido_inexistente_e_404(self, client, monkeypatch):\n        monkeypatch.setattr(registry.app_settings, \"FISCAL_PROVIDER\", \"fake\")\n"
    ],
    [
      "Alteração 5",
      "    def test_reenviar_pendentes_so_para_administrador(self, client, monkeypatch):\n        monkeypatch.setattr(fiscal_cancel.app_settings, \"FISCAL_PROVIDER\", \"none\")\n",
      "    def test_reenviar_pendentes_so_para_administrador(self, client, monkeypatch):\n        monkeypatch.setattr(registry.app_settings, \"FISCAL_PROVIDER\", \"none\")\n"
    ],
    [
      "Alteração 6",
      "    def test_reenviar_pendentes_com_a_emissao_desligada_e_409_e_nao_zero_pendentes(self, client, monkeypatch):\n        monkeypatch.setattr(fiscal_cancel.app_settings, \"FISCAL_PROVIDER\", \"fake\")\n",
      "    def test_reenviar_pendentes_com_a_emissao_desligada_e_409_e_nao_zero_pendentes(self, client, monkeypatch):\n        monkeypatch.setattr(registry.app_settings, \"FISCAL_PROVIDER\", \"fake\")\n"
    ],
    [
      "Alteração 7",
      "    def test_reenviar_pendentes_com_provedor_devolve_quantos_tentou(self, client, monkeypatch):\n        monkeypatch.setattr(fiscal_cancel.app_settings, \"FISCAL_PROVIDER\", \"fake\")\n",
      "    def test_reenviar_pendentes_com_provedor_devolve_quantos_tentou(self, client, monkeypatch):\n        monkeypatch.setattr(registry.app_settings, \"FISCAL_PROVIDER\", \"fake\")\n"
    ],
    [
      "Alteração 8",
      "        monkeypatch.setattr(\"app.services.fiscal_golive.app_settings.FISCAL_PROVIDER\", \"none\")\n",
      ""
    ]
  ]
}

for rel, edits in EDITS.items():
    path = Path(rel.split("::")[0])
    text = path.read_text(encoding="utf-8", newline="")
    crlf = "\r\n" in text
    text = text.replace("\r\n", "\n")
    for label, old, new in edits:
        count = text.count(old)
        if count != 1:
            sys.exit(f"{rel}: '{label}' casou {count} vezes (esperado 1)")
        text = text.replace(old, new)
    path.write_text(text.replace("\n", "\r\n") if crlf else text, encoding="utf-8", newline="")
    print(f"ok {rel}")
```

```bash
python apply_edits.py
```
Expected: `ok` para os cinco arquivos.

- [ ] **Step 2: Rodar e ver falhar**

```bash
cd backend && python -m pytest -q tests/test_fiscal_registry.py tests/test_fiscal_golive.py; cd ..
```
Expected: FAIL (`No module named 'app.services.fiscal.registry'`).

- [ ] **Step 3: Implementar**

Create `backend/app/services/fiscal/registry.py`:

```python
"""Escolhe o adaptador de emissão a partir do MODO salvo nas configurações fiscais (banco), não de variável de ambiente.

Modos: `none` (desligado), `sefaz_direto` (o sistema fala com a SEFAZ com o certificado A1; o adaptador chega na etapa E2) e `fake`
(só em desenvolvimento). Um modo sem adaptador pronto devolve `None`: nada é emitido e a tela explica.
"""
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from app.core.config import settings as app_settings
from app.models.fiscal import FiscalSettings
from app.services.fiscal.provider import FiscalProvider, get_provider

# Modos que emitem nota de verdade (o `fake` não conta). "provider" entra quando existir o adaptador do provedor.
REAL_MODES = ("sefaz_direto",)


def settings_query():
    """Consulta única da linha de configuração; a ordem fixa garante que todos leiam a mesma linha."""
    return select(FiscalSettings).order_by(FiscalSettings.id).limit(1)


def effective_mode(saved_mode: str | None) -> str:
    """O modo em vigor. Em desenvolvimento, FISCAL_PROVIDER=fake força o provedor falso (atalho de quem desenvolve)."""
    forced = (app_settings.FISCAL_PROVIDER or "").strip().lower()
    if forced == "fake" and app_settings.ENVIRONMENT == "development":
        return "fake"
    return saved_mode or "none"


def provider_for_mode(mode: str) -> FiscalProvider | None:
    """None = nada a emitir: modo desligado, ou modo cujo adaptador ainda não existe, ou provedor falso fora de desenvolvimento."""
    if mode == "fake":
        try:
            return get_provider("fake", app_settings.ENVIRONMENT)
        except RuntimeError:
            return None
    return None  # none e sefaz_direto (o adaptador chega na E2)


def provider_for_settings(row: object | None) -> FiscalProvider | None:
    return provider_for_mode(effective_mode(getattr(row, "mode", None)))


def mode_available(mode: str) -> bool:
    """O adaptador deste modo existe neste sistema?"""
    return provider_for_mode(mode) is not None


async def load_provider(db: AsyncSession) -> FiscalProvider | None:
    """O provedor a usar agora, conforme o modo salvo. Com o atalho de desenvolvimento nem consulta o banco."""
    if effective_mode(None) == "fake":
        return provider_for_mode("fake")
    row = (await db.execute(settings_query())).scalar_one_or_none()
    return provider_for_settings(row)
```

Substitua o conteúdo de `backend/app/services/fiscal_golive.py` por:

```python
"""Conferências para mudar a emissão fiscal de homologação para produção. Produção emite notas de verdade, com valor fiscal."""
import re
from typing import Any

from sqlalchemy import func, select
from sqlalchemy.ext.asyncio import AsyncSession

from app.models.order import Order
from app.services import fiscal_secrets
from app.services.fiscal.registry import REAL_MODES, mode_available


async def go_live_problems(db: AsyncSession, data: Any) -> list[str]:
    """O que ainda impede a ida para produção (lista vazia = pode ir). `data` é o FiscalSettingsIn que está sendo salvo."""
    problems: list[str] = []

    if data.mode not in REAL_MODES:
        problems.append("escolha um modo de emissão real (SEFAZ direto): o desligado e o de teste não emitem nota de verdade")
    elif not mode_available(data.mode):
        problems.append("o modo escolhido ainda não está disponível neste sistema (em desenvolvimento)")
    elif data.mode == "sefaz_direto":
        status = await fiscal_secrets.secrets_status(db)
        certificate = status["certificate"]
        if not certificate["configured"]:
            problems.append("cadastre o certificado digital A1")
        else:
            if certificate["days_left"] < 0:
                problems.append("o certificado digital venceu")
            company_cnpj = re.sub(r"\D", "", data.cnpj or "")
            if certificate["cnpj"] and company_cnpj and certificate["cnpj"] != company_cnpj:
                problems.append("o CNPJ do certificado é diferente do CNPJ da empresa")
        if not status["csc_prod"]["configured"]:
            problems.append("cadastre o CSC de produção")

    if not data.enabled:
        problems.append("ligue a emissão e preencha todos os dados da empresa")

    hml_authorized = (
        await db.execute(
            select(func.count()).select_from(Order).where(
                Order.fiscal_status.in_(("authorized", "cancelled")), Order.fiscal_reference.like("hml-%")
            )
        )
    ).scalar_one()
    if not hml_authorized:  # autorizada e depois cancelada (para testar o cancelamento) também vale
        problems.append("faça ao menos uma venda de teste autorizada em homologação")

    if not data.production_confirmation:
        problems.append("confirme que o contador validou o cupom emitido em homologação")
    return problems
```

Mesmo procedimento de edição: os serviços, o reenvio e as rotas passam a pedir o provedor ao registro, e o status informa o modo:

```python
import sys
from pathlib import Path

EDITS = {
  "backend/app/services/fiscal_service.py::registry": [
    [
      "Alteração 1",
      "from app.core.config import settings as app_settings\n",
      ""
    ],
    [
      "Alteração 2",
      "from app.services.fiscal.provider import EmitResult, FiscalProvider, get_provider\n",
      "from app.services.fiscal.provider import EmitResult, FiscalProvider\nfrom app.services.fiscal.registry import load_provider, settings_query\n"
    ],
    [
      "Alteração 3",
      "\n\ndef settings_query():\n    \"\"\"Consulta única da linha de configuração; a ordem fixa garante que todos leiam a mesma linha.\"\"\"\n    return select(FiscalSettings).order_by(FiscalSettings.id).limit(1)\n",
      ""
    ],
    [
      "Alteração 4",
      "        provider = get_provider(app_settings.FISCAL_PROVIDER, app_settings.ENVIRONMENT)\n        if provider is None:\n            return\n",
      ""
    ],
    [
      "Alteração 5",
      "        async with AsyncSessionLocal() as db:\n",
      "        async with AsyncSessionLocal() as db:\n            provider = await load_provider(db)  # o modo vem das configurações (banco)\n            if provider is None:\n                return\n"
    ],
    [
      "Alteração 6",
      "    try:\n        provider = get_provider(app_settings.FISCAL_PROVIDER, app_settings.ENVIRONMENT)\n    except (RuntimeError, ValueError):\n        provider = None\n",
      "    provider = await load_provider(db)\n"
    ],
    [
      "Alteração 7",
      "        raise HTTPException(status_code=409, detail=\"Emissão fiscal não configurada neste servidor\")\n",
      "        raise HTTPException(status_code=409, detail=\"Emissão fiscal sem modo de emissão disponível: escolha e configure em Configurações > Fiscal\")\n"
    ]
  ],
  "backend/app/services/fiscal_cancel.py::registry": [
    [
      "Alteração 1",
      "from app.core.config import settings as app_settings\n",
      ""
    ],
    [
      "Alteração 2",
      "from app.services.fiscal.provider import FiscalProvider, ProviderUnavailable, get_provider\n",
      "from app.services.fiscal.provider import FiscalProvider, ProviderUnavailable\nfrom app.services.fiscal.registry import provider_for_settings\n"
    ],
    [
      "Alteração 3",
      "\n\ndef configured_provider() -> FiscalProvider | None:\n    \"\"\"O provedor configurado no servidor, ou None (desligado, ou um valor que não pode ser usado neste ambiente).\"\"\"\n    try:\n        return get_provider(app_settings.FISCAL_PROVIDER, app_settings.ENVIRONMENT)\n    except (RuntimeError, ValueError):\n        return None\n",
      ""
    ],
    [
      "Alteração 4",
      "    provider = configured_provider()\n",
      "    provider = provider_for_settings(fiscal_settings)\n"
    ],
    [
      "Alteração 5",
      "        raise HTTPException(status_code=409, detail=\"Emissão fiscal não configurada neste servidor: não dá para cancelar a NFC-e deste pedido.\")\n",
      "        raise HTTPException(status_code=409, detail=\"Emissão fiscal sem modo de emissão disponível: não dá para cancelar a NFC-e deste pedido.\")\n"
    ]
  ],
  "backend/app/services/fiscal_retry.py::registry": [
    [
      "Alteração 1",
      "from app.services.fiscal_cancel import configured_provider\n",
      "from app.services.fiscal.registry import load_provider\n"
    ],
    [
      "Alteração 2",
      "            provider = configured_provider()\n            if provider is None:\n                continue\n",
      ""
    ],
    [
      "Alteração 3",
      "            async with AsyncSessionLocal() as db:\n",
      "            async with AsyncSessionLocal() as db:\n                provider = await load_provider(db)  # o modo pode mudar nas configurações a qualquer hora\n                if provider is None:\n                    continue\n"
    ],
    [
      "Alteração 4",
      "    \"\"\"Inicia o reenvio automático se houver provedor e intervalo configurados; devolve a tarefa (para cancelar ao desligar).\"\"\"\n",
      "    \"\"\"Inicia o reenvio automático se o intervalo for maior que zero; devolve a tarefa (para cancelar ao desligar).\n    Cada ciclo confere o modo nas configurações: sem modo de emissão disponível, o ciclo não faz nada.\"\"\"\n"
    ],
    [
      "Alteração 5",
      "    if interval <= 0 or configured_provider() is None:\n",
      "    if interval <= 0:\n"
    ]
  ],
  "backend/app/api/v1/fiscal.py::registry": [
    [
      "Alteração 1",
      "from app.core.config import settings as app_settings\n",
      ""
    ],
    [
      "Alteração 2",
      "from app.services.fiscal.provider import get_provider\n",
      "from app.services.fiscal.registry import effective_mode, load_provider, provider_for_settings\n"
    ],
    [
      "Alteração 3",
      "    \"\"\"Para o PDV: a emissão está ligada, há provedor configurado e em que ambiente.\"\"\"\n",
      "    \"\"\"Para o PDV: a emissão está ligada, o modo de emissão escolhido tem adaptador pronto e em que ambiente.\"\"\"\n"
    ],
    [
      "Alteração 4",
      "    try:\n        configured = get_provider(app_settings.FISCAL_PROVIDER, app_settings.ENVIRONMENT) is not None\n    except (RuntimeError, ValueError):\n        configured = False\n",
      "    configured = provider_for_settings(row) is not None\n"
    ],
    [
      "Alteração 5",
      "        provider_configured=configured,\n",
      "        provider_configured=configured,\n        mode=effective_mode(row.mode if row else None),\n        mode_available=configured,\n"
    ],
    [
      "Alteração 6",
      "    \"\"\"\"Consultar situação\": pergunta ao provedor o que houve com a nota (útil depois de um tempo esgotado).\"\"\"\n    provider = fiscal_cancel.configured_provider()\n",
      "    \"\"\"\"Consultar situação\": pergunta ao provedor o que houve com a nota (útil depois de um tempo esgotado).\"\"\"\n    provider = await load_provider(db)\n"
    ],
    [
      "Alteração 7",
      "    if provider is None:\n        raise HTTPException(status_code=409, detail=\"Emissão fiscal não configurada neste servidor\")\n    order = await fiscal_cancel.refresh_order(db, order_id, provider, current_user.name)\n",
      "    if provider is None:\n        raise HTTPException(status_code=409, detail=\"Emissão fiscal sem modo de emissão disponível\")\n    order = await fiscal_cancel.refresh_order(db, order_id, provider, current_user.name)\n"
    ],
    [
      "Alteração 8",
      "    \"\"\"Reenvia agora as NFC-e pendentes por falha do provedor (o servidor também faz isso sozinho, de tempos em tempos).\"\"\"\n    provider = fiscal_cancel.configured_provider()\n",
      "    \"\"\"Reenvia agora as NFC-e pendentes por falha do provedor (o servidor também faz isso sozinho, de tempos em tempos).\"\"\"\n    provider = await load_provider(db)\n"
    ],
    [
      "Alteração 9",
      "    if provider is None:\n        raise HTTPException(status_code=409, detail=\"Emissão fiscal não configurada neste servidor\")\n    try:\n",
      "    if provider is None:\n        raise HTTPException(status_code=409, detail=\"Emissão fiscal sem modo de emissão disponível\")\n    try:\n"
    ]
  ],
  "backend/app/schemas/fiscal.py::registry": [
    [
      "Alteração 1",
      "    provider_configured: bool\n",
      "    provider_configured: bool\n    # Modo em vigor (none | sefaz_direto | fake) e se o adaptador dele já existe neste sistema.\n    mode: str = \"none\"\n    mode_available: bool = False\n"
    ]
  ]
}

for rel, edits in EDITS.items():
    path = Path(rel.split("::")[0])
    text = path.read_text(encoding="utf-8", newline="")
    crlf = "\r\n" in text
    text = text.replace("\r\n", "\n")
    for label, old, new in edits:
        count = text.count(old)
        if count != 1:
            sys.exit(f"{rel}: '{label}' casou {count} vezes (esperado 1)")
        text = text.replace(old, new)
    path.write_text(text.replace("\n", "\r\n") if crlf else text, encoding="utf-8", newline="")
    print(f"ok {rel}")
```

```bash
python apply_edits.py
```
Expected: `ok` para os cinco arquivos.

- [ ] **Step 4: Rodar e ver passar**

```bash
cd backend && python -m pytest -q; cd ..
```
Expected: `261 passed` (244 + 17).

- [ ] **Step 5: Conferir que o app sobe e expõe as rotas novas**

```bash
cd backend && python -c "import app.main as m; print(sorted(p for p in m.app.openapi()['paths'] if '/fiscal/secrets' in p))"; cd ..
```
Expected: `['/api/v1/fiscal/secrets', '/api/v1/fiscal/secrets/certificate', '/api/v1/fiscal/secrets/csc/{environment}']`. (Precisa das variáveis de ambiente do `.env` do backend.)

- [ ] **Step 6: Commit**

```bash
git add backend/app/services/fiscal/registry.py backend/app/services/fiscal_golive.py backend/app/services/fiscal_service.py backend/app/services/fiscal_cancel.py backend/app/services/fiscal_retry.py backend/app/api/v1/fiscal.py backend/app/schemas/fiscal.py backend/tests/test_fiscal_registry.py backend/tests/test_fiscal_golive.py backend/tests/test_fiscal_service.py backend/tests/test_fiscal_cancel.py backend/tests/test_fiscal_retry.py backend/tests/test_fiscal_api.py backend/tests/test_fiscal_c3_api.py
git commit -m "feat(fiscal): registro de modos de emissão lido do banco e trava de produção pelo modo direto"
```

---

### Task 5: Frontend — tipos, chamadas e regras puras

**Files:**
- Modify: `frontend/src/types/fiscal.ts`, `frontend/src/api/fiscal.ts`, `frontend/src/utils/fiscal.ts`, `frontend/src/utils/fiscal.test.ts`

**Interfaces:**
- Produces: `FiscalMode`; `FiscalFields` com `cst_ibs_cbs`, `c_class_trib`, `aliquota_ibs`, `aliquota_cbs`; `FiscalSettings.mode`; `FiscalStatusInfo.mode/mode_available`; `CertificateStatus`, `CscStatus`, `FiscalSecrets`; `FiscalSettingsBody.mode?`; `getFiscalSecrets()`, `uploadCertificate(file, password)` (multipart), `deleteCertificate()`, `saveCsc(environment, id, token)`, `deleteCsc(environment)`; `modeOptions(current, development)`, `modeNotice(mode, available)`, `certificateVariant(level)`, `certificateSummary(certificate)`; `fiscalPayload` leva os campos novos (vírgula das alíquotas vira ponto).

- [ ] **Step 1: Escrever os testes que falham**

Mesmo procedimento de edição (salve como `apply_edits.py` fora do repositório e rode da raiz). Aqui ele acrescenta os testes novos a `utils/fiscal.test.ts`:

```python
import sys
from pathlib import Path

EDITS = {
  "frontend/src/utils/fiscal.test.ts::logic": [
    [
      "Alteração 1",
      "  cancelReasonError,\n",
      "  cancelReasonError,\n  certificateSummary,\n  certificateVariant,\n"
    ],
    [
      "Alteração 2",
      "  MAX_FISCAL_POLLS,\n",
      "  MAX_FISCAL_POLLS,\n  modeNotice,\n  modeOptions,\n"
    ],
    [
      "Alteração 3",
      "      ncm: '', cest: '', origem: '', cfop: '', cst_icms: '', aliquota_icms: '', cst_pis: '', cst_cofins: '',\n",
      "      ncm: '', cest: '', origem: '', cfop: '', cst_icms: '', aliquota_icms: '', cst_pis: '', cst_cofins: '',\n      cst_ibs_cbs: '', c_class_trib: '', aliquota_ibs: '', aliquota_cbs: '',\n"
    ],
    [
      "Alteração 4",
      "    expect(fiscalPayload(values)).toEqual({ ncm: '0803.90.00', origem: 0, aliquota_icms: '20.5', cst_icms: '41' })\n",
      "    expect(fiscalPayload(values)).toEqual({ ncm: '0803.90.00', origem: 0, aliquota_icms: '20.5', cst_icms: '41' })\n  })\n\n  it('os campos da reforma tributária vão no corpo, com a vírgula das alíquotas trocada por ponto', () => {\n    const values = { ...fiscalFormDefaults(null), cst_ibs_cbs: '000', c_class_trib: '000001', aliquota_ibs: '0,1', aliquota_cbs: '0,9' }\n    expect(fiscalPayload(values)).toEqual({ cst_ibs_cbs: '000', c_class_trib: '000001', aliquota_ibs: '0.1', aliquota_cbs: '0.9' })\n"
    ],
    [
      "Alteração 5",
      "  for (const bad of ['', 'produ', 'homologação', 'PRODUÇÃO!']) expect(isProductionWord(bad)).toBe(false)\n})\n",
      "  for (const bad of ['', 'produ', 'homologação', 'PRODUÇÃO!']) expect(isProductionWord(bad)).toBe(false)\n})\n\ndescribe('modo de emissão', () => {\n  it('o provedor de teste só é oferecido em desenvolvimento (ou se já estiver salvo)', () => {\n    expect(modeOptions('none', false).map((o) => o.value)).toEqual(['none', 'sefaz_direto'])\n    expect(modeOptions('none', true).map((o) => o.value)).toEqual(['none', 'sefaz_direto', 'fake'])\n    expect(modeOptions('fake', false).map((o) => o.value)).toContain('fake')\n  })\n\n  it('o aviso diz o que o modo faz hoje', () => {\n    expect(modeNotice('none', false)).toMatch(/desligada/)\n    expect(modeNotice('sefaz_direto', false)).toMatch(/em desenvolvimento/)\n    expect(modeNotice('sefaz_direto', true)).toBeNull()\n    expect(modeNotice('fake', true)).toMatch(/simuladas/)\n  })\n})\n\ndescribe('certificado digital', () => {\n  const base = { configured: true, subject: 'EMPRESA TESTE LTDA', not_after: '2027-08-15T12:00:00+00:00' }\n\n  it('a cor segue a urgência do vencimento', () => {\n    expect(certificateVariant('ok')).toBe('green')\n    expect(certificateVariant('warn')).toBe('amber')\n    expect(certificateVariant('critical')).toBe('red')\n    expect(certificateVariant(null)).toBe('slate')\n  })\n\n  it('resume titular, validade e dias que faltam', () => {\n    expect(certificateSummary({ ...base, days_left: 300 })).toBe('Certificado de EMPRESA TESTE LTDA, válido até 08/2027 (faltam 300 dias)')\n    expect(certificateSummary({ ...base, days_left: 1 })).toContain('falta 1 dia')\n  })\n\n  it('vencido pede um novo; sem certificado não há resumo', () => {\n    expect(certificateSummary({ ...base, days_left: -2 })).toMatch(/VENCIDO em 08\\/2027\\. Cadastre um novo\\./)\n    expect(certificateSummary({ configured: false })).toBeNull()\n  })\n})\n"
    ]
  ]
}

for rel, edits in EDITS.items():
    path = Path(rel.split("::")[0])
    text = path.read_text(encoding="utf-8", newline="")
    crlf = "\r\n" in text
    text = text.replace("\r\n", "\n")
    for label, old, new in edits:
        count = text.count(old)
        if count != 1:
            sys.exit(f"{rel}: '{label}' casou {count} vezes (esperado 1)")
        text = text.replace(old, new)
    path.write_text(text.replace("\n", "\r\n") if crlf else text, encoding="utf-8", newline="")
    print(f"ok {rel}")
```

```bash
python apply_edits.py
```
Expected: `ok frontend/src/utils/fiscal.test.ts`.

- [ ] **Step 2: Rodar e ver falhar**

```bash
cd frontend && npx vitest run src/utils/fiscal.test.ts; cd ..
```
Expected: FAIL (as funções novas ainda não existem: `modeOptions is not a function` e afins).

- [ ] **Step 3: Implementar**

```python
import sys
from pathlib import Path

EDITS = {
  "frontend/src/types/fiscal.ts::logic": [
    [
      "Alteração 1",
      "  cst_cofins: string | null\n",
      "  cst_cofins: string | null\n  // Reforma tributária (grupo IBS/CBS): os valores vêm do contador\n  cst_ibs_cbs: string | null\n  c_class_trib: string | null\n  aliquota_ibs: number | string | null\n  aliquota_cbs: number | string | null\n"
    ],
    [
      "Alteração 2",
      "\nexport interface FiscalSettings {\n",
      "\n/** Como a nota é emitida: desligado, direto na SEFAZ (certificado A1) ou o provedor falso (só em desenvolvimento). */\nexport type FiscalMode = 'none' | 'sefaz_direto' | 'fake'\n\nexport interface FiscalSettings {\n"
    ],
    [
      "Alteração 3",
      "  regime: FiscalRegime\n",
      "  regime: FiscalRegime\n  mode: FiscalMode\n"
    ],
    [
      "Alteração 4",
      "  provider_configured: boolean\n",
      "  provider_configured: boolean\n  /** Modo em vigor e se o adaptador dele já existe neste sistema. */\n  mode: FiscalMode\n  mode_available: boolean\n"
    ],
    [
      "Alteração 5",
      "  issuer: FiscalIssuer | null\n",
      "  issuer: FiscalIssuer | null\n}\n\nexport type CertificateLevel = 'ok' | 'warn' | 'critical'\n\n/** O que a tela sabe do certificado: só dados públicos (nunca o arquivo nem a senha). */\nexport interface CertificateStatus {\n  configured: boolean\n  subject?: string | null\n  cnpj?: string | null\n  not_after?: string | null\n  days_left?: number | null\n  level?: CertificateLevel | null\n}\n\nexport interface CscStatus {\n  configured: boolean\n  id?: string | null\n}\n\n/** Resposta de GET /fiscal/secrets. `vault_available` = o servidor tem a chave mestra dos segredos. */\nexport interface FiscalSecrets {\n  vault_available: boolean\n  certificate: CertificateStatus\n  csc_hml: CscStatus\n  csc_prod: CscStatus\n"
    ]
  ],
  "frontend/src/api/fiscal.ts::logic": [
    [
      "Alteração 1",
      "  FiscalEnvironment,\n",
      "  FiscalEnvironment,\n  FiscalMode,\n"
    ],
    [
      "Alteração 2",
      "  FiscalRegime,\n",
      "  FiscalRegime,\n  FiscalSecrets,\n"
    ],
    [
      "Alteração 3",
      "  regime: FiscalRegime\n",
      "  regime: FiscalRegime\n  mode?: FiscalMode\n"
    ],
    [
      "Alteração 4",
      "  const { data } = await api.put<FiscalSettings>('/fiscal/settings', body)\n",
      "  const { data } = await api.put<FiscalSettings>('/fiscal/settings', body)\n  return data\n}\n\n/** O que está cadastrado no cofre (certificado, CSC), sem nenhum conteúdo secreto. */\nexport async function getFiscalSecrets(): Promise<FiscalSecrets> {\n  const { data } = await api.get<FiscalSecrets>('/fiscal/secrets')\n  return data\n}\n\n/** Envia o .pfx e a senha. O servidor confere senha, validade e CNPJ e guarda cifrado; a resposta não traz segredo. */\nexport async function uploadCertificate(file: File, password: string): Promise<FiscalSecrets> {\n  const form = new FormData()\n  form.append('file', file)\n  form.append('password', password)\n  const { data } = await api.put<FiscalSecrets>('/fiscal/secrets/certificate', form, { headers: { 'Content-Type': 'multipart/form-data' } })\n  return data\n}\n\nexport async function deleteCertificate(): Promise<FiscalSecrets> {\n  const { data } = await api.delete<FiscalSecrets>('/fiscal/secrets/certificate')\n  return data\n}\n\nexport async function saveCsc(environment: FiscalEnvironment, id: string, token: string): Promise<FiscalSecrets> {\n  const { data } = await api.put<FiscalSecrets>(`/fiscal/secrets/csc/${environment}`, { id, token })\n  return data\n}\n\nexport async function deleteCsc(environment: FiscalEnvironment): Promise<FiscalSecrets> {\n  const { data } = await api.delete<FiscalSecrets>(`/fiscal/secrets/csc/${environment}`)\n"
    ]
  ],
  "frontend/src/utils/fiscal.ts::logic": [
    [
      "Alteração 1",
      "import type { FiscalFieldKey, FiscalFields, FiscalStatus, OrderFiscal } from '@/types/fiscal'\n",
      "import type { CertificateLevel, CertificateStatus, FiscalFieldKey, FiscalFields, FiscalMode, FiscalStatus, OrderFiscal } from '@/types/fiscal'\n"
    ],
    [
      "Alteração 2",
      "export const FISCAL_KEYS: FiscalFieldKey[] = ['ncm', 'cest', 'origem', 'cfop', 'cst_icms', 'aliquota_icms', 'cst_pis', 'cst_cofins']\n",
      "export const FISCAL_KEYS: FiscalFieldKey[] = [\n  'ncm', 'cest', 'origem', 'cfop', 'cst_icms', 'aliquota_icms', 'cst_pis', 'cst_cofins',\n  'cst_ibs_cbs', 'c_class_trib', 'aliquota_ibs', 'aliquota_cbs',\n]\n"
    ],
    [
      "Alteração 3",
      "  cst_cofins: string\n",
      "  cst_cofins: string\n  cst_ibs_cbs: string\n  c_class_trib: string\n  aliquota_ibs: string\n  aliquota_cbs: string\n"
    ],
    [
      "Alteração 4",
      "    else if (key === 'aliquota_icms') payload.aliquota_icms = text.replace(',', '.')\n",
      "    else if (key === 'aliquota_icms' || key === 'aliquota_ibs' || key === 'aliquota_cbs') payload[key] = text.replace(',', '.')\n"
    ],
    [
      "Alteração 5",
      "  return word === 'PRODUÇÃO' || word === 'PRODUCAO'\n}\n",
      "  return word === 'PRODUÇÃO' || word === 'PRODUCAO'\n}\n\nexport interface ModeOption {\n  value: FiscalMode\n  label: string\n}\n\n/** Modos de emissão que a tela oferece. O provedor falso só existe em desenvolvimento (o servidor recusa fora dele). */\nexport function modeOptions(current: FiscalMode, development: boolean): ModeOption[] {\n  const options: ModeOption[] = [\n    { value: 'none', label: 'Nenhum (emissão desligada)' },\n    { value: 'sefaz_direto', label: 'SEFAZ direto (certificado digital A1)' },\n  ]\n  if (development || current === 'fake') options.push({ value: 'fake', label: 'Provedor de teste (só desenvolvimento)' })\n  return options\n}\n\n/** Aviso sob o seletor de modo: o que esse modo faz hoje. `null` quando não há nada a avisar. */\nexport function modeNotice(mode: FiscalMode, available: boolean): string | null {\n  if (mode === 'none') return 'Com a emissão desligada, nenhuma NFC-e é emitida e o PDV funciona como sempre.'\n  if (mode === 'sefaz_direto' && !available) {\n    return 'O envio direto à SEFAZ ainda está em desenvolvimento neste sistema: dá para cadastrar o certificado e o CSC agora, mas nenhuma nota é emitida neste modo.'\n  }\n  if (mode === 'fake') return 'Modo de teste: as notas são simuladas e não existem na SEFAZ.'\n  return null\n}\n\nconst CERTIFICATE_VARIANT: Record<CertificateLevel, 'green' | 'amber' | 'red'> = { ok: 'green', warn: 'amber', critical: 'red' }\n\nexport function certificateVariant(level: CertificateLevel | null | undefined): 'green' | 'amber' | 'red' | 'slate' {\n  return level ? CERTIFICATE_VARIANT[level] : 'slate'\n}\n\n/** \"Certificado de EMPRESA…, válido até 12/2026 (faltam 300 dias)\" ou o aviso de vencido. Sem certificado: `null`. */\nexport function certificateSummary(certificate: CertificateStatus): string | null {\n  if (!certificate.configured || !certificate.not_after) return null\n  const until = new Date(certificate.not_after).toLocaleDateString('pt-BR', { month: '2-digit', year: 'numeric' })\n  const days = certificate.days_left ?? 0\n  const owner = certificate.subject ? `Certificado de ${certificate.subject}` : 'Certificado cadastrado'\n  if (days < 0) return `${owner}: VENCIDO em ${until}. Cadastre um novo.`\n  return `${owner}, válido até ${until} (${days === 1 ? 'falta 1 dia' : `faltam ${days} dias`})`\n}\n"
    ]
  ]
}

for rel, edits in EDITS.items():
    path = Path(rel.split("::")[0])
    text = path.read_text(encoding="utf-8", newline="")
    crlf = "\r\n" in text
    text = text.replace("\r\n", "\n")
    for label, old, new in edits:
        count = text.count(old)
        if count != 1:
            sys.exit(f"{rel}: '{label}' casou {count} vezes (esperado 1)")
        text = text.replace(old, new)
    path.write_text(text.replace("\n", "\r\n") if crlf else text, encoding="utf-8", newline="")
    print(f"ok {rel}")
```

```bash
python apply_edits.py
```
Expected: `ok` para os três arquivos.

- [ ] **Step 4: Rodar e ver passar, e checar os tipos**

```bash
cd frontend && npx vitest run && npx tsc -b; cd ..
```
Expected: `175 passed` (169 já existentes + 6 novos) e `tsc` sem saída.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/types/fiscal.ts frontend/src/api/fiscal.ts frontend/src/utils/fiscal.ts frontend/src/utils/fiscal.test.ts
git commit -m "feat(fiscal): tipos, chamadas e regras do modo de emissão, do cofre e do IBS/CBS"
```

---

### Task 6: Frontend — modo de emissão, cofre e campos de IBS/CBS

**Files:**
- Modify: `frontend/src/components/fiscal/FiscalFieldsForm.tsx`, `frontend/src/pages/settings/FiscalTab.tsx`

**Interfaces:**
- Consumes: Task 5.
- Produces: quatro campos novos no formulário fiscal (produto e tabela por categoria); seletor **Modo de emissão** com o aviso do que o modo faz hoje; cartão **Certificado digital e CSC (SEFAZ direto)**, visível só com o modo direto salvo: aviso vermelho quando o servidor não tem a chave mestra (com o comando para gerar), certificado com o vencimento colorido e "Remover", envio de `.pfx` + senha, e um bloco de CSC (ID + código) para homologação e outro para produção. Depois de qualquer envio os campos de senha e código são **limpos** e nada secreto é mostrado.

- [ ] **Step 1: Implementar**

Mesmo procedimento de edição (salve como `apply_edits.py` fora do repositório e rode da raiz):

```python
import sys
from pathlib import Path

EDITS = {
  "frontend/src/components/fiscal/FiscalFieldsForm.tsx::ui": [
    [
      "Alteração 1",
      "      <Input label=\"CST do COFINS\" placeholder=\"2 dígitos\" inputMode=\"numeric\" {...register('cst_cofins')} />\n",
      "      <Input label=\"CST do COFINS\" placeholder=\"2 dígitos\" inputMode=\"numeric\" {...register('cst_cofins')} />\n      <Input\n        label=\"CST do IBS/CBS\"\n        placeholder=\"3 dígitos\"\n        inputMode=\"numeric\"\n        hint=\"Reforma tributária: os valores vêm do contador\"\n        {...register('cst_ibs_cbs')}\n      />\n      <Input label=\"Classificação tributária (cClassTrib)\" placeholder=\"6 dígitos\" inputMode=\"numeric\" {...register('c_class_trib')} />\n      <Input label=\"Alíquota do IBS (%)\" placeholder=\"Ex.: 0,1\" inputMode=\"decimal\" {...register('aliquota_ibs')} />\n      <Input label=\"Alíquota da CBS (%)\" placeholder=\"Ex.: 0,9\" inputMode=\"decimal\" {...register('aliquota_cbs')} />\n"
    ]
  ],
  "frontend/src/pages/settings/FiscalTab.tsx::ui": [
    [
      "Alteração 1",
      "import {\n  deleteFiscalDefault,\n",
      "import {\n  deleteCertificate,\n  deleteCsc,\n  deleteFiscalDefault,\n"
    ],
    [
      "Alteração 2",
      "  deleteFiscalDefault,\n",
      "  deleteFiscalDefault,\n  getFiscalSecrets,\n"
    ],
    [
      "Alteração 3",
      "  retryPendingNfce,\n",
      "  retryPendingNfce,\n  saveCsc,\n"
    ],
    [
      "Alteração 4",
      "  saveFiscalSettings,\n",
      "  saveFiscalSettings,\n  uploadCertificate,\n"
    ],
    [
      "Alteração 5",
      "import { FiscalFieldsForm } from '@/components/fiscal/FiscalFieldsForm'\n",
      "import { FiscalFieldsForm } from '@/components/fiscal/FiscalFieldsForm'\nimport { Badge } from '@/components/ui/Badge'\n"
    ],
    [
      "Alteração 6",
      "import type { FiscalDefault, FiscalSettings } from '@/types/fiscal'\n",
      "import type { FiscalDefault, FiscalEnvironment, FiscalMode, FiscalSecrets, FiscalSettings } from '@/types/fiscal'\n"
    ],
    [
      "Alteração 7",
      "import {\n  cleanValidationMessage,\n",
      "import {\n  certificateSummary,\n  certificateVariant,\n  cleanValidationMessage,\n"
    ],
    [
      "Alteração 8",
      "  isProductionWord,\n",
      "  isProductionWord,\n  modeNotice,\n  modeOptions,\n"
    ],
    [
      "Alteração 9",
      "  regime: 'normal' | 'simples'\n",
      "  regime: 'normal' | 'simples'\n  mode: FiscalMode\n"
    ],
    [
      "Alteração 10",
      "    regime: s.regime,\n",
      "    regime: s.regime,\n    mode: s.mode,\n"
    ],
    [
      "Alteração 11",
      "  const { register, handleSubmit } = useForm<CompanyForm>({ defaultValues: companyDefaults(settings) })\n",
      "  const { register, handleSubmit, watch } = useForm<CompanyForm>({ defaultValues: companyDefaults(settings) })\n  const { data: status } = useQuery({ queryKey: ['fiscal', 'status'], queryFn: getFiscalStatus })\n  const mode = watch('mode')\n  const notice = modeNotice(mode, status?.mode === mode && status.mode_available)\n"
    ],
    [
      "Alteração 12",
      "        regime: v.regime,\n",
      "        regime: v.regime,\n        mode: v.mode,\n"
    ],
    [
      "Alteração 13",
      "      </label>\n\n",
      "      </label>\n\n      <div className=\"mt-4\">\n        <Select label=\"Modo de emissão\" {...register('mode')}>\n          {modeOptions(settings.mode, import.meta.env.DEV).map((o) => (\n            <option key={o.value} value={o.value}>{o.label}</option>\n          ))}\n        </Select>\n        {notice && <p className=\"mt-1 text-xs text-slate-500 dark:text-slate-400\">{notice}</p>}\n      </div>\n\n"
    ],
    [
      "Alteração 14",
      "      </div>\n    </Modal>\n  )\n",
      "      </div>\n    </Modal>\n  )\n}\n\n/** Um CSC (ID e código gerados na SEFAZ) de um ambiente. O código nunca volta do servidor: só \"cadastrado, ID 1\". */\nfunction CscRow({\n  environment,\n  label,\n  status,\n  disabled,\n  onDone,\n  onError,\n}: {\n  environment: FiscalEnvironment\n  label: string\n  status: FiscalSecrets['csc_hml']\n  disabled: boolean\n  onDone: (text: string) => void\n  onError: (err: unknown) => void\n}) {\n  const [id, setId] = useState('')\n  const [token, setToken] = useState('')\n  const save = useMutation({\n    mutationFn: () => saveCsc(environment, id, token),\n    onSuccess: () => {\n      setId('')\n      setToken('')\n      onDone(`${label} guardado.`)\n    },\n    onError,\n  })\n  const remove = useMutation({ mutationFn: () => deleteCsc(environment), onSuccess: () => onDone(`${label} removido.`), onError })\n\n  return (\n    <div className=\"mt-4 border-t border-slate-100 pt-4 dark:border-slate-700\">\n      <div className=\"flex flex-wrap items-center gap-2\">\n        <h3 className=\"text-sm font-medium text-slate-800 dark:text-slate-200\">{label}</h3>\n        {status.configured ? <Badge variant=\"green\">Cadastrado (ID {status.id})</Badge> : <Badge variant=\"slate\">Não cadastrado</Badge>}\n        {status.configured && (\n          <button className=\"text-sm text-red-600 hover:underline dark:text-red-400\" disabled={remove.isPending} onClick={() => remove.mutate()}>\n            Remover\n          </button>\n        )}\n      </div>\n      <div className=\"mt-2 grid grid-cols-1 gap-3 sm:grid-cols-[8rem_1fr_auto]\">\n        <Input label=\"ID do CSC\" placeholder=\"Ex.: 1\" inputMode=\"numeric\" value={id} onChange={(e) => setId(e.target.value)} />\n        <Input label=\"Código do CSC\" type=\"password\" autoComplete=\"new-password\" value={token} onChange={(e) => setToken(e.target.value)} />\n        <div className=\"flex items-end\">\n          <Button size=\"sm\" variant=\"secondary\" disabled={disabled || !id || !token} loading={save.isPending} onClick={() => save.mutate()}>\n            {status.configured ? 'Trocar' : 'Guardar'}\n          </Button>\n        </div>\n      </div>\n    </div>\n  )\n}\n\n/** Cofre do modo direto: certificado digital A1 e CSC. Tudo é cifrado no servidor e nada secreto volta para esta tela. */\nfunction SecretsCard() {\n  const qc = useQueryClient()\n  const { data: secrets } = useQuery({ queryKey: ['fiscal', 'secrets'], queryFn: getFiscalSecrets })\n  const [file, setFile] = useState<File | null>(null)\n  const [password, setPassword] = useState('')\n  const [fileKey, setFileKey] = useState(0)\n  const [message, setMessage] = useState<Message>(null)\n\n  const done = (text: string) => {\n    setMessage({ kind: 'ok', text })\n    qc.invalidateQueries({ queryKey: ['fiscal'] })\n  }\n  const fail = (err: unknown) => setMessage({ kind: 'error', text: cleanValidationMessage(getApiError(err)) })\n\n  const upload = useMutation({\n    mutationFn: () => uploadCertificate(file!, password),\n    onSuccess: () => {\n      setPassword('')\n      setFile(null)\n      setFileKey((k) => k + 1) // limpa o campo do arquivo\n      done('Certificado guardado.')\n    },\n    onError: fail,\n  })\n  const removeCertificate = useMutation({ mutationFn: deleteCertificate, onSuccess: () => done('Certificado removido.'), onError: fail })\n\n  if (!secrets) return null\n  const locked = !secrets.vault_available\n  const summary = certificateSummary(secrets.certificate)\n\n  return (\n    <div className={cardClass}>\n      <h2 className=\"font-semibold text-slate-900 dark:text-slate-100\">Certificado digital e CSC (SEFAZ direto)</h2>\n      <p className=\"mt-1 text-sm text-slate-500 dark:text-slate-400\">\n        Guardados cifrados no servidor. Depois de enviados, nada disto volta para esta tela: só o que é público (titular, vencimento, ID do CSC).\n      </p>\n\n      {locked && (\n        <div role=\"alert\" className=\"mt-3 rounded-lg border border-red-300 bg-red-50 px-3 py-2 text-sm text-red-900 dark:border-red-700 dark:bg-red-900/20 dark:text-red-200\">\n          O servidor não tem a <strong>chave mestra</strong> dos segredos, então não dá para guardar certificado nem CSC. No Coolify, crie a variável{' '}\n          <code>FISCAL_SECRET_KEY</code> com o valor gerado por{' '}\n          <code>python -c &quot;import secrets,base64;print(base64.b64encode(secrets.token_bytes(32)).decode())&quot;</code> e reinicie o app.\n        </div>\n      )}\n\n      <div className=\"mt-4 flex flex-wrap items-center gap-2\">\n        <h3 className=\"text-sm font-medium text-slate-800 dark:text-slate-200\">Certificado A1</h3>\n        {summary ? <Badge variant={certificateVariant(secrets.certificate.level)}>{summary}</Badge> : <Badge variant=\"slate\">Nenhum certificado cadastrado</Badge>}\n        {secrets.certificate.configured && (\n          <button className=\"text-sm text-red-600 hover:underline dark:text-red-400\" disabled={removeCertificate.isPending} onClick={() => removeCertificate.mutate()}>\n            Remover\n          </button>\n        )}\n      </div>\n      <div className=\"mt-2 grid grid-cols-1 gap-3 sm:grid-cols-[1fr_1fr_auto]\">\n        <div className=\"flex flex-col gap-1\">\n          <label htmlFor=\"certificate-file\" className=\"text-sm font-medium text-slate-700 dark:text-slate-300\">Arquivo do certificado (.pfx)</label>\n          <input\n            key={fileKey}\n            id=\"certificate-file\"\n            type=\"file\"\n            accept=\".pfx,.p12\"\n            onChange={(e) => setFile(e.target.files?.[0] ?? null)}\n            className=\"text-sm text-slate-700 dark:text-slate-300\"\n          />\n        </div>\n        <Input label=\"Senha do certificado\" type=\"password\" autoComplete=\"new-password\" value={password} onChange={(e) => setPassword(e.target.value)} />\n        <div className=\"flex items-end\">\n          <Button size=\"sm\" variant=\"secondary\" disabled={locked || !file || !password} loading={upload.isPending} onClick={() => upload.mutate()}>\n            {secrets.certificate.configured ? 'Trocar' : 'Enviar'}\n          </Button>\n        </div>\n      </div>\n\n      <CscRow environment=\"homologacao\" label=\"CSC de homologação\" status={secrets.csc_hml} disabled={locked} onDone={done} onError={fail} />\n      <CscRow environment=\"producao\" label=\"CSC de produção\" status={secrets.csc_prod} disabled={locked} onDone={done} onError={fail} />\n\n      {message && (\n        <p role=\"status\" className={`mt-4 text-sm ${message.kind === 'ok' ? 'text-green-700 dark:text-green-400' : 'text-red-700 dark:text-red-400'}`}>\n          {message.text}\n        </p>\n      )}\n    </div>\n  )\n"
    ],
    [
      "Alteração 15",
      "      )}\n      <PendingNotesCard />\n",
      "      )}\n      {settings?.mode === 'sefaz_direto' && <SecretsCard />}\n      <PendingNotesCard />\n"
    ]
  ]
}

for rel, edits in EDITS.items():
    path = Path(rel.split("::")[0])
    text = path.read_text(encoding="utf-8", newline="")
    crlf = "\r\n" in text
    text = text.replace("\r\n", "\n")
    for label, old, new in edits:
        count = text.count(old)
        if count != 1:
            sys.exit(f"{rel}: '{label}' casou {count} vezes (esperado 1)")
        text = text.replace(old, new)
    path.write_text(text.replace("\n", "\r\n") if crlf else text, encoding="utf-8", newline="")
    print(f"ok {rel}")
```

```bash
python apply_edits.py
```
Expected: `ok` para os dois arquivos.

- [ ] **Step 2: Checar tipos, testes e build**

```bash
cd frontend && npx tsc -b && npx vitest run && npx vite build; cd ..
```
Expected: `tsc` sem saída; `175 passed`; `vite build` termina com `built in`.

- [ ] **Step 3: Verificar no navegador (API simulada)**

Suba o app (`preview_start` com o `hortifacil-frontend`), entre como administrador (chave `hortifacil-auth` no `localStorage`), **desligue a impressão automática** (`pdv-printer` com `enabled:false`) e simule a API trocando `window.XMLHttpRequest` (a simulação precisa devolver **status HTTP** 422/409 com `{detail: '...'}`, não só 200). Use empresa e dados **fictícios**. Force `document.visibilityState` como `'visible'`.

Rotas simuladas: `GET /fiscal/status`, `GET /fiscal/settings` (empresa completa, `mode:'none'` na 1ª vez, `environment:'homologacao'`, `cancel_window_minutes:30`), `PUT /fiscal/settings` (devolve o corpo recebido), `GET /fiscal/secrets` (`{vault_available:true, certificate:{configured:false}, csc_hml:{configured:false}, csc_prod:{configured:false}}`), `PUT /fiscal/secrets/certificate` (multipart: guarde o corpo recebido para conferir), `PUT|DELETE /fiscal/secrets/csc/{environment}`, `DELETE /fiscal/secrets/certificate`.

1. **Seletor de modo:** em Configurações > Fiscal aparece **Modo de emissão** com "Nenhum", "SEFAZ direto" e (em desenvolvimento) "Provedor de teste"; com "Nenhum" o aviso diz que a emissão está desligada; **não** aparece o cartão do cofre. Salvar envia `mode:'none'` no `PUT`.
2. **Modo direto sem adaptador:** com `mode:'sefaz_direto'` salvo (e `mode_available:false` no status) o aviso diz que o envio direto ainda está em desenvolvimento e **aparece** o cartão "Certificado digital e CSC (SEFAZ direto)", com "Nenhum certificado cadastrado" e os dois CSC "Não cadastrado".
3. **Cofre sem chave:** com `vault_available:false` aparece o aviso vermelho (`role="alert"`) citando `FISCAL_SECRET_KEY` e o comando de gerar a chave, e os botões **Enviar**/**Guardar** ficam desabilitados.
4. **Certificado:** o botão **Enviar** só habilita com arquivo **e** senha. Ao enviar, o corpo é multipart com `file` e `password`; a mensagem "Certificado guardado." aparece, o campo da senha e o do arquivo **ficam vazios** e o cartão passa a mostrar (com `configured:true`, `subject`, `not_after`, `days_left:300`, `level:'ok'`) o resumo "Certificado de … válido até …" em **verde**; com `days_left:10` e `level:'critical'` fica **vermelho**; com `days_left:-2` diz "VENCIDO". **Remover** chama `DELETE`.
5. **CSC:** com ID `1` e código de 36 caracteres, **Guardar** chama `PUT /fiscal/secrets/csc/homologacao`; a resposta com `csc_hml:{configured:true, id:'1'}` mostra "Cadastrado (ID 1)" e os dois campos ficam **vazios**. Um 422 simulado mostra a mensagem **em vermelho** sem apagar o que foi digitado.
6. **Campos de IBS/CBS:** no cadastro de um produto (ou na tabela por categoria) aparecem "CST do IBS/CBS", "Classificação tributária (cClassTrib)", "Alíquota do IBS (%)" e "Alíquota da CBS (%)"; digitar `0,1` e salvar envia `aliquota_ibs:'0.1'`.

Expected: os 6 comportamentos. Se algum divergir, corrija antes do commit. Pare o servidor de preview ao terminar.

- [ ] **Step 4: Commit**

```bash
git add frontend/src/components/fiscal/FiscalFieldsForm.tsx frontend/src/pages/settings/FiscalTab.tsx
git commit -m "feat(fiscal): modo de emissão e cofre de certificado e CSC em Configurações > Fiscal"
```

---

### Task 7: Documentação, variável de ambiente e entrega

**Files:**
- Modify: `docker-compose.yaml`, `backend/.env.example`, `docs/fiscal/README.md`

- [ ] **Step 1: Documentar e repassar a chave mestra**

Mesmo procedimento de edição (salve como `apply_edits.py` fora do repositório e rode da raiz):

```python
import sys
from pathlib import Path

EDITS = {
  "docker-compose.yaml": [
    [
      "FISCAL_SECRET_KEY no compose",
      "      CORS_ORIGINS: ${CORS_ORIGINS:-http://localhost:3000}\n",
      "      CORS_ORIGINS: ${CORS_ORIGINS:-http://localhost:3000}\n      # Chave mestra do cofre de segredos fiscais (certificado, CSC). O compose só repassa o que está listado aqui.\n      FISCAL_SECRET_KEY: ${FISCAL_SECRET_KEY:-}\n"
    ]
  ],
  "backend/.env.example::secret": [
    [
      "FISCAL_SECRET_KEY no exemplo",
      "FISCAL_RETRY_INTERVAL_SECONDS=120\n",
      "FISCAL_RETRY_INTERVAL_SECONDS=120\n# Chave mestra (32 bytes em base64) que cifra o certificado digital e o CSC guardados no banco.\n# Gere com: python -c \"import secrets,base64;print(base64.b64encode(secrets.token_bytes(32)).decode())\"\n# Guarde só aqui e no Coolify; perdê-la obriga a cadastrar de novo o certificado e o CSC.\nFISCAL_SECRET_KEY=\n"
    ]
  ],
  "docs/fiscal/README.md": [
    [
      "Itens 3 e 4 do que falta",
      "3. Provedor escolhido e contratado, com o token guardado **só** nas variáveis de ambiente do Coolify.\n4. Adaptador do provedor real (um arquivo novo em `app/services/fiscal/`, registrado em `get_provider`).\n",
      "3. Um **modo de emissão real** funcionando. Hoje só a base existe (ver \"Escolha do modo de emissão\"): o adaptador do modo direto chega nas próximas etapas e o do provedor só depois de escolhido e contratado (o token dele iria para o cofre, nunca para o repositório).\n4. Certificado digital A1 (e-CNPJ ou NF-e, `.pfx`) da empresa, cadastrado em Configurações > Fiscal.\n"
    ],
    [
      "Seção do modo de emissão",
      "## Cancelamento, reenvio e produção (C3)\n",
      "## Escolha do modo de emissão (E0)\n\nEm Configurações > Fiscal o administrador escolhe **como** a nota é emitida: **Nenhum** (desligado, o padrão), **SEFAZ direto** (certificado digital A1, sem intermediário) ou, só em desenvolvimento, o **provedor de teste**. Um provedor pago entra depois como uma terceira opção, sem mexer no resto. A escolha vale para o sistema todo e fica no banco (coluna `fiscal_settings.mode`).\n\n- **Hoje o modo direto ainda não emite:** a tela deixa cadastrar o certificado e o CSC, e o servidor confere e guarda, mas nenhuma nota é emitida (o aviso aparece sob o seletor). Enquanto isso o modo direto também **não passa na trava de produção**.\n- **Cofre de segredos:** certificado (`.pfx` + senha) e CSC ficam **cifrados no banco** (AES-256-GCM), cada um preso ao seu tipo. Nada secreto volta para o navegador: a tela só vê titular, CNPJ, vencimento e o ID do CSC. A chave mestra é a variável `FISCAL_SECRET_KEY` (32 bytes em base64; o comando para gerar está em `backend/.env.example`). **No Coolify, crie a variável `FISCAL_SECRET_KEY` antes de cadastrar o certificado** (o `docker-compose.yaml` só repassa as variáveis listadas nele). Sem ela a tela avisa e recusa. Perder a chave obriga a cadastrar de novo o certificado e o CSC; trocá-la, idem.\n- **Conferências ao enviar o certificado:** senha correta, arquivo de até 1 MB, ainda dentro da validade e, quando a empresa já tem CNPJ salvo, **o mesmo CNPJ** (o CNPJ vem do certificado ICP-Brasil). A tela pinta o vencimento: aviso a 60 dias e vermelho a 15 dias.\n- **CSC:** um ID (só dígitos) e o código, um par para homologação e outro para produção, gerados no portal da SEFAZ-PE.\n- **Dados da reforma tributária:** cadastro de produto, tabela fiscal por categoria e itens do pedido ganharam **CST do IBS/CBS, cClassTrib e alíquotas de IBS e CBS**. Os valores vêm do contador (regime normal). Ainda não são exigidos para emitir e nada os usa até a etapa de emissão direta.\n- **Trava de produção:** exige um modo real (o direto), certificado da empresa dentro da validade, CSC de produção, venda de teste autorizada em homologação e a confirmação do contador.\n- **Não usar certificado real em testes:** os testes geram certificados descartáveis. O certificado verdadeiro só entra pela tela, no servidor.\n\n## Cancelamento, reenvio e produção (C3)\n"
    ]
  ]
}

for rel, edits in EDITS.items():
    path = Path(rel.split("::")[0])
    text = path.read_text(encoding="utf-8", newline="")
    crlf = "\r\n" in text
    text = text.replace("\r\n", "\n")
    for label, old, new in edits:
        count = text.count(old)
        if count != 1:
            sys.exit(f"{rel}: '{label}' casou {count} vezes (esperado 1)")
        text = text.replace(old, new)
    path.write_text(text.replace("\n", "\r\n") if crlf else text, encoding="utf-8", newline="")
    print(f"ok {rel}")
```

```bash
python apply_edits.py
```
Expected: `ok` para os três arquivos.

- [ ] **Step 2: Suítes completas, build e app**

```bash
cd backend && python -m pytest -q; cd ..
cd frontend && npx vitest run && npx tsc -b && npx vite build; cd ..
git status --short
```
Expected: backend `261 passed`; frontend `175 passed`, `tsc` limpo, `built in`; `git status` só com os três arquivos deste passo por commitar (e, no máximo, a alteração do usuário no `NewOrder.tsx` e `.impeccable/config.json`).

- [ ] **Step 3: Commit**

```bash
git add docker-compose.yaml backend/.env.example docs/fiscal/README.md
git commit -m "docs(fiscal): chave mestra do cofre (FISCAL_SECRET_KEY) e o que o E0 faz"
```

---

### Task 8: Achados da revisão final

Uma revisão independente (contexto limpo) da Task 1 à Task 7 não achou nada crítico; estes são os achados verificados, todos corrigidos aqui.

**Files:**
- Modify: `backend/app/services/fiscal/vault.py`, `backend/app/services/fiscal/certificate.py`, `backend/app/services/fiscal_secrets.py`, `backend/app/services/fiscal_golive.py`, `backend/tests/test_fiscal_vault.py`, `backend/tests/test_fiscal_certificate.py`, `backend/tests/test_fiscal_secrets.py`, `backend/tests/test_fiscal_golive.py`, `frontend/src/utils/fiscal.ts`, `frontend/src/utils/fiscal.test.ts`, `frontend/src/pages/settings/FiscalTab.tsx`, `frontend/nginx.conf`, `docs/fiscal/README.md`

**O que muda:**
1. **Trava de produção (médio, latente até a E2):** ela só olhava os dados públicos do cadastro. Agora exige o cofre aberto (`vault_available()`), **decifra** o certificado e abre o `.pfx` com a senha guardada (`inspect_pfx`), confere validade, `not_before` e CNPJ (certificado **sem CNPJ** deixa de passar) e decifra o CSC de produção; segredo que não decifra vira pendência com mensagem em português.
2. **Modo "Nenhum" e nota antiga (médio, latente):** o provedor sai do modo salvo, então com o modo em Nenhum um pedido com NFC-e autorizada não pode ser cancelado nem consultado. Corrigir de vez exige gravar no pedido o modo que emitiu a nota, o que só faz sentido com o adaptador real (E2); por ora o aviso do seletor e o `docs/fiscal/README.md` dizem isso com todas as letras.
3. **Ids repetidos nos campos do CSC (baixo/médio):** os blocos de homologação e produção geravam os mesmos `id`; o rótulo de um focava o campo do outro. Agora `csc-<ambiente>-id` e `csc-<ambiente>-token`.
4. **Mensagens do certificado:** arquivo que não é `.pfx` (não começa por SEQUENCE 0x30) e senha errada/arquivo corrompido passam a dar mensagens diferentes.
5. **Chave mestra estrita (baixo):** só base64 (padrão ou urlsafe) **com** o preenchimento; uma frase de 43 caracteres deixa de virar chave de 32 bytes.
6. **Corpo multipart (baixo):** `client_max_body_size 2m` explícito no `/api` do nginx, e a tela confere `1 MB` antes de enviar (com a mensagem em português, sem chamar a API).
7. **Corrida no primeiro envio (baixo):** dois envios ao mesmo tempo do primeiro segredo davam 500 (`IntegrityError`); agora a perdedora recebe 409 em português.
8. **Telas (baixo):** a faixa "sem provedor" só aparece quando o modo não é Nenhum e com texto do modo; o modal de produção cita certificado e CSC; "Remover" pede confirmação e tem `aria-label` com contexto; erro na leitura do cofre aparece na tela em vez de sumir.

- [ ] **Step 1: Aplicar as correções**

Mesmo procedimento de edição (salve como `apply_edits.py` fora do repositório e rode da raiz). Primeiro o backend, com os testes novos (chave estrita, mensagens do certificado, corrida no envio e a trava que decifra o cofre):

```python
import sys
from pathlib import Path

EDITS = {
  "backend/app/services/fiscal/vault.py::review": [
    [
      "Alteração 1",
      "        raise VaultUnavailable(f\"A chave mestra dos segredos fiscais (FISCAL_SECRET_KEY) não está configurada no servidor: {HOW_TO_CREATE_KEY}.\")\n    try:\n",
      "        raise VaultUnavailable(f\"A chave mestra dos segredos fiscais (FISCAL_SECRET_KEY) não está configurada no servidor: {HOW_TO_CREATE_KEY}.\")\n    try:  # estrito: só o alfabeto do base64 e com o preenchimento exato, para uma frase escolhida à mão não virar chave\n"
    ],
    [
      "Alteração 2",
      "            key = base64.urlsafe_b64decode(raw + \"=\" * (-len(raw) % 4))\n",
      "            key = base64.b64decode(raw, altchars=b\"-_\", validate=True)  # a variante urlsafe, também com o preenchimento\n"
    ],
    [
      "Alteração 3",
      "            raise VaultUnavailable(\"FISCAL_SECRET_KEY não é um base64 válido.\") from None\n",
      "            raise VaultUnavailable(f\"FISCAL_SECRET_KEY não é um base64 válido: {HOW_TO_CREATE_KEY}.\") from None\n"
    ]
  ],
  "backend/app/services/fiscal/certificate.py::review": [
    [
      "Alteração 1",
      "    \"\"\"Abre o .pfx e devolve os dados públicos do certificado. Levanta CertificateError com mensagem em português.\"\"\"\n",
      "    \"\"\"Abre o .pfx e devolve os dados públicos do certificado. Levanta CertificateError com mensagem em português.\"\"\"\n    if data[:1] != b\"\\x30\":  # todo .pfx (PKCS#12) é uma estrutura DER que começa por SEQUENCE (0x30)\n        raise CertificateError(\"O arquivo enviado não é um certificado .pfx (A1).\")\n"
    ],
    [
      "Alteração 2",
      "        raise CertificateError(\"Não foi possível abrir o certificado: confira a senha e se o arquivo é um .pfx (A1).\") from None\n",
      "        raise CertificateError(\"Não foi possível abrir o certificado: a senha está incorreta ou o arquivo está corrompido.\") from None\n"
    ]
  ],
  "backend/app/services/fiscal_secrets.py::review": [
    [
      "Alteração 1",
      "from sqlalchemy import select\n",
      "from sqlalchemy import select\nfrom sqlalchemy.exc import IntegrityError\n"
    ],
    [
      "Alteração 2",
      "\nasync def save_certificate(db: AsyncSession, pfx: bytes, password: str, company_cnpj: str | None, user_name: str) -> cert.CertInfo:\n",
      "\nasync def _commit_or_409(db: AsyncSession) -> None:\n    \"\"\"Dois envios ao mesmo tempo do primeiro segredo disputam a linha única do tipo: quem perde recebe um aviso, não um erro 500.\"\"\"\n    try:\n        await db.commit()\n    except IntegrityError:\n        await db.rollback()\n        raise HTTPException(status_code=409, detail=\"Outro envio dos mesmos dados aconteceu ao mesmo tempo. Confira a tela e envie de novo.\") from None\n\n\nasync def save_certificate(db: AsyncSession, pfx: bytes, password: str, company_cnpj: str | None, user_name: str) -> cert.CertInfo:\n"
    ],
    [
      "Alteração 3",
      "    await _upsert(db, KIND_CERTIFICATE_PASSWORD, password.encode(\"utf-8\"), None, user_name)\n    await db.commit()\n",
      "    await _upsert(db, KIND_CERTIFICATE_PASSWORD, password.encode(\"utf-8\"), None, user_name)\n    await _commit_or_409(db)\n"
    ],
    [
      "Alteração 4",
      "    await _upsert(db, kind, json.dumps({\"id\": csc_id, \"token\": token}).encode(\"utf-8\"), {\"id\": csc_id}, user_name)\n    await db.commit()\n",
      "    await _upsert(db, kind, json.dumps({\"id\": csc_id, \"token\": token}).encode(\"utf-8\"), {\"id\": csc_id}, user_name)\n    await _commit_or_409(db)\n"
    ]
  ],
  "backend/app/services/fiscal_golive.py::review": [
    [
      "Alteração 1",
      "from app.services import fiscal_secrets\n",
      "from app.services import fiscal_secrets\nfrom app.services.fiscal import certificate, vault\n"
    ],
    [
      "Alteração 2",
      "from app.services.fiscal.registry import REAL_MODES, mode_available\n",
      "from app.services.fiscal.registry import REAL_MODES, mode_available\n\n\nasync def _direct_mode_problems(db: AsyncSession, data: Any) -> list[str]:\n    \"\"\"Modo direto: não basta o cadastro existir (a tela só vê os dados públicos); o cofre precisa abrir e o certificado precisa servir.\"\"\"\n    if not vault.vault_available():\n        return [\"a chave mestra dos segredos (FISCAL_SECRET_KEY) não está configurada no servidor: sem ela o certificado e o CSC não abrem\"]\n    problems: list[str] = []\n    try:\n        loaded = await fiscal_secrets.load_certificate(db)\n    except vault.VaultUnavailable:\n        problems.append(\"o certificado digital guardado não abre (a chave mestra mudou?): cadastre-o de novo\")\n    else:\n        if loaded is None:\n            problems.append(\"cadastre o certificado digital A1\")\n        else:\n            try:\n                info = certificate.inspect_pfx(*loaded)\n            except certificate.CertificateError as exc:\n                problems.append(f\"o certificado digital guardado não serve: {exc}\")\n            else:\n                company_cnpj = re.sub(r\"\\D\", \"\", data.cnpj or \"\")\n                if info.cnpj is None:\n                    problems.append(\"o certificado digital não traz o CNPJ da empresa: use um e-CNPJ A1\")\n                problems += certificate.problems_for_company(info, company_cnpj if info.cnpj is not None else None)\n    try:\n        csc = await fiscal_secrets.load_csc(db, \"producao\")\n    except vault.VaultUnavailable:\n        problems.append(\"o CSC de produção guardado não abre (a chave mestra mudou?): cadastre-o de novo\")\n    else:\n        if csc is None:\n            problems.append(\"cadastre o CSC de produção\")\n    return problems\n"
    ],
    [
      "Alteração 3",
      "        status = await fiscal_secrets.secrets_status(db)\n        certificate = status[\"certificate\"]\n        if not certificate[\"configured\"]:\n            problems.append(\"cadastre o certificado digital A1\")\n        else:\n            if certificate[\"days_left\"] < 0:\n                problems.append(\"o certificado digital venceu\")\n            company_cnpj = re.sub(r\"\\D\", \"\", data.cnpj or \"\")\n            if certificate[\"cnpj\"] and company_cnpj and certificate[\"cnpj\"] != company_cnpj:\n                problems.append(\"o CNPJ do certificado é diferente do CNPJ da empresa\")\n        if not status[\"csc_prod\"][\"configured\"]:\n            problems.append(\"cadastre o CSC de produção\")\n",
      "        problems += await _direct_mode_problems(db, data)\n"
    ]
  ],
  "backend/tests/test_fiscal_vault.py::review": [
    [
      "Alteração 1",
      "    def test_aceita_base64_padrao_e_urlsafe(self, monkeypatch):\n",
      "    def test_aceita_base64_padrao_e_urlsafe_com_preenchimento(self, monkeypatch):\n"
    ],
    [
      "Alteração 2",
      "        for text in (base64.b64encode(raw).decode(), base64.urlsafe_b64encode(raw).decode().rstrip(\"=\")):\n",
      "        for text in (base64.b64encode(raw).decode(), base64.urlsafe_b64encode(raw).decode()):\n"
    ],
    [
      "Alteração 3",
      "            assert vault.decrypt(\"csc\", vault.encrypt(\"csc\", b\"ok\")) == b\"ok\"\n",
      "            assert vault.decrypt(\"csc\", vault.encrypt(\"csc\", b\"ok\")) == b\"ok\"\n\n    def test_frase_escolhida_a_mao_nao_vira_chave(self, monkeypatch):\n        # 43 caracteres do alfabeto urlsafe, sem o preenchimento: antes decodificava para 32 bytes de baixa entropia\n        for weak in (\"minha-senha-do-cofre-que-eu-inventei-agora1\", \"a\" * 43, \"a\" * 44):\n            monkeypatch.setattr(vault.app_settings, \"FISCAL_SECRET_KEY\", weak)\n            assert vault.vault_available() is False, weak\n"
    ]
  ],
  "backend/tests/test_fiscal_certificate.py::review": [
    [
      "Alteração 1",
      "    def test_senha_errada_ou_arquivo_que_nao_e_pfx_da_mensagem_em_portugues(self):\n        for data, password in ((make_pfx(), \"errada\"), (b\"isto nao e um pfx\", PASSWORD), (b\"\", PASSWORD)):\n            with pytest.raises(cert.CertificateError, match=\"confira a senha\"):\n                cert.inspect_pfx(data, password)\n",
      "    def test_senha_errada_diz_que_a_senha_esta_incorreta(self):\n        with pytest.raises(cert.CertificateError, match=\"senha está incorreta\"):\n            cert.inspect_pfx(make_pfx(), \"errada\")\n\n    def test_arquivo_que_nao_e_pfx_diz_que_nao_e_um_certificado(self):\n        for data in (b\"isto nao e um pfx\", b\"\", b\"\\x00\\x01\\x02\"):\n            with pytest.raises(cert.CertificateError, match=\"não é um certificado\"):\n                cert.inspect_pfx(data, PASSWORD)\n\n    def test_pfx_corrompido_nao_e_confundido_com_senha_errada(self):\n        broken = make_pfx()[:40]  # começa como um .pfx, mas está cortado\n        with pytest.raises(cert.CertificateError, match=\"incorreta ou o arquivo está corrompido\"):\n            cert.inspect_pfx(broken, PASSWORD)\n"
    ]
  ],
  "backend/tests/test_fiscal_secrets.py::review": [
    [
      "Alteração 1",
      "from fastapi import HTTPException  # noqa: E402\n",
      "from fastapi import HTTPException  # noqa: E402\nfrom sqlalchemy.exc import IntegrityError  # noqa: E402\n"
    ],
    [
      "Alteração 2",
      "        pytest.param(\"valido\", \"errada\", CNPJ, \"confira a senha\", id=\"senha-errada\"),\n",
      "        pytest.param(\"valido\", \"errada\", CNPJ, \"senha está incorreta\", id=\"senha-errada\"),\n        pytest.param(b\"isto nao e um pfx\", PASSWORD, CNPJ, \"não é um certificado\", id=\"arquivo-que-nao-e-pfx\"),\n"
    ],
    [
      "Alteração 3",
      "        assert info.value.status_code == 409 and \"FISCAL_SECRET_KEY\" in info.value.detail and db.store == []\n",
      "        assert info.value.status_code == 409 and \"FISCAL_SECRET_KEY\" in info.value.detail and db.store == []\n\n    def test_dois_envios_ao_mesmo_tempo_perdem_a_corrida_com_aviso_em_portugues(self):\n        class RacingDb(MemoryDb):\n            rolled_back = False\n\n            async def commit(self):\n                raise IntegrityError(\"INSERT\", {}, Exception(\"duplicate key value violates unique constraint\"))\n\n            async def rollback(self):\n                self.rolled_back = True\n\n        db = RacingDb()\n        with pytest.raises(HTTPException) as info:\n            run(secrets.save_certificate(db, make_pfx(), PASSWORD, CNPJ, \"Maria\"))\n        assert info.value.status_code == 409 and \"ao mesmo tempo\" in info.value.detail and db.rolled_back\n        assert \"duplicate\" not in info.value.detail  # a mensagem do banco (em inglês) não vaza\n        with pytest.raises(HTTPException) as again:\n            run(secrets.save_csc(RacingDb(), \"producao\", \"1\", CSC_TOKEN, \"Maria\"))\n        assert again.value.status_code == 409\n"
    ],
    [
      "Alteração 4",
      "        assert response.status_code == 422 and \"confira a senha\" in response.json()[\"detail\"]\n",
      "        assert response.status_code == 422 and \"senha está incorreta\" in response.json()[\"detail\"]\n"
    ]
  ],
  "backend/tests/test_fiscal_golive.py::review": [
    [
      "Alteração 1",
      "from datetime import datetime, timedelta, timezone\n\n",
      ""
    ],
    [
      "Alteração 2",
      "from app.models.fiscal import FiscalSecret, FiscalSettings  # noqa: E402\n",
      "from app.models.fiscal import FiscalSettings  # noqa: E402\n"
    ],
    [
      "Alteração 3",
      "from app.services.fiscal import vault  # noqa: E402\n",
      "from app.services.fiscal import vault  # noqa: E402\nfrom tests.fiscal_certs import PASSWORD, make_pfx  # noqa: E402\n"
    ],
    [
      "Alteração 4",
      "NOW = datetime.now(timezone.utc)\n",
      ""
    ],
    [
      "Alteração 5",
      "def certificate_row(days_left=300, cnpj=\"11222333000181\"):\n    not_after = NOW + timedelta(days=days_left)\n    return FiscalSecret(kind=\"certificate\", ciphertext=b\"x\", meta={\n        \"subject\": \"EMPRESA TESTE LTDA:\" + (cnpj or \"\"), \"cnpj\": cnpj, \"not_before\": (NOW - timedelta(days=60)).isoformat(),\n        \"not_after\": not_after.isoformat(), \"fingerprint\": \"ab\" * 32})\n\n\ndef csc_prod_row():\n    return FiscalSecret(kind=\"csc_prod\", ciphertext=b\"x\", meta={\"id\": \"1\"})\n",
      "PFX_OK = make_pfx()\n"
    ],
    [
      "Alteração 6",
      "    \"\"\"Responde por tipo de consulta: contagem de vendas de teste, segredos do cofre ou a linha de configuração.\"\"\"\n",
      "    \"\"\"Responde por tipo de consulta: contagem de vendas de teste ou a linha de configuração (o cofre é trocado por `secrets`).\"\"\"\n"
    ],
    [
      "Alteração 7",
      "    def __init__(self, hml_authorized=1, secret_rows=(), settings=None):\n",
      "    def __init__(self, hml_authorized=1, settings=None):\n"
    ],
    [
      "Alteração 8",
      "        self.secret_rows = list(secret_rows)\n",
      ""
    ],
    [
      "Alteração 9",
      "            self.current, self.rows = self.hml_authorized, []\n",
      "            self.current = self.hml_authorized\n"
    ],
    [
      "Alteração 10",
      "        elif \"fiscal_secrets\" in text:\n            self.current, self.rows = None, list(self.secret_rows)\n",
      ""
    ],
    [
      "Alteração 11",
      "            self.current, self.rows = self.settings, [self.settings] if self.settings else []\n",
      "            self.current = self.settings\n"
    ],
    [
      "Alteração 12",
      "\n    def scalars(self):\n        return self\n\n    def all(self):\n        return list(self.rows)\n",
      ""
    ],
    [
      "Alteração 13",
      "\ndef data(**over):\n",
      "\ndef secrets(monkeypatch, pfx=PFX_OK, password=PASSWORD, csc=(\"1\", \"TOKEN-DO-CSC-1234567890\"), cert_error=None, csc_error=None):\n    \"\"\"O que o cofre entrega à trava (decifrado): o .pfx com a senha e o CSC de produção. `None` = não cadastrado.\"\"\"\n    async def load_certificate(_db):\n        if cert_error:\n            raise cert_error\n        return None if pfx is None else (pfx, password)\n\n    async def load_csc(_db, environment):\n        assert environment == \"producao\"\n        if csc_error:\n            raise csc_error\n        return csc\n\n    monkeypatch.setattr(golive.fiscal_secrets, \"load_certificate\", load_certificate)\n    monkeypatch.setattr(golive.fiscal_secrets, \"load_csc\", load_csc)\n\n\ndef data(**over):\n"
    ],
    [
      "Alteração 14",
      "    \"\"\"Simula o adaptador do modo direto já pronto (ele só chega na etapa E2).\"\"\"\n",
      "    \"\"\"Simula o adaptador do modo direto já pronto (ele só chega na etapa E2) e o cofre com certificado e CSC de produção.\"\"\"\n"
    ],
    [
      "Alteração 15",
      "    monkeypatch.setattr(golive, \"mode_available\", lambda mode: True)\n",
      "    monkeypatch.setattr(golive, \"mode_available\", lambda mode: True)\n    secrets(monkeypatch)\n"
    ],
    [
      "Alteração 16",
      "READY_ROWS = (certificate_row(), csc_prod_row())\n\n\n",
      ""
    ],
    [
      "Alteração 17",
      "        assert run(golive.go_live_problems(FakeDb(2, READY_ROWS), data())) == []\n",
      "        assert run(golive.go_live_problems(FakeDb(2), data())) == []\n"
    ],
    [
      "Alteração 18",
      "            problems = run(golive.go_live_problems(FakeDb(2, READY_ROWS), data(mode=mode)))\n",
      "            problems = run(golive.go_live_problems(FakeDb(2), data(mode=mode)))\n"
    ],
    [
      "Alteração 19",
      "        problems = run(golive.go_live_problems(FakeDb(2, READY_ROWS), data()))  # de verdade: o adaptador chega na E2\n",
      "        problems = run(golive.go_live_problems(FakeDb(2), data()))  # de verdade: o adaptador chega na E2\n"
    ],
    [
      "Alteração 20",
      "    def test_exige_certificado_valido_da_empresa_e_csc_de_producao(self, adapter_ready):\n        assert run(golive.go_live_problems(FakeDb(2, [csc_prod_row()]), data())) == [\"cadastre o certificado digital A1\"]\n        assert run(golive.go_live_problems(FakeDb(2, [certificate_row(days_left=-3), csc_prod_row()]), data())) == [\"o certificado digital venceu\"]\n        assert run(golive.go_live_problems(FakeDb(2, [certificate_row(cnpj=\"99888777000166\"), csc_prod_row()]), data())) == [\n            \"o CNPJ do certificado é diferente do CNPJ da empresa\"]\n        assert run(golive.go_live_problems(FakeDb(2, [certificate_row()]), data())) == [\"cadastre o CSC de produção\"]\n",
      "    def test_exige_certificado_cadastrado_e_csc_de_producao(self, adapter_ready, monkeypatch):\n        secrets(monkeypatch, pfx=None)\n        assert run(golive.go_live_problems(FakeDb(2), data())) == [\"cadastre o certificado digital A1\"]\n        secrets(monkeypatch, csc=None)\n        assert run(golive.go_live_problems(FakeDb(2), data())) == [\"cadastre o CSC de produção\"]\n\n    def test_certificado_vencido_ou_ainda_nao_valido_ou_de_outro_cnpj_nao_serve(self, adapter_ready, monkeypatch):\n        secrets(monkeypatch, pfx=make_pfx(valid_from_days=-400, valid_for_days=365))\n        assert any(\"venceu\" in p for p in run(golive.go_live_problems(FakeDb(2), data())))\n        secrets(monkeypatch, pfx=make_pfx(valid_from_days=10, valid_for_days=365))\n        assert any(\"só vale a partir de\" in p for p in run(golive.go_live_problems(FakeDb(2), data())))\n        secrets(monkeypatch, pfx=PFX_OK)\n        assert any(\"diferente\" in p for p in run(golive.go_live_problems(FakeDb(2), data(cnpj=\"99888777000166\"))))\n\n    def test_certificado_sem_cnpj_nao_serve_para_producao(self, adapter_ready, monkeypatch):\n        secrets(monkeypatch, pfx=make_pfx(cnpj_in_cn=None))\n        problems = run(golive.go_live_problems(FakeDb(2), data()))\n        assert problems == [\"o certificado digital não traz o CNPJ da empresa: use um e-CNPJ A1\"]\n\n    def test_senha_guardada_que_nao_abre_o_pfx_e_pendencia(self, adapter_ready, monkeypatch):\n        secrets(monkeypatch, password=\"outra-senha\")\n        problems = run(golive.go_live_problems(FakeDb(2), data()))\n        assert len(problems) == 1 and \"não serve\" in problems[0] and \"incorreta\" in problems[0]\n\n    def test_sem_chave_mestra_o_cofre_nao_abre_e_a_trava_nao_libera(self, adapter_ready, monkeypatch):\n        monkeypatch.setattr(vault.app_settings, \"FISCAL_SECRET_KEY\", \"\")\n        problems = run(golive.go_live_problems(FakeDb(2), data()))\n        assert len(problems) == 1 and \"FISCAL_SECRET_KEY\" in problems[0]\n\n    def test_segredo_que_nao_decifra_mais_e_pendencia(self, adapter_ready, monkeypatch):\n        broken = vault.VaultUnavailable(\"mudou\")\n        secrets(monkeypatch, cert_error=broken, csc_error=broken)\n        problems = run(golive.go_live_problems(FakeDb(2), data()))\n        assert problems == [\"o certificado digital guardado não abre (a chave mestra mudou?): cadastre-o de novo\",\n                            \"o CSC de produção guardado não abre (a chave mestra mudou?): cadastre-o de novo\"]\n"
    ],
    [
      "Alteração 21",
      "        assert run(golive.go_live_problems(FakeDb(0, READY_ROWS), data())) == [\"faça ao menos uma venda de teste autorizada em homologação\"]\n",
      "        assert run(golive.go_live_problems(FakeDb(0), data())) == [\"faça ao menos uma venda de teste autorizada em homologação\"]\n"
    ],
    [
      "Alteração 22",
      "        db = FakeDb(1, READY_ROWS)\n",
      "        db = FakeDb(1)\n"
    ],
    [
      "Alteração 23",
      "        assert run(golive.go_live_problems(FakeDb(1, READY_ROWS), data(production_confirmation=False))) == [\n",
      "        assert run(golive.go_live_problems(FakeDb(1), data(production_confirmation=False))) == [\n"
    ],
    [
      "Alteração 24",
      "        problems = run(golive.go_live_problems(FakeDb(1, READY_ROWS), FiscalSettingsIn(\n",
      "        problems = run(golive.go_live_problems(FakeDb(1), FiscalSettingsIn(\n"
    ],
    [
      "Alteração 25",
      "        db = FakeDb(1, READY_ROWS, settings=r)\n",
      "        db = FakeDb(1, settings=r)\n"
    ]
  ]
}

for rel, edits in EDITS.items():
    path = Path(rel.split("::")[0])
    text = path.read_text(encoding="utf-8", newline="")
    crlf = "\r\n" in text
    text = text.replace("\r\n", "\n")
    for label, old, new in edits:
        count = text.count(old)
        if count != 1:
            sys.exit(f"{rel}: '{label}' casou {count} vezes (esperado 1)")
        text = text.replace(old, new)
    path.write_text(text.replace("\n", "\r\n") if crlf else text, encoding="utf-8", newline="")
    print(f"ok {rel}")
```

```bash
python apply_edits.py
cd backend && python -m pytest -q; cd ..
```
Expected: `ok` para os oito arquivos e `271 passed` (261 + 10).

Depois o frontend, o nginx e o README:

```python
import sys
from pathlib import Path

EDITS = {
  "frontend/src/utils/fiscal.ts::review": [
    [
      "Alteração 1",
      "  if (mode === 'none') return 'Com a emissão desligada, nenhuma NFC-e é emitida e o PDV funciona como sempre.'\n",
      "  if (mode === 'none') {\n    return 'Com a emissão desligada, nenhuma NFC-e nova é emitida e o PDV funciona como sempre. Atenção: um pedido que já tem NFC-e autorizada só pode ser cancelado com o modo que a emitiu ativo.'\n  }\n"
    ],
    [
      "Alteração 2",
      "  if (mode === 'fake') return 'Modo de teste: as notas são simuladas e não existem na SEFAZ.'\n",
      "  if (mode === 'fake') return 'Modo de teste: as notas são simuladas e não existem na SEFAZ.'\n  return null\n}\n\n/** O servidor recusa .pfx acima de 1 MB (e o nginx corta antes, sem mensagem útil): a tela avisa antes de enviar. */\nexport const MAX_CERTIFICATE_BYTES = 1_000_000\n\nexport function certificateFileProblem(file: { size: number } | null): string | null {\n  if (!file) return null\n  if (file.size === 0) return 'O arquivo está vazio.'\n  if (file.size > MAX_CERTIFICATE_BYTES) return 'O arquivo passa de 1 MB: confira se é o certificado A1 (.pfx), e não outro arquivo.'\n"
    ]
  ],
  "frontend/src/utils/fiscal.test.ts::review": [
    [
      "Alteração 1",
      "  cancelReasonError,\n",
      "  cancelReasonError,\n  certificateFileProblem,\n"
    ],
    [
      "Alteração 2",
      "    expect(modeNotice('none', false)).toMatch(/desligada/)\n",
      "    expect(modeNotice('none', false)).toMatch(/desligada/)\n    expect(modeNotice('none', false)).toMatch(/já tem NFC-e autorizada/) // desligar não permite cancelar nota de outro modo\n"
    ],
    [
      "Alteração 3",
      "\n  it('vencido pede um novo; sem certificado não há resumo', () => {\n",
      "\n  it('o arquivo do certificado é conferido antes de enviar (vazio ou acima de 1 MB)', () => {\n    expect(certificateFileProblem(null)).toBeNull()\n    expect(certificateFileProblem({ size: 4_000 })).toBeNull()\n    expect(certificateFileProblem({ size: 1_000_000 })).toBeNull()\n    expect(certificateFileProblem({ size: 0 })).toMatch(/vazio/)\n    expect(certificateFileProblem({ size: 1_000_001 })).toMatch(/passa de 1 MB/)\n  })\n\n  it('vencido pede um novo; sem certificado não há resumo', () => {\n"
    ]
  ],
  "frontend/src/pages/settings/FiscalTab.tsx::review": [
    [
      "Alteração 1",
      "import {\n  certificateSummary,\n",
      "import {\n  certificateFileProblem,\n  certificateSummary,\n"
    ],
    [
      "Alteração 2",
      "          libera se: o servidor tiver um provedor fiscal real configurado, houver ao menos uma venda de teste autorizada em\n          homologação e a emissão estiver ligada com todos os dados da empresa. Se algo faltar, ele diz o quê.\n",
      "          libera se: o modo de emissão for real (SEFAZ direto) e estiver pronto, com o certificado digital da empresa dentro da\n          validade e o CSC de produção cadastrados, houver ao menos uma venda de teste autorizada em homologação e a emissão\n          estiver ligada com todos os dados da empresa. Se algo faltar, ele diz o quê.\n"
    ],
    [
      "Alteração 3",
      "          <button className=\"text-sm text-red-600 hover:underline dark:text-red-400\" disabled={remove.isPending} onClick={() => remove.mutate()}>\n",
      "          <button\n            className=\"text-sm text-red-600 hover:underline dark:text-red-400\"\n            aria-label={`Remover ${label}`}\n            disabled={remove.isPending}\n            onClick={() => window.confirm(`Remover o ${label}? A emissão nesse ambiente para de funcionar até cadastrar de novo.`) && remove.mutate()}\n          >\n"
    ],
    [
      "Alteração 4",
      "        <Input label=\"ID do CSC\" placeholder=\"Ex.: 1\" inputMode=\"numeric\" value={id} onChange={(e) => setId(e.target.value)} />\n        <Input label=\"Código do CSC\" type=\"password\" autoComplete=\"new-password\" value={token} onChange={(e) => setToken(e.target.value)} />\n",
      "        <Input id={`csc-${environment}-id`} label=\"ID do CSC\" placeholder=\"Ex.: 1\" inputMode=\"numeric\" value={id} onChange={(e) => setId(e.target.value)} />\n        <Input\n          id={`csc-${environment}-token`}\n          label=\"Código do CSC\"\n          type=\"password\"\n          autoComplete=\"new-password\"\n          value={token}\n          onChange={(e) => setToken(e.target.value)}\n        />\n"
    ],
    [
      "Alteração 5",
      "  const { data: secrets } = useQuery({ queryKey: ['fiscal', 'secrets'], queryFn: getFiscalSecrets })\n",
      "  const { data: secrets, isError } = useQuery({ queryKey: ['fiscal', 'secrets'], queryFn: getFiscalSecrets })\n"
    ],
    [
      "Alteração 6",
      "\n  if (!secrets) return null\n",
      "\n  if (isError) {\n    return (\n      <div role=\"alert\" className={`${cardClass} text-sm text-red-700 dark:text-red-400`}>\n        Não foi possível ler o que está cadastrado no cofre de segredos. Recarregue a página; se continuar, veja o servidor.\n      </div>\n    )\n  }\n  if (!secrets) return null\n"
    ],
    [
      "Alteração 7",
      "  if (!secrets) return null\n",
      "  if (!secrets) return null\n  const fileProblem = certificateFileProblem(file)\n"
    ],
    [
      "Alteração 8",
      "          <button className=\"text-sm text-red-600 hover:underline dark:text-red-400\" disabled={removeCertificate.isPending} onClick={() => removeCertificate.mutate()}>\n",
      "          <button\n            className=\"text-sm text-red-600 hover:underline dark:text-red-400\"\n            aria-label=\"Remover certificado digital\"\n            disabled={removeCertificate.isPending}\n            onClick={() =>\n              window.confirm('Remover o certificado digital? A emissão pelo modo direto para de funcionar até cadastrar um novo.') && removeCertificate.mutate()\n            }\n          >\n"
    ],
    [
      "Alteração 9",
      "          <Button size=\"sm\" variant=\"secondary\" disabled={locked || !file || !password} loading={upload.isPending} onClick={() => upload.mutate()}>\n",
      "          <Button size=\"sm\" variant=\"secondary\" disabled={locked || !file || !password || !!fileProblem} loading={upload.isPending} onClick={() => upload.mutate()}>\n"
    ],
    [
      "Alteração 10",
      "        </div>\n      </div>\n\n",
      "        </div>\n      </div>\n\n      {fileProblem && (\n        <p role=\"alert\" className=\"mt-2 text-sm text-red-700 dark:text-red-400\">\n          {fileProblem}\n        </p>\n      )}\n\n"
    ],
    [
      "Alteração 11",
      "      {status && !status.provider_configured && (\n",
      "      {status && !status.provider_configured && status.mode !== 'none' && (\n"
    ],
    [
      "Alteração 12",
      "          Este servidor ainda não tem provedor fiscal configurado. Mesmo com a emissão ligada aqui, nenhuma nota é emitida.\n",
      "          O modo de emissão escolhido ainda não emite notas neste servidor. Mesmo com a emissão ligada aqui, nenhuma nota é emitida.\n"
    ]
  ],
  "frontend/nginx.conf::review": [
    [
      "Alteração 1",
      "    listen 80;\n    root /usr/share/nginx/html;\n    index index.html;\n\n    location / {\n        try_files $uri $uri/ /index.html;\n    }\n\n    location /api {\n        proxy_pass http://api:8000;\n        proxy_set_header Host $host;\n        proxy_set_header X-Real-IP $remote_addr;\n        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;\n        proxy_set_header X-Forwarded-Proto $scheme;\n        proxy_redirect off;\n    }\n\n",
      "    listen 80;\n    root /usr/share/nginx/html;\n    index index.html;\n\n    location / {\n        try_files $uri $uri/ /index.html;\n    }\n\n    location /api {\n        client_max_body_size 2m;\n        proxy_pass http://api:8000;\n        proxy_set_header Host $host;\n        proxy_set_header X-Real-IP $remote_addr;\n        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;\n        proxy_set_header X-Forwarded-Proto $scheme;\n        proxy_redirect off;\n    }\n\n"
    ],
    [
      "Alteração 2",
      "    ssl_ciphers HIGH:!aNULL:!MD5;\n\n    root /usr/share/nginx/html;\n    index index.html;\n\n    location / {\n        try_files $uri $uri/ /index.html;\n    }\n\n    location /api {\n        proxy_pass http://api:8000;\n        proxy_set_header Host $host;\n        proxy_set_header X-Real-IP $remote_addr;\n        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;\n        proxy_set_header X-Forwarded-Proto $scheme;\n        proxy_redirect off;\n    }\n\n    gzip on;\n",
      "    ssl_ciphers HIGH:!aNULL:!MD5;\n\n    root /usr/share/nginx/html;\n    index index.html;\n\n    location / {\n        try_files $uri $uri/ /index.html;\n    }\n\n    location /api {\n        client_max_body_size 2m;\n        proxy_pass http://api:8000;\n        proxy_set_header Host $host;\n        proxy_set_header X-Real-IP $remote_addr;\n        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;\n        proxy_set_header X-Forwarded-Proto $scheme;\n        proxy_redirect off;\n    }\n\n    gzip on;\n"
    ]
  ],
  "docs/fiscal/README.md::review": [
    [
      "Alteração 1",
      "- **CSC:** um ID (só dígitos) e o código, um par para homologação e outro para produção, gerados no portal da SEFAZ-PE.\n",
      "- **CSC:** um ID (só dígitos) e o código, um par para homologação e outro para produção, gerados no portal da SEFAZ-PE.\n- **Desligar não cancela nota antiga:** o modo é lido a cada operação. Com o modo em **Nenhum**, um pedido que já tem NFC-e autorizada não pode ser cancelado nem ter a situação consultada (não há provedor ativo). Antes de trocar de modo, cancele o que ainda estiver no prazo. Quando houver um adaptador real (E2), cada pedido passará a guardar o modo que emitiu a nota e o cancelamento usará esse.\n"
    ]
  ]
}

for rel, edits in EDITS.items():
    path = Path(rel.split("::")[0])
    text = path.read_text(encoding="utf-8", newline="")
    crlf = "\r\n" in text
    text = text.replace("\r\n", "\n")
    for label, old, new in edits:
        count = text.count(old)
        if count != 1:
            sys.exit(f"{rel}: '{label}' casou {count} vezes (esperado 1)")
        text = text.replace(old, new)
    path.write_text(text.replace("\n", "\r\n") if crlf else text, encoding="utf-8", newline="")
    print(f"ok {rel}")
```

```bash
python apply_edits.py
cd frontend && npx tsc -b && npx vitest run && npx vite build; cd ..
```
Expected: `ok` para os cinco arquivos; `tsc` sem saída; `176 passed` (175 + 1); `vite build` termina com `built in`.

- [ ] **Step 2: Verificar no navegador (API simulada)**

Mesma simulação da Task 6, com o modo `sefaz_direto` salvo, `provider_configured:false` no status e `GET /fiscal/secrets` devolvendo certificado e CSC de homologação cadastrados. Conferir: (1) os campos do CSC têm `id` `csc-homologacao-id`, `csc-homologacao-token`, `csc-producao-id`, `csc-producao-token`, sem repetição, e cada rótulo aponta para o seu; (2) escolher um arquivo de 1.000.001 bytes mostra "O arquivo passa de 1 MB…" e o botão do certificado fica desabilitado; (3) **Remover** o certificado abre a confirmação e, recusando, **não** chama `DELETE`; os botões têm `aria-label` ("Remover certificado digital", "Remover CSC de homologação"); (4) a faixa do topo diz "O modo de emissão escolhido ainda não emite notas neste servidor" e a frase antiga sobre provedor não aparece.

- [ ] **Step 3: Commit**

```bash
git add backend/app/services/fiscal/vault.py backend/app/services/fiscal/certificate.py backend/app/services/fiscal_secrets.py backend/app/services/fiscal_golive.py backend/tests/test_fiscal_vault.py backend/tests/test_fiscal_certificate.py backend/tests/test_fiscal_secrets.py backend/tests/test_fiscal_golive.py frontend/src/utils/fiscal.ts frontend/src/utils/fiscal.test.ts frontend/src/pages/settings/FiscalTab.tsx frontend/nginx.conf docs/fiscal/README.md
git commit -m "fix(fiscal): achados da revisão do E0 (trava de produção decifra o cofre, chave mestra estrita, mensagens do certificado, corrida no envio, telas)"
```

---

### Task 9: Entrega

**Files:** nenhum.

- [ ] **Step 1: Suítes completas**

```bash
cd backend && python -m pytest -q; cd ..
cd frontend && npx vitest run && npx tsc -b && npx vite build; cd ..
git status --short
```
Expected: backend `271 passed`; frontend `176 passed`, `tsc` limpo, `built in`; `git status` sem arquivos do E0 pendentes (só, no máximo, a alteração do usuário no `NewOrder.tsx` e `.impeccable/config.json`).

- [ ] **Step 2: Entregar ao usuário e parar**

Apresente o resumo: a base da escolha do modo de emissão está pronta (modo no banco, cofre cifrado de certificado e CSC, conferência do certificado, campos de IBS/CBS, trava de produção atualizada), verificada com bancos falsos e no navegador com a API simulada; o impacto (**nenhum** na operação: nada emite no modo direto ainda e a migration 014 só acrescenta); e o que **só o usuário destrava**: (1) criar a variável **`FISCAL_SECRET_KEY`** no Coolify (o comando para gerar está em `backend/.env.example`) **antes** de cadastrar o certificado, (2) o **certificado A1** e sua senha, cadastrados **pela tela** (nunca em conversa), (3) o **CSC de homologação** e o credenciamento na SEFAZ-PE, (4) os valores de **IBS/CBS** com o contador, (5) o prazo de cancelamento, (6) a **escolha do provedor**, que ainda pode ser adiada. Próximo passo: etapa **E1**, o teste de fogo (endpoints da SEFAZ-PE, QR-Code v3 e mapeamento do IBS/CBS). Lembre que a migration 014 ainda não foi rodada contra PostgreSQL real e que isso deve ser feito antes de qualquer deploy. Não faça deploy.
