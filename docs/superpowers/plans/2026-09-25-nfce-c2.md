# NFC-e (C2): cupom fiscal com QR Code na Epson — Plano de Implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Imprimir na Epson do caixa o cupom fiscal (DANFE NFC-e, com QR Code) de uma NFC-e **autorizada**, pela mesma camada de impressão da B3. Nos demais casos (fiado, dado fiscal faltando, emissão desligada) continua saindo o cupom não fiscal.

**Architecture:** O cupom continua sendo uma lista de blocos; ganha o bloco `qrcode`, desenhado como SVG dentro da própria página (sem rede, sem imagem externa). `buildFiscalReceipt` monta o DANFE NFC-e a partir do pedido e dos dados da empresa emitente; `buildDocumentReceipt` escolhe o documento certo (fiscal só com nota autorizada e dados da empresa). O backend passa a devolver a empresa emitente em `GET /fiscal/status` (dado público que sai em todo cupom), para o operador do caixa poder imprimir. Na tela "Pedido registrado" um painel único (`OrderFiscalPanel`) mostra o estado da NFC-e, **espera a nota assentar** antes da impressão automática e oferece o botão de imprimir.

**Tech Stack:** FastAPI/pytest (backend); React 18, TypeScript estrito, Vite 5.4, React Query 5, Vitest 2.1.x e **uma dependência nova**, `qrcode-generator` (QR Code em JavaScript puro, sem dependências, API síncrona).

**Spec:** `docs/superpowers/specs/2026-09-25-nfce-design.md` §7. **Bases já executadas:** B3 (impressora), C1a (backend fiscal) e C1b (telas). Fica para depois: **C3** (cancelamento da NFC-e, contingência, produção).

## Global Constraints

- **Nada muda para quem não usa fiscal:** com a emissão desligada (padrão) ou sem provedor, a impressão automática continua **imediata**, o botão continua "Imprimir cupom" e nenhum selo aparece. Isso é verificado no navegador (Task 4) e por teste (`decideAutoPrint`).
- **Só nota autorizada vira cupom fiscal.** Autorizada **sem** os dados da empresa cai no cupom não fiscal (nunca um cupom fiscal incompleto). Cupom fiscal nunca diz "não fiscal".
- **Homologação:** o cupom leva a marca obrigatória **"EMITIDA EM AMBIENTE DE HOMOLOGAÇÃO / SEM VALOR FISCAL"**.
- **Impressão automática só uma vez por pedido**, e o caixa **nunca espera**: os botões da tela ficam livres o tempo todo; se a nota não assentar depois das consultas, imprime o cupom não fiscal.
- **Sem segredos no navegador:** a empresa emitente (razão social, CNPJ, IE, endereço) já sai impressa em todo cupom, então `GET /fiscal/status` pode entregá-la a qualquer usuário logado. Token do provedor e CSC **nunca** passam por essa rota.
- **Uma dependência nova**, `qrcode-generator@^2.0.4` (Task 2), registrada em `package.json` e `package-lock.json`. Nenhuma outra.
- **Fora do cupom, por ora (pendências registradas em `docs/fiscal/README.md`):** os **tributos aproximados** (Lei 12.741, que precisa da tabela IBPT por NCM) e o **endereço de consulta da SEFAZ** impresso por extenso (depende do provedor real). O contador precisa validar o cupom de homologação antes de qualquer uso em produção.
- **Testes sem impressora:** `printHtml` não é chamado nos testes; o que se testa é a lista de blocos e o HTML gerado.
- **TypeScript estrito** (`noUnusedLocals`, `noUnusedParameters`): `npx tsc -b` sem erros ao fim de toda tarefa; a suíte inteira passa com a contagem prometida.
- **Nada de deploy** sem o "pode fazer o deploy" explícito do usuário.
- **Arquivos de outra pessoa na árvore:** `frontend/src/pages/orders/NewOrder.tsx` costuma ter uma **alteração não commitada do usuário** (estilo de hover) e `.impeccable/config.json` também. Nunca use `git add .` nem `git add -A`; na Task 4 o `NewOrder.tsx` é tratado com `git stash`.
- Commits terminam com a linha `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`.
- Ambiente: comandos para o Git Bash, na raiz do repositório; backend com o venv ativo (`cd backend && python -m pytest -q`); frontend em `frontend/`.
- **Cuidado nas verificações no navegador:** o painel **trava** no diálogo real de impressão se a impressão não estiver neutralizada, e o painel oculto **pausa** as consultas automáticas do React Query (roteiro na Task 4).

## Review Focus

1. **Documento errado:** cupom fiscal para nota não autorizada, ou cupom "não fiscal" para nota autorizada. → `nota_autorizada_mas_sem_os_dados_da_empresa...` e `qualquer_outro_estado_da_nota_sai_como_cupom_nao_fiscal` (Task 3).
2. **Impressão automática duplicada ou perdida:** deve imprimir **uma vez**, esperar a nota assentar e não travar se ela não assentar. → `decideAutoPrint` (Task 4) e o roteiro do navegador.
3. **Regressão do padrão** (emissão desligada): imprime na hora, botão "Imprimir cupom", sem selo. → `sem_emissao_fiscal_imprime_na_hora...` (Task 4) e o roteiro.
4. **QR Code ilegível ou grande demais para o papel:** nível de correção M, margem de 2 módulos, 40 mm (80 mm) e 34 mm (58 mm); URL de NFC-e de centenas de caracteres. → `renderQr.test.ts` (Task 2) e o checklist da impressora (Task 4).
5. **Campos em branco da empresa** ("null", linhas vazias) e dados do consumidor (CPF/CNPJ só de cliente cadastrado, inválido vira "não identificado"). → `fiscalReceipt.test.ts` (Task 3).
6. **Dados da empresa expostos:** só os públicos, sem nada do provedor. → `test_qualquer_usuario_ve_a_empresa...` e `test_sem_cnpj_cadastrado...` (Task 1).

---

## Estrutura de arquivos

| Arquivo | Responsabilidade |
|---|---|
| `backend/app/schemas/fiscal.py`, `backend/app/api/v1/fiscal.py` (modificar) | `GET /fiscal/status` devolve `issuer` (empresa emitente) |
| `frontend/src/hardware/printer/receipt.ts`, `renderHtml.ts` (modificar) | Bloco `qrcode` e o SVG do QR Code |
| `frontend/src/hardware/printer/fiscalReceipt.ts` | `buildFiscalReceipt`, `canPrintFiscal`, formatação de CNPJ/CPF/data |
| `frontend/src/hardware/printer/orderReceipt.ts`, `printOrder.ts` (modificar) | Reuso de itens/pagamentos; `buildDocumentReceipt` e `printOrderDocument` |
| `frontend/src/types/fiscal.ts` (modificar) | `FiscalIssuer` e `issuer` no status |
| `frontend/src/utils/autoPrint.ts` | `decideAutoPrint` e `printButtonLabel` |
| `frontend/src/components/fiscal/useOrderFiscal.ts`, `OrderFiscalPanel.tsx`, `PrintReceiptButton.tsx` | Consulta da nota, painel da tela de sucesso e botão de imprimir |
| `frontend/src/components/fiscal/FiscalStatusLine.tsx` (remover) | Substituído pelo painel |
| `NewOrder.tsx`, `OrderDetail.tsx` (modificar) | Usam o painel e o botão |
| `docs/hardware/impressora-epson.md`, `docs/fiscal/README.md` (modificar) | Checklist do cupom fiscal e estado do C2 |

---

### Task 1: O status fiscal devolve a empresa emitente

**Files:**
- Modify: `backend/app/schemas/fiscal.py`, `backend/app/api/v1/fiscal.py`, `backend/tests/test_fiscal_api.py`

**Interfaces:**
- Produces: `FiscalIssuerOut` (razão social, CNPJ, IE, endereço) e `FiscalStatusOut.issuer` (`None` quando não há linha de configuração ou CNPJ). Rota `GET /api/v1/fiscal/status`, qualquer usuário logado.

- [ ] **Step 1: Escrever os testes que falham**

Salve o script abaixo **fora do repositório** (por exemplo `%TEMP%/apply_edits.py`) e rode-o **da raiz do repositório**. Ele troca cada trecho antigo pelo novo, aceita arquivos LF ou CRLF e **para** se algum trecho não aparecer exatamente uma vez. Aqui ele atualiza os testes de status (a linha de configuração dos testes passa a ter os dados da empresa) e acrescenta dois testes:

```python
import sys
from pathlib import Path

EDITS = {
  "backend/tests/test_fiscal_api.py": [
    [
      "Linha de configuração com os dados da empresa",
      "    def row(self, enabled=True, environment=\"homologacao\"):\n        return SimpleNamespace(enabled=enabled, environment=environment)\n",
      "    ISSUER = dict(legal_name=\"EMPRESA TESTE LTDA\", cnpj=\"11222333000181\", ie=\"123456789\", street=\"Rua Exemplo\",\n                  number=\"100\", district=\"Centro\", city=\"Recife\", state=\"PE\", zip_code=\"50000000\")\n\n    def row(self, enabled=True, environment=\"homologacao\", **issuer):\n        return SimpleNamespace(enabled=enabled, environment=environment, **{**self.ISSUER, **issuer})\n"
    ],
    [
      "Status desligado: sem linha de configuração não há empresa",
      "        assert body == {\"enabled\": False, \"provider_configured\": False, \"environment\": \"homologacao\"}\n",
      "        assert body == {\"enabled\": False, \"provider_configured\": False, \"environment\": \"homologacao\", \"issuer\": None}\n"
    ],
    [
      "Status ligado devolve a empresa emitente",
      "        assert body == {\"enabled\": True, \"provider_configured\": True, \"environment\": \"producao\"}\n",
      "        assert body[\"enabled\"] is True and body[\"provider_configured\"] is True and body[\"environment\"] == \"producao\"\n        assert body[\"issuer\"] == {**self.ISSUER}\n\n    def test_qualquer_usuario_ve_a_empresa_para_imprimir_o_cupom_fiscal(self, client, monkeypatch):\n        monkeypatch.setattr(\"app.api.v1.fiscal.app_settings.FISCAL_PROVIDER\", \"none\")\n        body = client(UserRole.operator, db=FakeDb([self.row()])).get(\"/api/v1/fiscal/status\").json()\n        assert body[\"issuer\"][\"legal_name\"] == \"EMPRESA TESTE LTDA\" and body[\"issuer\"][\"cnpj\"] == \"11222333000181\"\n\n    def test_sem_cnpj_cadastrado_nao_ha_empresa_emitente(self, client, monkeypatch):\n        monkeypatch.setattr(\"app.api.v1.fiscal.app_settings.FISCAL_PROVIDER\", \"none\")\n        body = client(db=FakeDb([self.row(cnpj=None)])).get(\"/api/v1/fiscal/status\").json()\n        assert body[\"issuer\"] is None\n"
    ]
  ]
}

for rel, edits in EDITS.items():
    path = Path(rel)
    text = path.read_text(encoding="utf-8", newline="")
    crlf = "\r\n" in text
    text = text.replace("\r\n", "\n")
    for label, old, new in edits:
        count = text.count(old)
        if count != 1:
            sys.exit(f"{rel}: '{label}' casou {count} vezes (esperado 1)")
        text = text.replace(old, new)
    path.write_text(text.replace("\n", "\r\n") if crlf else text, encoding="utf-8", newline="")
    print(f"ok {rel}")
```

```bash
python apply_edits.py
```
Expected: `ok backend/tests/test_fiscal_api.py`.

- [ ] **Step 2: Rodar e ver falhar**

```bash
cd backend && python -m pytest -q tests/test_fiscal_api.py; cd ..
```
Expected: FAIL (`4 failed`: o status ainda não devolve `issuer`).

- [ ] **Step 3: Implementar**

Mesmo procedimento (salve como `apply_edits.py` fora do repositório e rode da raiz):

```python
import sys
from pathlib import Path

EDITS = {
  "backend/app/schemas/fiscal.py": [
    [
      "Empresa emitente no status",
      "class FiscalStatusOut(BaseModel):\n    \"\"\"O que o PDV precisa saber para mostrar o estado da emissão: ligada, ambiente e se há provedor configurado.\"\"\"\n\n    enabled: bool\n    provider_configured: bool\n    environment: str\n",
      "class FiscalIssuerOut(BaseModel):\n    \"\"\"Dados da empresa que saem impressos em todo cupom fiscal (públicos): o operador do caixa precisa deles para imprimir.\"\"\"\n\n    model_config = {\"from_attributes\": True}\n\n    legal_name: str | None\n    cnpj: str | None\n    ie: str | None\n    street: str | None\n    number: str | None\n    district: str | None\n    city: str | None\n    state: str | None\n    zip_code: str | None\n\n\nclass FiscalStatusOut(BaseModel):\n    \"\"\"O que o PDV precisa saber: emissão ligada, ambiente, se há provedor configurado e a empresa emitente (para o cupom).\"\"\"\n\n    enabled: bool\n    provider_configured: bool\n    environment: str\n    issuer: FiscalIssuerOut | None = None\n"
    ]
  ],
  "backend/app/api/v1/fiscal.py": [
    [
      "Importar o schema da empresa",
      "    FiscalDefaultIn, FiscalDefaultOut, FiscalSettingsIn, FiscalSettingsOut, FiscalStatusOut, PendingProductOut,\n",
      "    FiscalDefaultIn, FiscalDefaultOut, FiscalIssuerOut, FiscalSettingsIn, FiscalSettingsOut, FiscalStatusOut,\n    PendingProductOut,\n"
    ],
    [
      "Devolver a empresa no status",
      "        environment=row.environment if row else \"homologacao\",\n    )\n",
      "        environment=row.environment if row else \"homologacao\",\n        issuer=FiscalIssuerOut.model_validate(row) if row and row.cnpj else None,\n    )\n"
    ]
  ]
}

for rel, edits in EDITS.items():
    path = Path(rel)
    text = path.read_text(encoding="utf-8", newline="")
    crlf = "\r\n" in text
    text = text.replace("\r\n", "\n")
    for label, old, new in edits:
        count = text.count(old)
        if count != 1:
            sys.exit(f"{rel}: '{label}' casou {count} vezes (esperado 1)")
        text = text.replace(old, new)
    path.write_text(text.replace("\n", "\r\n") if crlf else text, encoding="utf-8", newline="")
    print(f"ok {rel}")
```

```bash
python apply_edits.py
```
Expected: `ok backend/app/schemas/fiscal.py` e `ok backend/app/api/v1/fiscal.py`.

- [ ] **Step 4: Rodar e ver passar**

```bash
cd backend && python -m pytest -q; cd ..
```
Expected: `121 passed` (119 já existentes + 2 novos).

- [ ] **Step 5: Commit**

```bash
git add backend/app/schemas/fiscal.py backend/app/api/v1/fiscal.py backend/tests/test_fiscal_api.py
git commit -m "feat(fiscal): status fiscal devolve a empresa emitente para imprimir o cupom fiscal"
```

---

### Task 2: Bloco de QR Code

**Files:**
- Modify: `frontend/package.json`, `frontend/package-lock.json` (dependência), `frontend/src/hardware/printer/receipt.ts`, `frontend/src/hardware/printer/renderHtml.ts`
- Test: `frontend/src/hardware/printer/renderQr.test.ts`

**Interfaces:**
- Produces: bloco `{ type: 'qrcode'; data: string }` em `ReceiptBlock`; `renderReceiptHtml` o desenha como `<div class="qr"><svg …></svg></div>` (correção de erro M, margem de 2 módulos; 40 mm em 80 mm e 34 mm em 58 mm).

- [ ] **Step 1: Escrever o teste que falha**

Create `frontend/src/hardware/printer/renderQr.test.ts`:

```typescript
import { describe, expect, it } from 'vitest'
import { renderReceiptHtml } from './renderHtml'

const render = (data: string, widthMm: 58 | 80 = 80) => renderReceiptHtml({ blocks: [{ type: 'qrcode', data }] }, { widthMm })

describe('bloco de QR Code', () => {
  it('vira um SVG dentro da página, sem depender de rede nem de imagem externa', () => {
    const html = render('https://exemplo.invalid/nfce?p=123')
    expect(html).toContain('<div class="qr"><svg')
    expect(html).toContain('viewBox=')
    expect(html).not.toMatch(/<img|https:\/\/exemplo\.invalid\/nfce\?p=123/) // o texto da URL não aparece: só o desenho
  })

  it('textos diferentes geram desenhos diferentes', () => {
    expect(render('https://exemplo.invalid/a')).not.toBe(render('https://exemplo.invalid/b'))
  })

  it('o tamanho segue a largura da bobina', () => {
    expect(render('x', 80)).toContain('.qr svg { width: 40mm; height: 40mm; }')
    expect(render('x', 58)).toContain('.qr svg { width: 34mm; height: 34mm; }')
  })

  it('cabe uma URL de NFC-e de verdade (centenas de caracteres) com correção de erro M', () => {
    const url = `https://exemplo.invalid/nfce/qrcode?p=${'2'.repeat(44)}|2|1|1|${'A'.repeat(40)}`
    expect(() => render(url)).not.toThrow()
  })
})
```

- [ ] **Step 2: Rodar e ver falhar**

```bash
cd frontend && npx vitest run src/hardware/printer/renderQr.test.ts; cd ..
```
Expected: FAIL (o renderizador ainda não conhece o bloco `qrcode`: o HTML sai sem o SVG).

- [ ] **Step 3: Instalar a biblioteca e implementar**

```bash
cd frontend && npm install qrcode-generator@^2.0.4; cd ..
```
Expected: `package.json` ganha `"qrcode-generator": "^2.0.4"` em `dependencies` e o `package-lock.json` é atualizado. (Avisos `npm warn install-scripts` sobre o esbuild são esperados e inofensivos.)

Mesmo procedimento de edição (salve como `apply_edits.py` fora do repositório e rode da raiz):

```python
import sys
from pathlib import Path

EDITS = {
  "frontend/src/hardware/printer/receipt.ts": [
    [
      "Comentário: o cupom fiscal usa QR Code",
      " * O cupom fiscal (sub-projeto C) acrescentará blocos de QR Code e de código de barras.\n",
      " * O cupom fiscal usa o bloco de QR Code; código de barras não é usado (a NFC-e não pede).\n"
    ],
    [
      "Bloco de QR Code",
      "  | { type: 'cut' }\n",
      "  | { type: 'cut' }\n  /** QR Code do cupom fiscal: o renderizador desenha a partir do texto (a URL de consulta devolvida pelo provedor). */\n  | { type: 'qrcode'; data: string }\n"
    ]
  ],
  "frontend/src/hardware/printer/renderHtml.ts": [
    [
      "Importar o gerador de QR Code",
      "import type { Receipt, ReceiptBlock } from './receipt'\n",
      "import qrcode from 'qrcode-generator'\nimport type { Receipt, ReceiptBlock } from './receipt'\n"
    ],
    [
      "Tamanho do QR Code por largura",
      "const FONT_PX: Record<58 | 80, number> = { 58: 11, 80: 12 }\n",
      "const FONT_PX: Record<58 | 80, number> = { 58: 11, 80: 12 }\n// Lado do QR Code: grande o bastante para a câmera do celular ler no papel térmico.\nconst QR_MM: Record<58 | 80, number> = { 58: 34, 80: 40 }\n"
    ],
    [
      "Desenhar o QR Code em SVG",
      "function renderBlock(block: ReceiptBlock): string {\n",
      "/** QR Code como SVG (nível de correção M, com margem): vai dentro da própria página, sem imagem externa nem rede. */\nfunction qrSvg(data: string): string {\n  const qr = qrcode(0, 'M')\n  qr.addData(data)\n  qr.make()\n  return qr.createSvgTag({ cellSize: 1, margin: 2, scalable: true })\n}\n\nfunction renderBlock(block: ReceiptBlock): string {\n"
    ],
    [
      "Bloco de QR Code no HTML",
      "    case 'cut':\n      return '' // no navegador quem corta o papel é o driver da impressora\n",
      "    case 'cut':\n      return '' // no navegador quem corta o papel é o driver da impressora\n    case 'qrcode':\n      return `<div class=\"qr\">${qrSvg(block.data)}</div>`\n"
    ],
    [
      "CSS do QR Code",
      ".blank { height: 3mm; }\n",
      ".blank { height: 3mm; }\n.qr { display: flex; justify-content: center; }\n.qr svg { width: ${QR_MM[widthMm]}mm; height: ${QR_MM[widthMm]}mm; }\n"
    ]
  ]
}

for rel, edits in EDITS.items():
    path = Path(rel)
    text = path.read_text(encoding="utf-8", newline="")
    crlf = "\r\n" in text
    text = text.replace("\r\n", "\n")
    for label, old, new in edits:
        count = text.count(old)
        if count != 1:
            sys.exit(f"{rel}: '{label}' casou {count} vezes (esperado 1)")
        text = text.replace(old, new)
    path.write_text(text.replace("\n", "\r\n") if crlf else text, encoding="utf-8", newline="")
    print(f"ok {rel}")
```

```bash
python apply_edits.py
```
Expected: `ok frontend/src/hardware/printer/receipt.ts` e `ok frontend/src/hardware/printer/renderHtml.ts`.

- [ ] **Step 4: Rodar e ver passar, e checar os tipos**

```bash
cd frontend && npx vitest run && npx tsc -b; cd ..
```
Expected: `142 passed` (138 já existentes + 4 novos) e `tsc` sem saída (sem erros).

- [ ] **Step 5: Commit**

```bash
git add frontend/package.json frontend/package-lock.json frontend/src/hardware/printer/receipt.ts frontend/src/hardware/printer/renderHtml.ts frontend/src/hardware/printer/renderQr.test.ts
git commit -m "feat(impressora): bloco de QR Code no cupom (SVG na própria página)"
```

---

### Task 3: Cupom fiscal e escolha do documento

**Files:**
- Create: `frontend/src/hardware/printer/fiscalReceipt.ts`
- Modify: `frontend/src/types/fiscal.ts`, `frontend/src/hardware/printer/orderReceipt.ts`, `frontend/src/hardware/printer/printOrder.ts`
- Test: `frontend/src/hardware/printer/fiscalReceipt.test.ts`, `frontend/src/hardware/printer/printDocument.test.ts`

**Interfaces:**
- Consumes: bloco `qrcode` (Task 2); `formatAccessKey` (C1b); `itemBlocks` e `paymentLines` (passam a ser exportados de `orderReceipt.ts`).
- Produces: `FiscalIssuer` e `FiscalStatusInfo.issuer` (`types/fiscal.ts`); `FiscalPrintInfo`, `canPrintFiscal`, `formatCnpj`, `formatCpf`, `formatDateTimeSeconds`, `buildFiscalReceipt(order, info, footer?)` (`fiscalReceipt.ts`); `buildDocumentReceipt(order, settings, fiscal)` e `printOrderDocument(order, settings, fiscal)` (`printOrder.ts`, este nunca lança).

- [ ] **Step 1: Escrever os testes que falham**

Create `frontend/src/hardware/printer/fiscalReceipt.test.ts`:

```typescript
import { describe, expect, it } from 'vitest'
import type { Order } from '@/types'
import type { FiscalIssuer } from '@/types/fiscal'
import { buildFiscalReceipt, canPrintFiscal, formatCnpj, formatCpf, type FiscalPrintInfo } from './fiscalReceipt'
import type { Receipt } from './receipt'

const KEY = '26260311222333000181650010000000071000000019'
const QR = 'https://fake.invalid/qr/26260311222333000181650010000000071000000019'

const issuer: FiscalIssuer = {
  legal_name: 'EMPRESA TESTE LTDA', cnpj: '11222333000181', ie: '123456789', street: 'Rua Exemplo', number: '100',
  district: 'Centro', city: 'Recife', state: 'PE', zip_code: '50000000',
}
const info = (environment: 'homologacao' | 'producao' = 'producao', over: Partial<FiscalIssuer> = {}): FiscalPrintInfo => ({
  issuer: { ...issuer, ...over }, environment,
})

function order(over: Record<string, unknown> = {}): Order {
  return {
    id: 'o1', order_number: 42, customer_id: null, customer: null, total: '8.63', discount: '0', payment_type: 'cash',
    payment_splits: null, status: 'delivered', notes: null, invoice_number: '7', invoice_series: '1', invoice_key: KEY,
    created_by_name: 'Admin', created_at: '2026-09-25T15:30:00Z',
    fiscal_status: 'authorized', fiscal_protocol: '900000000000007', fiscal_qr_url: QR, fiscal_xml_url: null,
    fiscal_emitted_at: '2026-09-25T15:30:05Z', fiscal_error: null, fiscal_attempts: 1,
    items: [{ id: 'i1', product_id: 'p1', product_name: 'BANANA PRATA', product_code: 101, barcode: null, unit_type: 'kg', qty: '1.235', unit_price: '6.99', subtotal: '8.63' }],
    ...over,
  } as unknown as Order
}

/** Todo o texto do cupom, uma linha por bloco (linhas de duas colunas viram "esquerda | direita"). */
function lines(receipt: Receipt): string[] {
  return receipt.blocks.flatMap((block) => {
    if (block.type === 'text') return [block.text]
    if (block.type === 'row') return [`${block.left} | ${block.right}`]
    return []
  })
}

describe('canPrintFiscal', () => {
  it('só nota autorizada com chave e QR Code vira cupom fiscal', () => {
    expect(canPrintFiscal(order())).toBe(true)
    expect(canPrintFiscal(order({ fiscal_status: 'pending' }))).toBe(false)
    expect(canPrintFiscal(order({ fiscal_status: 'rejected' }))).toBe(false)
    expect(canPrintFiscal(order({ fiscal_status: undefined }))).toBe(false) // pedido de antes da NFC-e
    expect(canPrintFiscal(order({ invoice_key: null }))).toBe(false)
    expect(canPrintFiscal(order({ fiscal_qr_url: null }))).toBe(false)
  })
})

describe('formatação de documentos', () => {
  it('CNPJ e CPF com pontuação; o que não tem o tamanho certo fica como veio', () => {
    expect(formatCnpj('11222333000181')).toBe('11.222.333/0001-81')
    expect(formatCpf('12345678909')).toBe('123.456.789-09')
    expect(formatCnpj('123')).toBe('123')
  })
})

describe('cupom fiscal (DANFE NFC-e)', () => {
  it('cabeçalho com a empresa emitente', () => {
    const text = lines(buildFiscalReceipt(order(), info()))
    expect(text[0]).toBe('EMPRESA TESTE LTDA')
    expect(text).toContain('CNPJ: 11.222.333/0001-81  IE: 123456789')
    expect(text).toContain('Rua Exemplo, 100 - Centro')
    expect(text).toContain('Recife - PE  CEP 50000-000')
  })

  it('identifica o documento e nunca diz "não fiscal"', () => {
    const text = lines(buildFiscalReceipt(order(), info())).join('\n')
    expect(text).toContain('DANFE NFC-e')
    expect(text).toContain('Documento Auxiliar da Nota Fiscal de Consumidor Eletrônica')
    expect(text).not.toMatch(/NÃO FISCAL/)
  })

  it('em homologação leva a marca obrigatória; em produção não', () => {
    const hml = lines(buildFiscalReceipt(order(), info('homologacao')))
    expect(hml).toContain('EMITIDA EM AMBIENTE DE HOMOLOGAÇÃO')
    expect(hml).toContain('SEM VALOR FISCAL')
    expect(lines(buildFiscalReceipt(order(), info('producao'))).join('\n')).not.toMatch(/HOMOLOGAÇÃO/)
  })

  it('itens, total de itens e valor a pagar (valores chegam como texto da API)', () => {
    const text = lines(buildFiscalReceipt(order(), info()))
    expect(text).toContain('101 BANANA PRATA')
    expect(text.some((line) => line.startsWith('1,235 kg x') && /R\$\s8,63$/.test(line))).toBe(true)
    expect(text).toContain('Qtd. total de itens | 1')
    expect(text.find((line) => line.startsWith('VALOR A PAGAR'))).toMatch(/R\$\s8,63$/)
    expect(text.join('\n')).not.toMatch(/NaN/)
  })

  it('subtotal e desconto só aparecem quando há desconto', () => {
    expect(lines(buildFiscalReceipt(order(), info())).join('\n')).not.toMatch(/Desconto/)
    const text = lines(buildFiscalReceipt(order({ discount: '1.00', total: '7.63' }), info()))
    expect(text.find((line) => line.startsWith('Subtotal'))).toMatch(/R\$\s8,63$/)
    expect(text.find((line) => line.startsWith('Desconto'))).toMatch(/-R\$\s1,00$/)
    expect(text.find((line) => line.startsWith('VALOR A PAGAR'))).toMatch(/R\$\s7,63$/)
  })

  it('formas de pagamento: única e dividida', () => {
    expect(lines(buildFiscalReceipt(order({ payment_type: 'pix' }), info())).some((l) => l.startsWith('Pix'))).toBe(true)
    const split = lines(buildFiscalReceipt(order({
      payment_type: 'mixed', payment_splits: [{ type: 'cash', amount: 5 }, { type: 'pix', amount: '3.63' }],
    }), info()))
    expect(split.filter((line) => line.startsWith('Dinheiro') || line.startsWith('Pix'))).toHaveLength(2)
  })

  it('consumidor: não identificado, CPF ou CNPJ do cliente cadastrado; documento inválido não vira lixo', () => {
    const consumer = (customer: unknown) => lines(buildFiscalReceipt(order({ customer }), info())).find((l) => l.startsWith('CONSUMIDOR'))
    expect(consumer(null)).toBe('CONSUMIDOR NÃO IDENTIFICADO')
    expect(consumer({ name: 'Ana', document: '123.456.789-09' })).toBe('CONSUMIDOR CPF: 123.456.789-09')
    expect(consumer({ name: 'Loja', document: '11.222.333/0001-81' })).toBe('CONSUMIDOR CNPJ: 11.222.333/0001-81')
    expect(consumer({ name: 'X', document: '12345' })).toBe('CONSUMIDOR NÃO IDENTIFICADO')
  })

  it('número, série, emissão com segundos, chave em grupos e protocolo', () => {
    const text = lines(buildFiscalReceipt(order(), info()))
    expect(text).toContain('NFC-e nº 7  Série 1')
    expect(text.some((line) => /^Emissão: \d{2}\/\d{2}\/2026 \d{2}:\d{2}:05$/.test(line))).toBe(true)
    expect(text).toContain('2626 0311 2223 3300 0181 6500 1000 0000 0710 0000 0019')
    expect(text).toContain('Protocolo de autorização: 900000000000007')
  })

  it('o QR Code leva a URL devolvida pelo provedor e termina com corte', () => {
    const { blocks } = buildFiscalReceipt(order(), info())
    expect(blocks.filter((block) => block.type === 'qrcode')).toEqual([{ type: 'qrcode', data: QR }])
    expect(blocks.at(-1)).toEqual({ type: 'cut' })
  })

  it('rodapé opcional', () => {
    expect(lines(buildFiscalReceipt(order(), info(), 'Obrigado!'))).toContain('Obrigado!')
    expect(lines(buildFiscalReceipt(order(), info(), '  '))).not.toContain('')
  })

  it('campos da empresa em branco não deixam linhas vazias nem "null"', () => {
    const text = lines(buildFiscalReceipt(order(), info('producao', { ie: null, street: null, number: null, district: null, zip_code: null })))
    expect(text).toContain('CNPJ: 11.222.333/0001-81')
    expect(text).toContain('Recife - PE')
    expect(text.join('\n')).not.toMatch(/null|undefined/)
    expect(text.every((line) => line.trim() !== '')).toBe(true)
  })

  it('sem nome da empresa, o cupom ainda sai (a nota vale pela chave e pelo QR)', () => {
    const text = lines(buildFiscalReceipt(order(), info('producao', { legal_name: null })))
    expect(text[0]).toMatch(/^CNPJ:/)
  })
})
```

Create `frontend/src/hardware/printer/printDocument.test.ts`:

```typescript
import { describe, expect, it } from 'vitest'
import type { Order } from '@/types'
import type { FiscalPrintInfo } from './fiscalReceipt'
import { buildDocumentReceipt, type PrinterSettings } from './printOrder'

const settings: PrinterSettings = { storeName: 'LOJA', address: '', phone: '', footer: 'Volte sempre', paperWidthMm: 80 }
const fiscal: FiscalPrintInfo = {
  environment: 'producao',
  issuer: { legal_name: 'EMPRESA TESTE LTDA', cnpj: '11222333000181', ie: null, street: null, number: null, district: null, city: null, state: null, zip_code: null },
}

function order(over: Record<string, unknown> = {}): Order {
  return {
    id: 'o1', order_number: 1, customer_id: null, customer: null, total: '5.00', discount: '0', payment_type: 'cash',
    payment_splits: null, status: 'delivered', notes: null, invoice_number: '7', invoice_series: '1',
    invoice_key: '26260311222333000181650010000000071000000019', created_by_name: 'Admin', created_at: '2026-09-25T15:30:00Z',
    fiscal_status: 'authorized', fiscal_protocol: '9', fiscal_qr_url: 'https://fake.invalid/qr/1', fiscal_emitted_at: '2026-09-25T15:30:05Z',
    items: [{ id: 'i1', product_id: 'p1', product_name: 'OVOS', product_code: 5, unit_type: 'unit', qty: '1', unit_price: '5.00', subtotal: '5.00' }],
    ...over,
  } as unknown as Order
}

const texts = (o: Order, info: FiscalPrintInfo | null) =>
  buildDocumentReceipt(o, settings, info).blocks.flatMap((b) => (b.type === 'text' ? [b.text] : []))

describe('qual cupom sai para o pedido', () => {
  it('nota autorizada e dados da empresa: cupom fiscal, com o rodapé da configuração', () => {
    const text = texts(order(), fiscal)
    expect(text).toContain('DANFE NFC-e')
    expect(text).toContain('Volte sempre')
    expect(text).not.toContain('CUPOM NÃO FISCAL')
  })

  it('nota autorizada mas sem os dados da empresa: cai no cupom não fiscal, nunca num cupom fiscal incompleto', () => {
    expect(texts(order(), null)).toContain('CUPOM NÃO FISCAL')
  })

  it('qualquer outro estado da nota sai como cupom não fiscal', () => {
    for (const fiscal_status of ['not_required', 'pending', 'rejected', undefined]) {
      expect(texts(order({ fiscal_status }), fiscal)).toContain('CUPOM NÃO FISCAL')
    }
  })
})
```

- [ ] **Step 2: Rodar e ver falhar**

```bash
cd frontend && npx vitest run src/hardware/printer/fiscalReceipt.test.ts src/hardware/printer/printDocument.test.ts; cd ..
```
Expected: FAIL (`Failed to load url ./fiscalReceipt` e `buildDocumentReceipt` inexistente).

- [ ] **Step 3: Implementar**

Create `frontend/src/hardware/printer/fiscalReceipt.ts`:

```typescript
import type { Order } from '@/types'
import type { FiscalEnvironment, FiscalIssuer } from '@/types/fiscal'
import { formatCurrency, formatPayment } from '@/utils/format'
import { formatAccessKey } from '@/utils/fiscal'
import { itemBlocks, paymentLines } from './orderReceipt'
import type { Receipt, ReceiptBlock } from './receipt'

/** O que o cupom fiscal precisa além do pedido: a empresa emitente e o ambiente (homologação leva uma marca obrigatória). */
export interface FiscalPrintInfo {
  issuer: FiscalIssuer
  environment: FiscalEnvironment
}

/** Só uma nota autorizada, com chave e QR Code, vira cupom fiscal. Qualquer outro pedido sai como cupom não fiscal. */
export function canPrintFiscal(order: Pick<Order, 'fiscal_status' | 'invoice_key' | 'fiscal_qr_url'>): boolean {
  return order.fiscal_status === 'authorized' && !!order.invoice_key && !!order.fiscal_qr_url
}

export function formatCnpj(digits: string): string {
  return digits.replace(/^(\d{2})(\d{3})(\d{3})(\d{4})(\d{2})$/, '$1.$2.$3/$4-$5')
}

export function formatCpf(digits: string): string {
  return digits.replace(/^(\d{3})(\d{3})(\d{3})(\d{2})$/, '$1.$2.$3-$4')
}

/** Data e hora com segundos, como no cupom fiscal ("25/09/2026 12:30:05"). */
export function formatDateTimeSeconds(iso: string): string {
  return new Intl.DateTimeFormat('pt-BR', {
    day: '2-digit', month: '2-digit', year: 'numeric', hour: '2-digit', minute: '2-digit', second: '2-digit',
  }).format(new Date(iso)).replace(', ', ' ')
}

const row = (left: string, right: string): ReceiptBlock => ({ type: 'row', left, right })
const center = (text: string, bold = false): ReceiptBlock => ({ type: 'text', text, align: 'center', bold })

function issuerBlocks(issuer: FiscalIssuer): ReceiptBlock[] {
  const blocks: ReceiptBlock[] = []
  if (issuer.legal_name?.trim()) blocks.push({ type: 'text', text: issuer.legal_name.trim(), align: 'center', bold: true, size: 'large' })
  const ids = [issuer.cnpj ? `CNPJ: ${formatCnpj(issuer.cnpj)}` : '', issuer.ie ? `IE: ${issuer.ie}` : ''].filter(Boolean)
  if (ids.length > 0) blocks.push(center(ids.join('  ')))
  const street = [issuer.street, issuer.number].filter((part) => part?.trim()).join(', ')
  const line1 = [street, issuer.district].filter((part) => part?.trim()).join(' - ')
  if (line1) blocks.push(center(line1))
  const cep = issuer.zip_code ? `CEP ${issuer.zip_code.replace(/^(\d{5})(\d{3})$/, '$1-$2')}` : ''
  const line2 = [[issuer.city, issuer.state].filter((part) => part?.trim()).join(' - '), cep].filter(Boolean).join('  ')
  if (line2) blocks.push(center(line2))
  return blocks
}

function consumerLine(order: Order): string {
  const digits = (order.customer?.document ?? '').replace(/\D/g, '')
  if (digits.length === 11) return `CONSUMIDOR CPF: ${formatCpf(digits)}`
  if (digits.length === 14) return `CONSUMIDOR CNPJ: ${formatCnpj(digits)}`
  return 'CONSUMIDOR NÃO IDENTIFICADO'
}

/**
 * Cupom fiscal (DANFE NFC-e) de uma nota AUTORIZADA. Os valores da API chegam como texto (Decimal): tudo passa por Number().
 * Fora deste cupom, por ora: tributos aproximados (Lei 12.741) e o endereço de consulta da SEFAZ, que dependem de
 * dados que o sistema ainda não tem (ver docs/fiscal).
 */
export function buildFiscalReceipt(order: Order, info: FiscalPrintInfo, footer = ''): Receipt {
  const blocks: ReceiptBlock[] = [...issuerBlocks(info.issuer), { type: 'blank' }]

  blocks.push(center('DANFE NFC-e', true), center('Documento Auxiliar da Nota Fiscal de Consumidor Eletrônica'))
  if (info.environment === 'homologacao') {
    blocks.push({ type: 'divider' }, center('EMITIDA EM AMBIENTE DE HOMOLOGAÇÃO', true), center('SEM VALOR FISCAL', true))
  }
  blocks.push({ type: 'divider' })

  for (const item of order.items) blocks.push(...itemBlocks(item))
  blocks.push({ type: 'divider' })

  const subtotal = order.items.reduce((sum, item) => sum + Number(item.subtotal), 0)
  const discount = Number(order.discount) || 0
  blocks.push(row('Qtd. total de itens', String(order.items.length)))
  if (discount > 0) blocks.push(row('Subtotal', formatCurrency(subtotal)), row('Desconto', `-${formatCurrency(discount)}`))
  blocks.push({ type: 'row', left: 'VALOR A PAGAR', right: formatCurrency(Number(order.total)), bold: true })

  blocks.push({ type: 'divider' }, { type: 'text', text: 'FORMA DE PAGAMENTO', bold: true })
  for (const payment of paymentLines(order)) blocks.push(row(formatPayment(payment.type), formatCurrency(payment.amount)))

  blocks.push({ type: 'divider' }, center(consumerLine(order)), { type: 'divider' })

  blocks.push(center(`NFC-e nº ${order.invoice_number ?? ''}  Série ${order.invoice_series ?? ''}`.trim(), true))
  if (order.fiscal_emitted_at) blocks.push(center(`Emissão: ${formatDateTimeSeconds(order.fiscal_emitted_at)}`))
  blocks.push(center('Consulte pela chave de acesso:'), center(formatAccessKey(order.invoice_key)))
  if (order.fiscal_protocol) blocks.push(center(`Protocolo de autorização: ${order.fiscal_protocol}`))

  blocks.push({ type: 'blank' }, { type: 'qrcode', data: order.fiscal_qr_url ?? '' }, { type: 'blank' })
  if (footer.trim()) blocks.push(center(footer.trim()), { type: 'blank' })
  blocks.push({ type: 'cut' })
  return { blocks }
}
```

Mesmo procedimento de edição (salve como `apply_edits.py` fora do repositório e rode da raiz). Aqui: o tipo da empresa emitente, o reuso das linhas de item e pagamento e o `buildDocumentReceipt`/`printOrderDocument`:

```python
import sys
from pathlib import Path

EDITS = {
  "frontend/src/types/fiscal.ts": [
    [
      "Empresa emitente no status",
      "/** Resposta de GET /fiscal/status: o que o PDV precisa saber para mostrar o estado da emissão. */\nexport interface FiscalStatusInfo {\n  enabled: boolean\n  provider_configured: boolean\n  environment: FiscalEnvironment\n}\n",
      "/** Empresa emitente: os dados que saem impressos em todo cupom fiscal. */\nexport interface FiscalIssuer {\n  legal_name: string | null\n  cnpj: string | null\n  ie: string | null\n  street: string | null\n  number: string | null\n  district: string | null\n  city: string | null\n  state: string | null\n  zip_code: string | null\n}\n\n/** Resposta de GET /fiscal/status: o que o PDV precisa saber para mostrar o estado da emissão e imprimir o cupom fiscal. */\nexport interface FiscalStatusInfo {\n  enabled: boolean\n  provider_configured: boolean\n  environment: FiscalEnvironment\n  issuer: FiscalIssuer | null\n}\n"
    ]
  ],
  "frontend/src/hardware/printer/orderReceipt.ts": [
    [
      "Reaproveitar as linhas de item no cupom fiscal",
      "function itemBlocks(item: OrderItem): ReceiptBlock[] {\n",
      "export function itemBlocks(item: OrderItem): ReceiptBlock[] {\n"
    ],
    [
      "Reaproveitar as linhas de pagamento no cupom fiscal",
      "function paymentLines(order: Order): { type: string; amount: number }[] {\n",
      "export function paymentLines(order: Order): { type: string; amount: number }[] {\n"
    ]
  ],
  "frontend/src/hardware/printer/printOrder.ts": [
    [
      "Importar o cupom fiscal",
      "import { buildOrderReceipt, buildTestReceipt, type ReceiptConfig } from './orderReceipt'\n",
      "import { buildFiscalReceipt, canPrintFiscal, type FiscalPrintInfo } from './fiscalReceipt'\nimport { buildOrderReceipt, buildTestReceipt, type ReceiptConfig } from './orderReceipt'\n"
    ],
    [
      "Importar o tipo do cupom",
      "import { printHtml } from './print'\n",
      "import { printHtml } from './print'\nimport type { Receipt } from './receipt'\n"
    ],
    [
      "Escolher o documento certo para o pedido",
      "/** Página de teste para acertar o papel, o driver e a largura do caixa. */\n",
      "/**\n * O documento certo para o pedido: cupom fiscal quando a NFC-e está autorizada (e temos os dados da empresa), senão o\n * cupom não fiscal. Sem os dados da empresa não sai um cupom fiscal incompleto.\n */\nexport function buildDocumentReceipt(order: Order, settings: PrinterSettings, fiscal: FiscalPrintInfo | null): Receipt {\n  if (fiscal && canPrintFiscal(order)) return buildFiscalReceipt(order, fiscal, settings.footer)\n  return buildOrderReceipt(order, settings)\n}\n\n/** Imprime o cupom do pedido (fiscal ou não fiscal). Nunca lança, como as demais impressões. */\nexport function printOrderDocument(order: Order, settings: PrinterSettings, fiscal: FiscalPrintInfo | null): void {\n  safely(() => printHtml(renderReceiptHtml(buildDocumentReceipt(order, settings, fiscal), { widthMm: settings.paperWidthMm })))\n}\n\n/** Página de teste para acertar o papel, o driver e a largura do caixa. */\n"
    ]
  ]
}

for rel, edits in EDITS.items():
    path = Path(rel)
    text = path.read_text(encoding="utf-8", newline="")
    crlf = "\r\n" in text
    text = text.replace("\r\n", "\n")
    for label, old, new in edits:
        count = text.count(old)
        if count != 1:
            sys.exit(f"{rel}: '{label}' casou {count} vezes (esperado 1)")
        text = text.replace(old, new)
    path.write_text(text.replace("\n", "\r\n") if crlf else text, encoding="utf-8", newline="")
    print(f"ok {rel}")
```

```bash
python apply_edits.py
```
Expected: `ok` para os três arquivos.

- [ ] **Step 4: Rodar e ver passar, e checar os tipos**

```bash
cd frontend && npx vitest run && npx tsc -b; cd ..
```
Expected: `159 passed` (142 + 14 do cupom fiscal + 3 da escolha do documento) e `tsc` sem saída.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/hardware/printer/fiscalReceipt.ts frontend/src/hardware/printer/fiscalReceipt.test.ts frontend/src/hardware/printer/printDocument.test.ts frontend/src/hardware/printer/orderReceipt.ts frontend/src/hardware/printer/printOrder.ts frontend/src/types/fiscal.ts
git commit -m "feat(impressora): cupom fiscal (DANFE NFC-e) e escolha do documento certo para o pedido"
```

---

### Task 4: Imprimir o cupom fiscal no PDV e no detalhe do pedido

**Files:**
- Create: `frontend/src/utils/autoPrint.ts`, `frontend/src/components/fiscal/useOrderFiscal.ts`, `frontend/src/components/fiscal/PrintReceiptButton.tsx`, `frontend/src/components/fiscal/OrderFiscalPanel.tsx`
- Remove: `frontend/src/components/fiscal/FiscalStatusLine.tsx`
- Modify: `frontend/src/pages/orders/NewOrder.tsx`, `frontend/src/pages/orders/OrderDetail.tsx`, `docs/hardware/impressora-epson.md`, `docs/fiscal/README.md`
- Test: `frontend/src/utils/autoPrint.test.ts`

**Interfaces:**
- Consumes: `printOrderDocument`, `canPrintFiscal`, `FiscalPrintInfo` (Task 3); `getFiscalStatus`, `getOrder`, `describeFiscal`, `isFiscalSettled`, `fiscalPollDelay`, `MAX_FISCAL_POLLS` (C1b); `usePrinterSettings` (B3).
- Produces: `decideAutoPrint(input)` (`'skip' | 'wait' | 'print'`) e `printButtonLabel(fiscal, reprint)`; `useOrderFiscal(order)`; `PrintReceiptButton({ order, reprint?, size?, className? })`; `OrderFiscalPanel({ order })` (estado da NFC-e, impressão automática e botão). A tela "Pedido registrado" usa o painel; o detalhe do pedido usa o botão ("Reimprimir cupom fiscal" com a nota autorizada).

- [ ] **Step 1: Guardar a alteração do usuário no `NewOrder.tsx` (não misturar nos commits)**

```bash
git stash push -m "wip-usuario-NewOrder" -- frontend/src/pages/orders/NewOrder.tsx
git diff --stat -- frontend/src/pages/orders/NewOrder.tsx
git stash list
```
Expected: o `git diff --stat` **não** mostra nada e o `stash list` mostra `wip-usuario-NewOrder`. Se o arquivo não tinha alteração pendente, o `stash push` diz `No local changes to save`; nesse caso pule o Step 9.

- [ ] **Step 2: Escrever o teste que falha**

Create `frontend/src/utils/autoPrint.test.ts`:

```typescript
import { describe, expect, it } from 'vitest'
import { decideAutoPrint, printButtonLabel, type AutoPrintInput } from './autoPrint'

const base: AutoPrintInput = { printerEnabled: true, statusReady: true, emissionEnabled: false, settled: false, gaveUp: false }
const decide = (over: Partial<AutoPrintInput>) => decideAutoPrint({ ...base, ...over })

describe('impressão automática ao confirmar o pedido', () => {
  it('impressora do caixa desligada: nunca imprime sozinho', () => {
    expect(decide({ printerEnabled: false })).toBe('skip')
    expect(decide({ printerEnabled: false, emissionEnabled: true, settled: true })).toBe('skip')
  })

  it('espera o estado da emissão chegar antes de decidir', () => {
    expect(decide({ statusReady: false })).toBe('wait')
  })

  it('sem emissão fiscal imprime na hora, como sempre foi', () => {
    expect(decide({ emissionEnabled: false })).toBe('print')
  })

  it('com emissão fiscal espera a nota assentar e então imprime', () => {
    expect(decide({ emissionEnabled: true })).toBe('wait')
    expect(decide({ emissionEnabled: true, settled: true })).toBe('print')
  })

  it('se as consultas acabam sem a nota assentar, imprime mesmo assim (o cupom não fiscal não trava o caixa)', () => {
    expect(decide({ emissionEnabled: true, gaveUp: true })).toBe('print')
  })
})

describe('texto do botão de imprimir', () => {
  it('cupom fiscal só com a nota autorizada; "Reimprimir" na tela do pedido', () => {
    expect(printButtonLabel(false, false)).toBe('Imprimir cupom')
    expect(printButtonLabel(false, true)).toBe('Reimprimir cupom')
    expect(printButtonLabel(true, false)).toBe('Imprimir cupom fiscal')
    expect(printButtonLabel(true, true)).toBe('Reimprimir cupom fiscal')
  })
})
```

- [ ] **Step 3: Rodar e ver falhar**

```bash
cd frontend && npx vitest run src/utils/autoPrint.test.ts; cd ..
```
Expected: FAIL (`Failed to load url ./autoPrint`).

- [ ] **Step 4: Implementar a lógica e os componentes**

Create `frontend/src/utils/autoPrint.ts`:

```typescript
export type AutoPrintDecision = 'skip' | 'wait' | 'print'

export interface AutoPrintInput {
  /** "Imprimir o cupom ao confirmar o pedido" está ligado neste caixa. */
  printerEnabled: boolean
  /** O estado da emissão fiscal (GET /fiscal/status) já respondeu (ou falhou: aí conta como emissão desligada). */
  statusReady: boolean
  emissionEnabled: boolean
  /** A NFC-e do pedido já assentou (autorizada, rejeitada, ou com um motivo gravado). */
  settled: boolean
  /** Acabaram as consultas e a nota não assentou (provedor lento). */
  gaveUp: boolean
}

/**
 * Quando imprimir sozinho ao confirmar o pedido. Sem emissão fiscal, imprime na hora (como sempre foi). Com emissão, espera
 * a nota assentar para o cupom sair com o documento certo (fiscal se autorizada, não fiscal nos demais casos) e imprime
 * assim que ela assenta, ou quando as consultas acabam.
 */
export function decideAutoPrint(input: AutoPrintInput): AutoPrintDecision {
  if (!input.printerEnabled) return 'skip'
  if (!input.statusReady) return 'wait'
  if (!input.emissionEnabled) return 'print'
  return input.settled || input.gaveUp ? 'print' : 'wait'
}

/** Texto do botão de imprimir: o cupom fiscal quando a nota está autorizada, senão o cupom do pedido. */
export function printButtonLabel(fiscal: boolean, reprint: boolean): string {
  const base = fiscal ? 'cupom fiscal' : 'cupom'
  return `${reprint ? 'Reimprimir' : 'Imprimir'} ${base}`
}
```

Create `frontend/src/components/fiscal/useOrderFiscal.ts`:

```typescript
import { useRef } from 'react'
import { useQuery } from '@tanstack/react-query'
import { getFiscalStatus } from '@/api/fiscal'
import { getOrder } from '@/api/orders'
import type { FiscalPrintInfo } from '@/hardware/printer/fiscalReceipt'
import type { Order } from '@/types'
import { fiscalPollDelay, isFiscalSettled, MAX_FISCAL_POLLS } from '@/utils/fiscal'

/**
 * Estado da NFC-e de um pedido recém-criado. A emissão roda em segundo plano depois de o pedido ser salvo, então este
 * hook consulta o pedido algumas vezes até a nota assentar. Use uma vez por tela: as consultas contam por instância.
 */
export function useOrderFiscal(order: Order) {
  const { data: status, isPending: statusPending } = useQuery({
    queryKey: ['fiscal', 'status'],
    queryFn: getFiscalStatus,
    staleTime: 60_000,
  })
  const emissionEnabled = !!status?.enabled
  const polls = useRef(0)

  const { data: current } = useQuery({
    queryKey: ['order', order.id, 'fiscal'],
    queryFn: () => {
      polls.current += 1
      return getOrder(order.id)
    },
    enabled: emissionEnabled,
    initialData: order,
    refetchInterval: (query) => fiscalPollDelay(query.state.data ?? order, emissionEnabled, polls.current),
  })

  const latest = current ?? order
  const settled = isFiscalSettled(latest)
  // Acabaram as consultas e a nota não assentou (provedor lento): a tela deixa de dizer "Emitindo…".
  const gaveUp = emissionEnabled && polls.current >= MAX_FISCAL_POLLS && !settled
  const fiscalInfo: FiscalPrintInfo | null = status?.issuer ? { issuer: status.issuer, environment: status.environment } : null

  // Erro ao consultar o estado da emissão conta como "pronto e desligado": o caixa não pode ficar esperando por isso.
  return { latest, emissionEnabled, statusReady: !statusPending, settled, gaveUp, fiscalInfo }
}
```

Create `frontend/src/components/fiscal/PrintReceiptButton.tsx`:

```tsx
import { useQuery } from '@tanstack/react-query'
import { PrinterIcon } from '@heroicons/react/24/outline'
import { getFiscalStatus } from '@/api/fiscal'
import { Button } from '@/components/ui/Button'
import { canPrintFiscal, type FiscalPrintInfo } from '@/hardware/printer/fiscalReceipt'
import { printOrderDocument } from '@/hardware/printer/printOrder'
import { usePrinterSettings } from '@/stores/printer'
import type { Order } from '@/types'
import { printButtonLabel } from '@/utils/autoPrint'

interface Props {
  order: Order
  /** Na tela do pedido o botão diz "Reimprimir". */
  reprint?: boolean
  size?: 'sm' | 'md'
  className?: string
}

/** Botão de imprimir o cupom do pedido: fiscal se a NFC-e está autorizada, senão o cupom não fiscal. */
export function PrintReceiptButton({ order, reprint = false, size, className }: Props) {
  const { data: status } = useQuery({ queryKey: ['fiscal', 'status'], queryFn: getFiscalStatus, staleTime: 60_000 })
  const fiscal: FiscalPrintInfo | null = status?.issuer ? { issuer: status.issuer, environment: status.environment } : null
  const isFiscal = !!fiscal && canPrintFiscal(order)

  return (
    <Button
      variant="secondary"
      size={size}
      className={className}
      onClick={() => printOrderDocument(order, usePrinterSettings.getState(), fiscal)}
    >
      <PrinterIcon className="w-4 h-4 mr-1.5" />
      {printButtonLabel(isFiscal, reprint)}
    </Button>
  )
}
```

Create `frontend/src/components/fiscal/OrderFiscalPanel.tsx`:

```tsx
import { useEffect, useRef } from 'react'
import { Badge } from '@/components/ui/Badge'
import { printOrderDocument } from '@/hardware/printer/printOrder'
import { usePrinterSettings } from '@/stores/printer'
import type { Order } from '@/types'
import { decideAutoPrint } from '@/utils/autoPrint'
import { describeFiscal } from '@/utils/fiscal'
import { PrintReceiptButton } from './PrintReceiptButton'
import { useOrderFiscal } from './useOrderFiscal'

/**
 * Bloco da tela "Pedido registrado": estado da NFC-e, impressão automática (se o caixa a liga) e o botão de imprimir.
 * Com emissão fiscal a impressão automática espera a nota assentar, para o cupom sair com o documento certo; o caixa
 * nunca fica esperando, porque os botões da tela continuam livres o tempo todo.
 */
export function OrderFiscalPanel({ order }: { order: Order }) {
  const fiscal = useOrderFiscal(order)
  const printerEnabled = usePrinterSettings((state) => state.enabled)
  const printed = useRef(false)

  const decision = decideAutoPrint({
    printerEnabled,
    statusReady: fiscal.statusReady,
    emissionEnabled: fiscal.emissionEnabled,
    settled: fiscal.settled,
    gaveUp: fiscal.gaveUp,
  })

  useEffect(() => {
    if (decision !== 'print' || printed.current) return
    printed.current = true // uma vez só por pedido, mesmo que a tela renderize de novo
    printOrderDocument(fiscal.latest, usePrinterSettings.getState(), fiscal.fiscalInfo)
  }, [decision, fiscal.latest, fiscal.fiscalInfo])

  const summary = fiscal.gaveUp
    ? { label: 'NFC-e ainda em andamento', variant: 'amber' as const, detail: 'Confira o estado no detalhe do pedido.' }
    : describeFiscal(fiscal.latest, fiscal.emissionEnabled)
  const showStatus = fiscal.emissionEnabled || summary.label !== 'Sem NFC-e' // emissão desligada: a tela fica como sempre foi

  return (
    <div className="mb-3">
      {showStatus && (
        <div className="mb-6 flex flex-col items-center gap-1" aria-live="polite">
          <Badge variant={summary.variant}>{summary.label}</Badge>
          {summary.detail && <p className="text-xs text-slate-500 dark:text-slate-400">{summary.detail}</p>}
        </div>
      )}
      <PrintReceiptButton order={fiscal.latest} className="w-full" />
    </div>
  )
}
```

Remova o componente antigo (o painel o substitui) e aplique as edições nas telas e nos documentos (mesmo procedimento: salve como `apply_edits.py` fora do repositório e rode da raiz):

```bash
git rm frontend/src/components/fiscal/FiscalStatusLine.tsx
```

```python
import sys
from pathlib import Path

EDITS = {
  "frontend/src/pages/orders/NewOrder.tsx": [
    [
      "Sem o ícone de impressora (o botão mudou de lugar)",
      "  PrinterIcon,\n",
      ""
    ],
    [
      "Painel fiscal no lugar dos imports de impressão",
      "import { printOrderReceipt } from '@/hardware/printer/printOrder'\nimport { FiscalStatusLine } from '@/components/fiscal/FiscalStatusLine'\nimport { usePrinterSettings } from '@/stores/printer'\n",
      "import { OrderFiscalPanel } from '@/components/fiscal/OrderFiscalPanel'\n"
    ],
    [
      "A impressão automática passa para o painel (espera a NFC-e)",
      "      setSuccess(true)\n      // O pedido já está salvo: imprimir nunca atrasa nem impede o registro.\n      const printer = usePrinterSettings.getState()\n      if (printer.enabled) printOrderReceipt(order, printer)\n",
      "      setSuccess(true)\n"
    ],
    [
      "Estado da NFC-e e botão de imprimir no painel",
      "          <FiscalStatusLine order={lastOrder.order} />\n          <Button\n            variant=\"secondary\"\n            className=\"w-full mb-3\"\n            onClick={() => printOrderReceipt(lastOrder.order, usePrinterSettings.getState())}\n          >\n            <PrinterIcon className=\"w-4 h-4 mr-1.5\" />\n            Imprimir cupom\n          </Button>\n",
      "          <OrderFiscalPanel order={lastOrder.order} />\n"
    ]
  ],
  "frontend/src/pages/orders/OrderDetail.tsx": [
    [
      "Sem o ícone de impressora (o botão mudou de lugar)",
      "  PrinterIcon,\n",
      ""
    ],
    [
      "Botão de imprimir escolhe o cupom certo",
      "import { usePrinterSettings } from '@/stores/printer'\nimport { printOrderReceipt } from '@/hardware/printer/printOrder'\n",
      "import { PrintReceiptButton } from '@/components/fiscal/PrintReceiptButton'\n"
    ],
    [
      "Reimprimir o cupom fiscal ou não fiscal",
      "          <Button\n            variant=\"secondary\"\n            size=\"sm\"\n            className=\"mt-3\"\n            onClick={() => printOrderReceipt(order, usePrinterSettings.getState())}\n          >\n            <PrinterIcon className=\"w-4 h-4 mr-1.5\" />\n            Reimprimir cupom\n          </Button>\n",
      "          <PrintReceiptButton order={order} reprint size=\"sm\" className=\"mt-3\" />\n"
    ]
  ],
  "docs/hardware/impressora-epson.md": [
    [
      "Checklist do cupom fiscal",
      "- [ ] (Opcional) Troque para **58 mm** na aba Impressora e no driver, e repita o teste.\n",
      "- [ ] (Opcional) Troque para **58 mm** na aba Impressora e no driver, e repita o teste.\n- [ ] **Cupom fiscal** (só com a emissão fiscal ligada; em desenvolvimento, com o provedor falso): confirme um pedido; o cupom sai como **DANFE NFC-e**, com a marca de homologação, itens, valor a pagar, forma de pagamento, número/série, chave de acesso em grupos de 4 dígitos e o **QR Code**.\n- [ ] O **QR Code** não sai cortado nas bordas e a **câmera do celular o lê** (com o provedor falso ele abre um endereço `fake.invalid`, o que é esperado). Se não ler, confira o contraste da fita e a densidade de impressão do driver.\n- [ ] Pedido **sem nota autorizada** (fiado, dado fiscal faltando) continua saindo como **CUPOM NÃO FISCAL**.\n"
    ]
  ],
  "docs/fiscal/README.md": [
    [
      "Estado do C2",
      "5. Telas (C1b), cupom com QR Code na Epson (C2) e cancelamento/contingência (C3).\n",
      "5. Cancelamento e contingência (C3).\n\n## Cupom fiscal na Epson (C2)\n\nCom a NFC-e **autorizada**, o botão de imprimir passa a ser **Imprimir cupom fiscal** (na tela do pedido, **Reimprimir cupom fiscal**) e o cupom sai como DANFE NFC-e, com QR Code. Nos demais casos (fiado, dado fiscal faltando, emissão desligada) continua saindo o **cupom não fiscal**.\n\n- **Impressão automática ao confirmar:** com a emissão fiscal ligada, ela espera a nota assentar (alguns segundos) para imprimir o documento certo; se a nota não assentar a tempo, imprime o cupom não fiscal. Sem emissão fiscal, imprime na hora, como antes.\n- **Dados da empresa** no cupom vêm de Configurações > Fiscal (o servidor os entrega a qualquer usuário logado em `GET /api/v1/fiscal/status`).\n- **Ainda fora do cupom:** os **tributos aproximados** (Lei 12.741, que precisa da tabela IBPT por NCM) e o **endereço de consulta da SEFAZ** impresso por extenso (depende do provedor real). Confirmar com o contador antes de emitir em produção.\n"
    ]
  ]
}

for rel, edits in EDITS.items():
    path = Path(rel)
    text = path.read_text(encoding="utf-8", newline="")
    crlf = "\r\n" in text
    text = text.replace("\r\n", "\n")
    for label, old, new in edits:
        count = text.count(old)
        if count != 1:
            sys.exit(f"{rel}: '{label}' casou {count} vezes (esperado 1)")
        text = text.replace(old, new)
    path.write_text(text.replace("\n", "\r\n") if crlf else text, encoding="utf-8", newline="")
    print(f"ok {rel}")
```

```bash
python apply_edits.py
```
Expected: `ok` para os quatro arquivos.

- [ ] **Step 5: Checar tipos, testes e build**

```bash
cd frontend && npx tsc -b && npx vitest run && npx vite build; cd ..
```
Expected: `tsc` sem saída; `165 passed` (159 + 6); `vite build` termina com `built in`.

- [ ] **Step 6: Verificar no navegador (API simulada, impressão neutralizada)**

Suba o app (`preview_start` com o `hortifacil-frontend`) e entre como administrador (chave `hortifacil-auth` no `localStorage`). **Antes de qualquer pedido**, ligue a impressão automática do caixa (`pdv-printer` com `enabled:true`), **recarregue**, e neutralize a impressão real: `window.print=()=>{}` e `Object.defineProperty(HTMLIFrameElement.prototype,'srcdoc',{configurable:true,set(h){window.__printed.push({t:Date.now(),h})},get(){return ''}})` (com `window.__printed=[]`). Force `Object.defineProperty(document,'visibilityState',{configurable:true,get:()=>'visible'})` (o painel oculto pausa as consultas). Simule a API trocando `window.XMLHttpRequest` (como nas tarefas anteriores). Respostas:

- `GET /fiscal/status` → `{enabled:true, provider_configured:true, environment:'homologacao', issuer:{legal_name:'EMPRESA TESTE LTDA', cnpj:'11222333000181', ie:'123456789', street:'Rua Exemplo', number:'100', district:'Centro', city:'Recife', state:'PE', zip_code:'50000000'}}` (empresa **fictícia**)
- `GET /products` → uma `BANANA` de `unit_type:'unit'`; `POST /orders` → um pedido `o1` com `fiscal_status:'not_required'` e sem `fiscal_error`; `GET /orders/o1` → o mesmo pedido nas duas primeiras consultas e depois `fiscal_status:'authorized'` com `invoice_number:'7'`, `invoice_series:'1'`, `invoice_key` de 44 dígitos, `fiscal_protocol`, `fiscal_emitted_at` e `fiscal_qr_url:'https://fake.invalid/qr/…'`.

1. **PDV com emissão ligada:** confirme um pedido (busque BANAN, adicione o item **disparando `pointerdown`, `mousedown`, `pointerup`, `mouseup` e `click` no botão da linha**, `+ Dinheiro`, **Confirmar Pedido**). Na tela "Pedido registrado" aparece **"Emitindo NFC-e…"** e o botão **"Imprimir cupom"**, e **nada foi impresso ainda**. Quando vira **"Autorizada"** (depois de duas consultas), o botão vira **"Imprimir cupom fiscal"** e a impressão automática dispara **uma única vez**: o HTML capturado contém `DANFE NFC-e`, `EMITIDA EM AMBIENTE DE HOMOLOGAÇÃO`, a chave em grupos de 4 dígitos, `<div class="qr"><svg` e o rodapé configurado, e **não** contém `CUPOM NÃO FISCAL`. Para ver o cupom, escreva o HTML capturado num `iframe` visível (`contentDocument.write`): itens, valor a pagar, forma de pagamento, "CONSUMIDOR NÃO IDENTIFICADO", número/série, emissão com segundos, protocolo e o QR Code aparecem inteiros na largura de 80 mm.
2. **Detalhe do pedido** (`/orders/o2`, um pedido `authorized` com cliente de CPF `123.456.789-09`): o botão diz **"Reimprimir cupom fiscal"** e o cupom capturado traz `CONSUMIDOR CPF: 123.456.789-09` e o QR Code.
3. **Padrão (emissão desligada):** recarregue e responda `GET /fiscal/status` com `{enabled:false, provider_configured:false, environment:'homologacao', issuer:null}`. Confirme um pedido: a impressão automática dispara **na hora** (em menos de meio segundo) com **`CUPOM NÃO FISCAL`**, o botão diz **"Imprimir cupom"** e **nenhum selo** de NFC-e aparece.

Expected: os 3 comportamentos. Se algum divergir, corrija antes do commit. Desligue a impressão automática (`pdv-printer` com `enabled:false`) e pare o servidor de preview ao terminar.

- [ ] **Step 7: Commit só das alterações do C2**

```bash
git add frontend/src/utils/autoPrint.ts frontend/src/utils/autoPrint.test.ts frontend/src/components/fiscal/useOrderFiscal.ts frontend/src/components/fiscal/PrintReceiptButton.tsx frontend/src/components/fiscal/OrderFiscalPanel.tsx frontend/src/pages/orders/NewOrder.tsx frontend/src/pages/orders/OrderDetail.tsx docs/hardware/impressora-epson.md docs/fiscal/README.md
git commit -m "feat(impressora): imprimir o cupom fiscal no PDV e no detalhe do pedido, esperando a NFC-e assentar"
```
Confirme antes do commit que `git diff --cached -- frontend/src/pages/orders/NewOrder.tsx` **não** contém o estilo de hover do usuário (`hover:text-green-700 dark:hover:text-green-400`) e que `git status` mostra a remoção de `FiscalStatusLine.tsx` no mesmo commit.

- [ ] **Step 8: Conferir que nada ficou pendente**

```bash
git status --short
```
Expected: só a alteração guardada do usuário (fora do índice) e `.impeccable/config.json`.

- [ ] **Step 9: Devolver a alteração do usuário**

```bash
git stash pop
git status --short
git stash list
```
Expected: o `stash pop` devolve a alteração dele (` M frontend/src/pages/orders/NewOrder.tsx`) e o `stash list` fica vazio. Se acusar conflito, resolva mantendo **as duas** alterações.

---

### Task 5: Entrega e teste na impressora (portão de hardware)

**Files:** nenhum.

- [ ] **Step 1: Suíte completa, build e backend**

```bash
cd frontend && npx vitest run && npx tsc -b && npx vite build; cd ..
cd backend && python -m pytest -q; cd ..
```
Expected: frontend `165 passed`, `tsc` limpo, `built in`; backend `121 passed`.

- [ ] **Step 2: Entregar ao usuário e parar**

Apresente o resumo: o cupom fiscal está pronto e verificado no navegador com a API simulada (DANFE NFC-e com QR Code, homologação marcada, impressão automática que espera a nota assentar e volta ao comportamento de sempre com a emissão desligada); o impacto (**nenhum** até a emissão fiscal ser ligada); e o que falta: **(1) teste na Epson** seguindo os itens novos do checklist em `docs/hardware/impressora-epson.md` (layout, QR Code lido pela câmera do celular, corte), **(2)** as pendências do usuário para emitir de verdade (tabela do contador, CSC da SEFAZ-PE, provedor) e **(3)** as duas coisas ainda fora do cupom (tributos aproximados e endereço de consulta da SEFAZ), que dependem do provedor real e do contador. Depois: C3 (cancelamento, contingência, produção). Não faça deploy.
